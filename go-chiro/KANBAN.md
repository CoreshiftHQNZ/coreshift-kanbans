# Go-Chiro — Kanban

> Visual build state for the standalone practice management system. Edit this file and run `node tools/build.js go-chiro` from the coreshift-kanbans repo root to refresh the published board.
>
> **Card format:** `- **Title** \`tag\` \`tag\` — Description.`
>
> The column headings below are what the renderer looks for. Keep the leading emoji — it's how the engine assigns colours.
>
> **🎯 Current focus: M7 — retail.** Direction change 2026-07-21: Go-Chiro stops being a layer on top of Splose and becomes a complete, standalone practice management system. M1 (diary), M2 (native invoicing) and M4 (recalls, waitlist, intake, roles) are all in production. M3 — proving a full patient day end to end — is **blocked on Luke** (his ACC Vendor ID, and his time to run the rehearsal), so on 2026-08-01 Ricky pulled M7 forward: a lean clinic storefront for the clothing Luke sells. Retail shares no tables and no flows with the cutover, so the two run in parallel. Full plan: `docs/ROADMAP.md`, shop spec in `docs/SHOP.md`.
>
> Retrofitted into the working model on 2026-08-01 from the hand-rolled board of 2026-07-21. Cards below are preserved as written.
>
> **All three phases built; one piece of code left.** Schema, product admin, storefront, checkout and the fulfilment queue are live on staging and proven there — two real card orders, one taken to `collected`. The shop is held shut on production by `SHOP_PUBLIC_ENABLED=false`. Outstanding: the guest send path for the five retail emails, promoting PR #100 to production, and Luke's catalogue. Re-verified against the live systems 2026-08-18.

---

## 👉 On Ricky

- **🔴 The 11 patients and 165 appointments are still missing until the sync re-runs** `M3` `decision` — The fix is on PR #102 but nothing is repaired until it is deployed and a sync runs. **Still do not flip `SPLOSE_CUTOVER`.** Two things needed: merge #102 and promote it, and then decide how to prove the repair. Re-running the sync against staging would mean copying 310 real patients into an environment deliberately emptied of patient data — that is a privacy call, not a side effect of a bug fix, so it was not done. The alternative is to let the production sync run on its normal schedule after promotion and assert the counts against Splose afterwards, which is what I would do. Once it has run, the breakdown of what the 165 actually resolve to becomes readable straight off `splose_sync_logs.skip_reasons` instead of needing the Splose API.
- **🔑 Fix the staging Supabase Site URL — 30 seconds, unblocks all staging testing** `M3.5` `blocked-by-access` — Ricky could not log in to staging: every attempt bounced him to the live app, so the testing he thought he was doing on staging was happening in production. **Cause proven 2026-08-18:** the staging Supabase branch `khcuxlkhjscmlmasmwuw` has Site URL `https://go-chiro.com` and does not allow the staging origin, so Supabase falls back to Site URL. Confirmed with `GET /auth/v1/verify?redirect_to=…`, which returned `location: https://go-chiro.com` for both a bogus redirect *and* the staging origin. The client bundle is innocent — it correctly targets the staging project. **Fix:** https://supabase.com/dashboard/project/khcuxlkhjscmlmasmwuw/auth/url-configuration → Site URL `https://go-chiro-api-staging.up.railway.app`, and add `https://go-chiro-api-staging.up.railway.app/**` to the redirect allow-list. Claude cannot do this: the Supabase MCP covers SQL, migrations and branches but not auth config, and there is no `SUPABASE_ACCESS_TOKEN` on the machine — logged in `~/.claude/ENABLEMENT.md`.
- **Run the full-day rehearsal as a real patient** `M3` `decision` — This is the M3 gate and it is on you, because you are an actual customer of Luke's and the only person who can play the patient without touching a real one. Book, attend, have the note published with summary and reports, check homework lands in the patient app, and let the invoice auto-raise and auto-charge your saved card. Nothing else in M3 can be proved without it. Deferred 2026-08-01 — Luke was not working, so M7 was pulled forward instead.
- **Pick Luke's switch morning** `M3.5` `decision` — Audit done 2026-07-21. On the agreed morning Luke stops booking in Splose, we flip SPLOSE_CUTOVER=true and NOTIFICATIONS_PAUSED=false, and he logs in on his phone and adds Go-Chiro to his home screen. 79 future appointments, 332 clients and the booking config are already in the app. Not to be picked until the rehearsal passes.
- **Say when notifications go live** `M3.5` `decision` — Reminders, confirmations and recalls stay silent on staging and prod until NOTIFICATIONS_PAUSED=false on both envs. That flip is the moment patients start receiving mail from Go-Chiro rather than Splose, so it is your call and nobody else's.
- **Two-minute staging tidy-up** `M7` `ops` — Both are small and neither blocks phase 2. (1) Roll the staging Stripe **test** secret key — it was exposed in a session transcript on 2026-08-01 by a bad redaction regex. Test-mode only, so no real money or data is reachable, but roll it and re-set `STRIPE_SECRET_KEY`. (2) Delete the leftover `STRIPE_PUBLISHABLE_KEY` var from staging — it still holds an old **live** key from a third Stripe account (`51TEKvRH`) and nothing in the codebase reads it. Worth deleting from production too, for the same reason.
- **Decide on the identifiers left in this repo's git history** `ops` `decision` — Luke's provider and practice numbers were briefly committed to this public repo on 2026-08-01 (commit `0017a9c`). They are gone from the files and the live page but remain in the history. Leave it, or rewrite history and force-push main? Recommendation: leave it — those numbers appear on every ACC invoice Luke issues, so they are not secret. Your call either way, and it stays open until you make it.
- **Grab a full Splose export before cancelling the subscription** `M3.5` `ops` — Splose is month-to-month. Once the cord is cut it becomes a read-only archive for a period, but the export has to be taken while the account is still live.

## ✅ Done

- **Splose sync no longer loses patients to shared emails** `M3` `bug` `fixed` — PR #102 (split out of #101 so it reviews alone), migration 0030. Two defects found 2026-08-18 by chasing one name that did not match between Splose's diary and the Today screen. (1) `public.users` carried a blanket `UNIQUE (email)`; four Concord Rest Home residents share the home's admin address, so only the first could exist — production was missing **11 Splose patients** and **165 appointments** from 2026-03-01, the latter because the appointment sync skips any appointment whose patient it cannot resolve. Replaced with a partial unique index over login-capable rows only, which is what Supabase's `auth.users` already guarantees. (2) The worse one: the sync matched an existing user **by email alone** and overwrote its `splose_patient_id`, so on a shared address each new patient took the previous one's row and it ended up pointing at whoever the API returned last. Only an unclaimed row can be adopted now. (3) Both loops used to `continue` past anything unresolvable while counting nothing, so every run logged success while dropping records — each skip is now tallied against a reason into `splose_sync_logs.records_skipped` / `skip_reasons`. **Verified** on staging, 5 assertions passing, including that the exact drizzle query the fixed sync runs returns no stealable candidate; both databases pre-checked for login-capable duplicate emails before the constraint was dropped (zero on each); migration applied to staging and production and asserted; `tsc` clean and build passing.
- **Practice contact details, GST number and ACC identifiers all correct** `M3.5` `config` `shipped` — Set 2026-08-18 on both environments. Phone was the placeholder `+64 9 000 0000` and email was `admin@go-chiro.co.nz` on a domain the practice does not use; `server/services/pdf/invoice.ts:48` prints both in the header of **every** invoice, so that fake number was going out on all of them — and ACC submission had just been unblocked. Now `contact@go-chiro.com` and `+6421 133 1127`, which Ricky confirmed are the only contact details the practice uses. The email is also where the new shop-order alert lands, so that now reaches a mailbox somebody reads.
- **Today screen showed the wrong day's appointments** `M1` `bug` `fixed` — PR #101. `toApiDayRange` in `DashboardPage.tsx` built the range from the local calendar date then appended a literal `Z`, so in NZ the screen asked for noon today through noon tomorrow. **Proven against production for 2026-08-18:** the buggy range returned 9 appointments where the real NZ day has 8 — it dropped the 11:30am appointment, the first of the day, and appended two from tomorrow morning, which is what Ricky spotted against the diary. Fixed by deriving the boundaries as real instants from the existing local-time helpers; verified the window is now `2026-08-17T12:00:00Z → 2026-08-18T11:59:59.999Z` and that a January date yields `11:00Z`, so daylight saving is handled rather than hardcoded.
- **Phase 3 — fulfilment + notifications** `M7` `build` `shipped` — Orders page, the fulfilment state machine and the five retail emails. Verified 2026-08-18: two real orders taken end to end through hosted Stripe Checkout on staging, both with a payment intent and a `paid_at`, one driven to `collected` and one to `ready`; GST correct on both (717c on a 5500c inclusive total). The emails were wired on PR #101 — retail needed its own send path because `sendNotification()` starts from a user id and shop buyers are guests with no user row. Migration 0029 makes `notification_logs.user_id` nullable and adds `recipient_email`, so a guest send is still logged in the same table as clinical mail. Exercised against the staging database with Postmark deliberately unconfigured so nothing sent: paused wrote zero rows, unpaused rendered all five templates with zero failures and logged five guest rows. **Not verified: an actually delivered email** — `NOTIFICATIONS_PAUSED` is true on both environments.
- **ACC billing identifiers complete** `M3.5` `config` `shipped` — Settled 2026-08-18. Vendor ID `G0M661` on the practice, Luke's ACC provider number on his practitioner record, and the GST number on the practice — the last of which was validated against the NZ IRD check-digit algorithm before being written (9 digits, in range, primary weighting, check digit matched). Also set on staging so the rehearsal produces a realistic document. Contract ID is deliberately empty: Ricky's earlier recollection put `G0M661` there, it turned out to be the Vendor ID, and no code reads Contract ID. **Verified by rendering an actual invoice PDF** — it carries `TAX INVOICE`, `GST no: 141-567-543`, the GST (15%) breakdown, and the ACC block with Provider no. / Vendor ID / Claim no. Identifiers are on this board only where they already appear on every invoice Luke issues.
- **Tax invoices were missing the GST number** `M3.5` `bug` `fixed` — Found and closed 2026-08-18. `practices.gst_number` was null while `gst_registered` was true. `server/services/pdf/invoice.ts:52` prints the number only `if (gstRegistered && practice.gstNumber)`, so the line vanished silently while line 58 still stamped the document **TAX INVOICE** and line 145 still printed the GST breakdown — a document headed TAX INVOICE, showing GST, with no registration number. It affected every invoice the app issued, not only ACC ones. Fixed by data, not code. **Still worth a guard** refusing ACC submission while the number is null, so a future practice onboarding to Go-Chiro cannot repeat it — logged in This Week.
- **Real staging environment** `M7` `infra` `shipped` — Staging existed but shared production's database and held live Stripe keys — a second front door onto live data and live money, not an environment. Now on its own Supabase branch (`khcuxlkhjscmlmasmwuw`, ~$0.32/day), verified an exact schema match for production (identical fingerprint `bd37241…`, 425 columns, 32 tables) and empty of patient data. Railway repointed server *and* client: the `VITE_` vars mattered because `client/src/lib/supabase.ts` falls back to hardcoded production values when unset, so staging's login had been silently authenticating against the production auth server. Stripe now a matched **test** pair (account `51TzRyLH`), verified baked into the served bundle. Seeded with a practice and a practitioner login. **Verified end to end against the deployed app:** product created with sizes, stock driven to −3 without clamping, duplicate size 409, unknown id 404, and production still reads 0 products / 0 orders with its 338 users and 3,751 appointments intact. Getting here also required repairing the migration ledger and force-aligning the diverged `staging` branch (content proven byte-identical to `main` first).
- **Phase 1 — schema + product admin** `M7` `build` `shipped` — Migration 0027 plus the Products page: CRUD, sizes with stock steppers that don't clamp at zero, publish toggle, pre-order + expected date, flat shipping rate. Extended the same day with migration 0028 (product_images) so a garment can carry front, back and detail shots, and fixed a Tailwind width collision that had collapsed the size-label field to nothing. Verified end to end against the deployed staging app.
- **Shop spec** `M7` `spec` `merged` — docs/SHOP.md: the data model, the backorder and pre-order rules, order state machine, GST treatment, and what is deliberately out of scope. Written and scope-locked before any code, so the three build phases have something to be checked against. Merged to dev on 2026-08-01 via PR #98, on its own so it stays reviewable independently of the build.
- **Automated recalls** `M4` `shipped` — Live in prod. Nudges patients whose last visit was ≥ the practice's recall interval ago with nothing booked; throttled one-per-interval. Off by default (per-practice toggle + interval in Practice settings); rides the notification stack so it's silent until launch. "Clients due for a recall" card on the Today dashboard. PR #80, promoted to production 2026-07-21.
- **Waitlist** `M4` `shipped` — Live in prod. Add clients wanting an earlier/any slot (with optional preferred practitioner + note) from the Today dashboard; reception sees who to call when a gap opens. Auto-offer-by-notification is a later add-on (rides the notification stack once un-paused). PR #89, promoted to production 2026-07-21.
- **Online intake forms** `M4` `shipped` — Live in prod. Standard NZ-chiro new-patient form patients fill in their portal (details, GP, presenting complaint, medical-history checklist, injury/ACC, consent-to-treat); prompt on the patient home until submitted. Practitioner reads it per-client from the Clients list with flagged conditions surfaced. PR #92, promoted to production 2026-07-21.
- **Staff accounts & roles** `M4` `shipped` — Live in prod. Add team members in-app from the new Team page: invite as practitioner (gets a diary column) or admin (reception/billing login, no column), with role change + activate/deactivate. Portal invite emailed via Supabase. Migration 0024 (native practitioners). Fine-grained per-role permission enforcement is a follow-up. PR #86, promoted to production 2026-07-21.
- **ACC invoicing via email channel — cutover gate** `M3` `shipped` `inert` — Built and in production. One-click "Submit to ACC" emails a compliant invoice PDF (ACC billing block: provider no., vendor ID, claim no., NHI + service codes + GST) to providerinvoices@acc.co.nz, subject vendorId INV-n, one per email. Status tracking (submitted/paid/declined from ProviderHub). Billing & ACC settings screen for the IDs. Inert until Luke's Vendor ID is entered — see On Ricky. Electronic channel (8-day, auto-reconciliation) is the M5 upgrade. PR #78.
- **Splose cutover — built; sync resumed pending switch day** `M3` `shipped` — The reversible SPLOSE_CUTOVER flag is built and proven, but the audit showed Luke hasn't switched off Splose yet — so the sync is back ON to prevent silent divergence (anything he books in Splose keeps flowing in). On the agreed switch morning: Luke stops using Splose → flip the flag → app is sole source of truth. One env var, 30 seconds. PR #83.
- **Native invoice engine (ACC-aware)** `M2` `shipped` — Live in prod. Invoices born in-app (auto-drafted on note publish + manual), GST-inclusive maths, per-practice sequential numbering, GST-compliant PDF (practitioner + patient), ACC-portion vs patient co-pay split. Patient pays only their co-pay via Stripe. sploseInvoiceId now nullable (migration 0021). PR #75.
- **Day + week calendar views** `M1` `shipped` — The screen the practice lives in all day. Per-practitioner columns, venue/route-day awareness, mobile-first like the rest of the app. Splose's core job, replaced. PR #73.
- **Practitioner appointment CRUD** `M1` `shipped` — Create/edit/delete appointments for any patient, service, venue from the practitioner side. Before M1 the API only had confirm/cancel/reschedule — appointments were born in Splose or the patient wizard.
- **Drag-to-reschedule + conflicts** `M1` `shipped` — Drag appointments between slots/days with the existing overlap detection guarding double-bookings.
- **Quick-create patient from diary** `M1` `shipped` — Phone rings, new patient, book them in one flow — name + contact, full record filled in later (or by the M4 intake form).
- **Native booking source default** `M1` `shipped` — Flip appointments.bookingSource default 'splose' → 'native'. From M1 on, appointments are born here.
- **Security authz hardening** `M3` `shipped` — Authorization hardening across sessions, appointments and prompts, promoted straight through dev → staging → production on 2026-07-21. PRs #95/#96/#97, the last thing merged before the project went quiet.
- **Clinical loop — session → AI SOAP → publish** `v0` `shipped` — Freeform notes with auto-save, AI formatting into SOAP, editable note + patient-facing summary, publish completes the appointment and notifies the patient. The core differentiated flow.
- **Booking wizard + config + venues + routes** `v0` `shipped` — Patient self-booking (mobile vs clinic first, route days with capacity, NZ address autocomplete), appointment types, venues, weekly/fortnightly schedules, Route Today run sheet with optimization.
- **Stripe payments + auto-pay (live)** `v0` `shipped` — Pay Now, saved-card auto-charge, revenue dashboard. Live keys, webhook + signing secret verified in prod 2026-06-27.
- **ACC claims — capture, lodge, history** `v0` `shipped` — Point-of-care ACC45 capture, lodge tracking + claim numbers, ACC45/ACC18 PDFs, patient-facing claim history + download.
- **Homework + exercise library + messaging** `v0` `shipped` — Library CRUD, per-session assignment, patient do-the-work UI with real streaks, two-way messaging with practitioner replies.
- **Notifications — email + PWA push (paused)** `v0` `shipped` `paused` — Postmark email + VAPID web push across all templates, per-user prefs, reminders on 48h/2h cycle. Globally paused until launch. SMS is a graceful no-op by choice.
- **Patient portal + UX tiers 1–3** `v0` `shipped` — Auth (invite, magic link, reset), patient settings, cancel/reschedule, add-to-calendar, dark theme, design-system reskin, mobile-first with bottom nav.
- **Splose two-way sync (to be retired at M3.5)** `v0` `shipped` — Polling sync + durable write outbox with retries. Served its purpose in the Splose era — scheduled for deletion after cutover. Currently back ON deliberately, because Luke has not switched yet.

## 🟡 In Progress




## 🚫 Blocked
- **🔴 ACC submission is not covered by the pause switch** `M3.5` `risk` — Found 2026-08-18. `NOTIFICATIONS_PAUSED` only gags things routed through `sendNotification()`; the ACC path in `server/routes/invoices.ts` calls the Postmark API directly, so it sends regardless. `ACC_INVOICE_EMAIL` was unset on both environments, meaning it fell back to ACC's real `providerinvoices@acc.co.nz` inbox. Staging has now been given `ACC_INVOICE_EMAIL=ricky@coreshifthq.com` so the rehearsal cannot mail ACC; staging's Vendor ID is also still null, so it is doubly inert. Production is deliberately left pointing at the real address — that is the feature. The consequence to understand: there is no dry run in production. The first click is a real submission.

- **Run the full-day rehearsal** `M3` `blocked-by-luke` — The M3 gate. Needs Luke working and needs Ricky playing the patient. Deferred 2026-08-01 because Luke was not in. Nothing else in M3 can be proved without it.
- **Re-orient on the project after the July pause** `M3` `ops` — Nothing moved between 2026-07-21 and 2026-08-01. **Supabase half done 2026-08-01:** the live database was checked before migration 0027 and matched the board — head was 0026_intake_forms, 28 public tables, buckets avatars / exercise-videos / session-clips, and Luke is the only non-patient user. **Railway checked 2026-08-18** — project `go-chiro`, service `go-chiro-api`, both environments Online. Staging is `go-chiro-api-staging.up.railway.app` with `SHOP_PUBLIC_ENABLED=true`, `SPLOSE_CUTOVER=true`, a test Stripe secret key; production is `go-chiro.com` with `SHOP_PUBLIC_ENABLED=false`, `SPLOSE_CUTOVER=false`, a live Stripe secret key. `NOTIFICATIONS_PAUSED=true` on both, as recorded. **One correction to the record:** the two environments do *not* share a database. Staging points at its own Supabase preview branch (`khcuxlkhjscmlmasmwuw`, child of the go-chiro project, created 2026-08-01), which is why staging carries test products and orders while production has none. The branch is non-persistent, so treat anything in it as disposable.
- **Script the full-day rehearsal** `M3` `test` — The step-by-step run sheet with an evidence slot per step: book, practitioner sees it in the diary, session note published with AI SOAP, summary and reports visible in both the client record and the patient view, homework created and live in the patient app, invoice auto-raised, saved card auto-charged. Named evidence per step, not "looked fine".
- **Fix whatever the rehearsal breaks** `M3` — The point of a rehearsal is that it fails somewhere. Whatever it surfaces is M3 work, not a new milestone — it is needed for M3's doneWhen to be true.
- **Un-pause notifications (launch)** `M3.5` `blocked-by-ricky` — Flip NOTIFICATIONS_PAUSED off on both envs — reminders, confirmations, recalls go live. Gated on Ricky's say-so. Note this also gags every shop email, so retail launching before this flip means buyers get Stripe's receipt and nothing from Go-Chiro.

## 🔵 This Week

- **🔴 Parallel sessions share one checkout — `git add -A` is unsafe here** `infra` `follow-up` — 2026-08-18: a commit made with `git add -A` in `Repo/go-chiro` swept up 33 paths of an in-progress email-templating refactor belonging to a **different session working in the same directory**, and pushed it under a message describing only the Splose sync fix. Caught and unwound the same session — the work was restored to uncommitted and nothing was lost, and the branch was force-pushed to drop the polluted commit. The underlying hazard remains: several sessions run against one working tree, so any blanket stage can capture another session's half-finished work, and one session's branch switch could clobber it outright. **Options:** give each session its own git worktree (the repo already supports it — this split was done in one), or make it a standing rule that commits stage explicit paths and never `-A`. Recommend the worktree route: a rule only holds until somebody forgets. Meanwhile the email refactor is sitting uncommitted in the shared tree and would be safer committed to its own branch soon.
- **Audit the `${date}T00:00:00Z` pattern on the server** `M3.5` `bug` — The Today screen bug fixed on PR #101 was a local calendar date with a literal `Z` stapled on, which in NZ shifted the window 12 hours. The identical pattern is still in `server/routes/booking-config.ts:632`, `server/services/routing.ts:182`, `server/routes/booking-bookings.ts:88` and `server/routes/reports.ts:59`. Those receive a date string from a caller rather than reading a local clock, so the semantics genuinely differ and they may be correct — but the same 12-hour shift on **booking availability** would offer patients the wrong slots, and on **reports** would put takings in the wrong day. Not changed on PR #101 because rewriting booking-critical date logic untested is worse than the bug. Wants a test per call site, then a fix where it is actually wrong.
- **Weekly ACC billing run, not per-patient clicks** `M3.5` `build` — Luke bills ACC **weekly in a batch**, not one patient at a time (Ricky, 2026-08-18). What is built is a per-row `Submit to ACC` button on each invoice in `PaymentsPage.tsx`, so his weekly run would be N separate clicks with no record of which he had already done — workable but wrong, and easy to double-send or miss one. **Constraint that shapes the fix:** the 2026-07-21 spike recorded in `docs/ROADMAP.md` says ACC's email channel requires *one invoice per PDF, one PDF per email* — so a week's billing cannot be collapsed into a single email on the channel M3 ships. Splose batches because it uses the eBusiness gateway (channel 3), which does take schedules. So the fix is a batch **action**, not a batch document: one `Submit week to ACC` button that selects every invoice with an ACC portion still at `not_submitted`, shows the count and total first, then sends them one email each, reporting per-invoice success or failure and marking each one as it goes. Idempotent by construction — it only ever picks up `not_submitted`, so a retry after a partial failure cannot double-send. Needed before M3.5 go-live: a 30-click first billing run is a bad first day. Not needed for M7.
- **Ask Luke what "weekly" means at ACC's end** `M3.5` `question` — He is in on 2026-08-18. Worth settling while he is there: does Splose send ACC **one submission containing the week**, or a weekly run of individual submissions? It decides whether the app can stay on the email channel for his real workflow or whether the M5 eBusiness-gateway upgrade moves forward in the plan. He is the only person who has actually watched this happen for the last few years.
- **Guard ACC submission on the GST number** `M3.5` `hardening` — Three lines in `server/routes/invoices.ts` beside the existing Vendor ID check: refuse to submit when the practice is GST-registered but has no GST number, the same way it already refuses without a Vendor ID. Luke's data is now correct so nothing is currently exposed, but the app is meant to be sold to other practices and the failure was silent — an omitted line on a document still headed TAX INVOICE. Money-critical file, so it wants a test alongside it.
- **First real ACC submission** `M3.5` `test` — **No longer blocked** as of 2026-08-18: Vendor ID, provider number and GST number are all in production, and the claim number comes per-claim off the ACC45. Needs an actual ACC invoice to exist and someone to click Submit. Rehearse on staging first — it now points `ACC_INVOICE_EMAIL` at Ricky's inbox, and its Vendor ID is null, so it cannot reach ACC. In production there is no dry run: the first click is a real submission.
- **Promote M7 retail to production** `M7` `ops` — PR #100, `staging` → `main`. Production has no shop code at all while its database is already at migration 0028, so Luke cannot load his catalogue there until this merges. Safe to merge before launch: production has `SHOP_PUBLIC_ENABLED=false`, so the public routes 404 until that flag flips. Merging it is a production deploy, so it is Ricky's call.
- **Luke loads the real catalogue** `M7` `blocked-by-luke` — One pass of data entry, in production, after PR #100 merges. Doing it on staging instead would have to be redone, and the staging database is a non-persistent Supabase branch that can be reaped.
- **The `Deploy` workflow is broken** `infra` `follow-up` — `.github/workflows/deploy.yml` uses `bervProject/railway-deploy@main`, which fails with `railway: not found` and warns on invalid inputs. It has been failing on every run, not just recent ones. Railway auto-deploys from the watched branch anyway, so nothing is actually broken in the pipeline — this workflow is a redundant legacy step producing permanent red X's on the repo. Delete it or fix it; red CI that everyone ignores is worse than no CI.
- **Migration ledger drift — watch for a repeat** `infra` `follow-up` — `supabase_migrations.schema_migrations` was missing its first four migrations, including `001_initial_schema`, because the earliest ones were applied by drizzle push or the dashboard rather than `apply_migration`. Repaired 2026-08-01 (23 → 27 rows), which is what made the staging branch buildable. Anything applied outside `apply_migration` from here re-opens the same hole and silently breaks the next branch, clone or restore.
- **Clinical Stripe calls pin an API version a new account rejects** `M6` `follow-up` — `routes/invoices.ts`, `patients.ts`, `booking-bookings.ts` and `stripe-webhook.ts` all pin apiVersion `2024-11-20`. The live account accepts it because it predates that version, but a newly created account rejects it outright — which is exactly how every shop checkout failed on the staging test account until retail was switched to the SDK's own version. Nothing is broken in production and those files are money-critical, so they were left alone. This will bite the moment M6 onboards a practice on a fresh Stripe account.
- **Pending orders accumulate from abandoned checkouts** `M7` `follow-up` — The order row is written before Stripe is called, deliberately, so a completed session always has a row to match on. The cost is that abandoning the hosted page (or any Stripe error) leaves a `pending` order behind. Harmless — they never paid — but the phase 3 Orders page must filter them out by default, and a sweep that expires pending orders after a day or two would keep the table honest.
- **Drag-to-reorder the catalogue** `M7` `follow-up` — The phase 1 plan said "drag-order" and it was not built. The `sort_order` column exists and both the API and the page honour it, so ordering works — there is just no drag UI, and nothing sets it away from 0 today. Not needed for the milestone's doneWhen (Luke can load and sell a catalogue without reordering it), so it is a card rather than a blocker. Cheapest version: up/down arrows on each row, not a drag library.
- **System-of-record hardening (remaining)** `M3.5` `infra` — Now the source of truth for health data: confirm Supabase backups/PITR, add a basic audit trail, review against NZ Privacy Act + Health Information Privacy Code 2020. Must be done before the cord is cut, not after.
- **Full history migration audit** `M3.5` — Audit what Splose holds that the sync doesn't pull today (historic treatment notes, patient files/attachments, ACC claim + invoice history) and migrate it, then run the final full import via the existing sync — its last job.

## ⚪ Backlog

- **Reporting pack** `M5` — Revenue, appointments, no-shows, per-practitioner — simple and glanceable, not enterprise BI.
- **Xero export** `M5` — NZ small business runs on Xero. CSV export first — buys enormous goodwill for little work; API integration later if it earns it.
- **ACC electronic channel upgrade** `M5` — Move from emailed PDFs (M3) to ACC's eBusiness gateway / Invoicing API: straight-through processing, ~8-day payment, electronic remittance + auto-reconciliation. Prereqs: Luke's HealthSecure .pfx + passphrase (likely already held — Splose required uploading the practice's own HealthLink certificate, so this is a config exercise, not a registration slog) and vendor onboarding with ACC digital operations. Plus declined-invoice rework flow.
- **Tenant onboarding + billing (parked)** `M6` — Signup/onboarding for other practices, plan billing, product marketing site. Name locked: Go-Chiro. Parked until the system is proven in Go Chiro's daily use.
- **ACC18 work-capacity capture form** `backlog` — Carried over: ACC18 generator exists but work-capacity + work-modification sections are placeholders. Needs capture form + schema + PDF wiring. Own workstream.
- **Exercise videos via YouTube link** `backlog` — Decided: URL field + embedded player. Carried from the v0 backlog, never scheduled.
- **Retail revenue in the reporting pack** `M5` `M7` — Shop orders are a separate table from clinical invoices on purpose: different tax treatment, and ACC must never touch a hoodie. Both need to land in the M5 reporting pack as separate revenue lines.
- **Shop discount codes** `M7` `follow-up` — Deliberately out of v1. Stripe Checkout supports promotion codes natively, so this is a config flag plus a UI toggle later, not a build.
- **Fine-grained per-role permission enforcement** `M4` `follow-up` — Staff accounts ship with practitioner/admin roles and a diary column distinction, but per-role permission enforcement was explicitly left as a follow-up when roles shipped in PR #86. Two concrete inconsistencies found while building M7 phase 1: `routes/exercises.ts` gates exercise *creation* on the role being exactly `practitioner`, so a practice_admin cannot add an exercise (while the PUT/DELETE on the same file accept both) — meaning promoting anyone to admin silently costs them a capability. And there is no "owner" role at all, which is why the shop had to gate on the practitioner/admin/super_admin trio rather than the owner-only gate the spec asked for.
- **A real owner role** `M6` `follow-up` — docs/SHOP.md wanted the shop restricted to "owner and admin"; the role enum has no owner, and Luke — who owns the practice — is a `practitioner`. So the shop uses the same trio as every other practitioner surface. The distinction only starts to matter with a second practitioner who should not manage retail or see revenue, which is a M6 (SaaS-ready) concern. Doing it properly means an owner role plus a permissions matrix, not another special case.

## 🅿️ Parking Lot

- **Delete the Splose sync code** `M3.5` `deferred` — Roughly 1k lines of polling sync, outbox and write-backs become dead the day the flag flips. Deliberately kept for a safe archive period after cutover rather than deleted at the switch, so a rollback is still possible. The unverified Splose cancel/reschedule write-back payload follow-up dies here too.
- **Make all splose_* columns nullable** `M3.5` `deferred` — Schema cleanup that belongs with the sync deletion, not before it.
- **SMS notifications** `deferred` — SMS is a deliberate graceful no-op; the notification stack is email + PWA push only. Revisit only if patients demonstrably miss reminders after go-live.
- **Rebranding the codebase** `M6` `deferred` — The repo evolves rather than being restarted (21k lines of prod-proven code with the right architecture). A rebrand happens if and when it becomes a product, not before.
