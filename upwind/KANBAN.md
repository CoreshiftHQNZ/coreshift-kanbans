# Upwind — Kanban

> Visual build state for the core product. Edit this file and run `node tools/build.js upwind` from the coreshift-kanbans repo root to refresh the published board.
>
> **Card format:** `- **Title** \`tag\` \`tag\` — Description.`
>
> The five column headings below are what the renderer looks for. Keep the leading emoji — it's how the engine assigns colours.
>
> **🎯 Current focus: retail go-live at US$49, then the Partner Program.** Plan approved 2026-07-17: Track 0 (global USD reprice — shipped) → open retail → P1 per-site billing → P2 partner rails (white-label reseller model) → P3 commission ledger + payouts-first partner dashboard → P4 retention/inputs/partner marketing. Full plan in the repo session plan file.

---

## 👉 On Ricky

- **Legal sign-off on the Partner Agreement** `M4` `legal` — This is the document a recruited partner accepts, so it gates selling at any scale beyond us. Ricky, 29 Jul: *"I will get legal to check your change before we go live for real."* The commission model changed after the draft (100% of build fee less real payment costs and 5%).
- **A purchase with a build fee over US$60** `M5` `decision` — The only way to get a matured commission worth paying. Under US$60 the partner nets less than the US$50 minimum and never appears in the payout list at all.
- **Fund the Openprovider balance** `M7` `decision` — Sits at US$0.00. Domain search and quoting are live; registration cannot be built against an unfunded account.
- **Approve or reject `SCOPE-one-key-set-per-environment.md`** `M6` `decision` — Removes `STRIPE_MODE` and the `*_TEST` twin key sets so any future developer can see which keys an environment uses. Needs two verification purchases, so it wants a clear morning.
- **Prod's `*_TEST` Stripe secrets belong to a different Stripe account** `M6` `security` — Fragment `HXnhNJQhs8`, not Upwind's `HtrmVIubMC`. Dormant under `STRIPE_MODE=live`, live ammunition if it flips; it has already misrouted one production payment once. Replace or delete.
- **Inbox placement, iCloud + Gmail** `M4` `launch` — Not checked since the Postmark token swap. Custom SMTP and the raised rate limits went in on 30 Jul, so the throttle is no longer the blocker.
- **Request zero-data-retention from Anthropic, and get a DPA signed** `privacy` `PIA` `legal` `ZDR` `IPP-12` — Confirmed 31 Jul: ZDR is **not** enabled, so Upwind's prompts sit in Anthropic's standard retention window (understood to be 30 days). Enabling it is an email to Anthropic, costs nothing, and deletes a whole line of questioning from this and every future PIA. Two things to keep straight: **(1)** ZDR does *not* answer the cross-border IPP 12 question — Anthropic processes in the US, so the data leaves NZ during processing either way; that call stays with the Privacy Officer. **(2)** the bigger gap is that there appears to be no signed **DPA** with Anthropic — a sub-processor handling personal information without one is a cleaner audit finding than a retention window. Not a launch blocker: what we send is already-public website copy, no credentials / payment / lead data, and volume is single-digit generations today. Cheap now, annoying at 500 customers.

## ✅ Done

- **📬 Journey #3 · Screen 3 — "check your email"** `partner` `ux` `trust` `shipped` — The hop out to an inbox and back is the most fragile link in the chain and everyone lost there is lost silently. **Three of the listed faults were already fixed** and the card had not caught up: the address *is* named in bold, the junk-folder note and a way back exist, and the "Looking to become a partner? Apply here" link plus the wrong "Sign in to your sites" heading live in the standalone `SignIn` wrapper — which the claim page has never rendered since the SignInForm/SignIn split. The four-step rail also already sits outside the sign-in card, so it stays visible throughout. **What was genuinely broken:** pressing *Send it again* re-rendered a byte-identical screen, so the only evidence the click had worked was the email itself — the very thing the person is unsure about — and there was **no cooldown at all**, so that instinct went straight into the Supabase auth rate limit raised on 30 Jul precisely because the magic link is the whole front door. Now a 30s cooldown that starts on the **first** send (otherwise the button is live the instant the screen appears), counted down on the button face rather than merely greying out, plus a "Sent again." confirmation. *Wrong address?* clears the cooldown deliberately — someone who typed `gmial.com` must not be made to watch a timer before fixing it, which is the entire reason that route exists. **The bigger find: the PARTNER front door still had the original dead end** — "✓ Check your email for a sign-in link" replacing the form, no address, no resend, no way out — long after retail had been rebuilt away from exactly that. Partner recruitment is what M4 is blocked on and every recruited partner passes through this screen. Both surfaces now share `useMagicLink()` + `<MagicLinkSent/>`; they drifted because there were two implementations. The partner form also now collapses entirely on send — its consent tickbox was staying live after the action it gates had fired, with a second sign-in route beside it. **Verified on production**, auth request intercepted so no email was sent: one request fires, the screen names `agency@example.com`, the button reads `Send it again in 30s` and is disabled, consent box / Google button / email field are all gone, and only the two escape hatches remain. On staging the countdown was watched ticking 20s → 19s, and *Wrong address?* returned to the form with the address preserved and submit immediately re-enabled. 639 tests (+19), `tsc` clean. `#60`, `#61`.

- **📜 A consent record now means someone consented** `legal` `trust` `shipped` — Two defects, and the second matters directly to the Partner Agreement sitting with the lawyer. **(a) The Partner Agreement had never been recorded as accepted by anyone.** The partner signup box reads "I agree to the Partner Terms, Platform & Hosting Agreement and Privacy Policy", but the only writer hardcoded `document='platform_agreement'` — all 14 rows in prod's `terms_acceptances` are that one document, and the `kind` column has never held anything but its default. **(b) The rows that existed were not evidence.** `recordTermsAcceptance()` was called from `App.tsx` under `if (session)` — every sign-in, every reload, no reference to whether anyone ticked anything. The 13 rows at `2026-07-17` are 13 sign-ins, and bumping `TERMS_VERSION` would have filed a fresh "accepted" row for every existing user for a document none of them had been shown. A manufactured consent record is worse than a missing one: a gap is obviously a gap, this looked like proof. Consent is now noted at the moment of the act (on submit — ticking then closing the tab accepts nothing), carried across the magic-link roundtrip because there is no `user_id` at the moment they act, and written once a session exists; nothing pending means nothing written. Each surface records the documents it actually names. `kind` is derived server-side so it cannot disagree with the document, and an unrecognised document is refused rather than filed under a name nothing reads back. **Deliberately not backfilled** — there is no honest way to write "this partner accepted the Partner Agreement" for someone whose acceptance was never captured. 620 tests, `tsc` clean; staging verified `kind` derivation, idempotency, and refusal of both a typo'd document and an empty version. `1b17386`.

- **🔐 Staff can't claim a customer site, because they never could** `infra` `shipped` — The claim path still inserted the caller as `owner` of the account it creates, so claiming as staff put a staff membership straight back into a customer workspace — the exact grant `20260730190000` removed. Prod had one: `ricky@coreshifthq.com` owning "Best Chef 厨匠" since 06:33 on 30 Jul. **The card's suggested fix — skip the membership insert — would have been worse:** it creates an account with a site and no members, which nobody can manage and nothing can pay for. The deeper point is that a staff claim cannot work even when it appears to, because `ensure_personal_account()` resolves staff to Coreshift HQ, so they own on paper an account they cannot open. So it refuses. The refusal raises, rolling the transaction back, so `claim_token_used_at` is never stamped and the link still works — a guard that burned the token would turn one wrong click into a dead invitation for a real customer. **Also guarded client-side, and not as belt-and-braces:** the auto-claim effect stashes the token in `localStorage` and clears it only on success, and App routes to ClaimPage while that key exists — so hitting the database refusal would have locked staff out of their own dashboard until they cleared browser storage, and staff are the people most likely to click a claim link. Staff lose nothing: `is_app_admin()` already reads everything for support, and agency access runs through `account_links`. Verified on staging both ways (refused as staff with the token intact; the same token then claimed successfully as a non-staff user). `d540df2`.

- **💰 Show every amount owed, not just the payable ones** `M5` `money` `shipped` — The by-hand payout fallback depended on a screen that said *"No matured commission to pay right now"* over a ledger holding real money. Staging had **US$451.97** behind that sentence, all inside the 14-day maturity hold. **The card's own diagnosis was wrong, and so was LAUNCH-CHECKLIST #35:** passing a lower minimum would have shown nothing. `payout_eligibility()` filters `matures_at <= now()` *first*, then applies `having sum(...) >= p_min_cents` to only the rows that survived — and production holds matured clawbacks of −2734 against held commission of +2734, so the `having` fails at **every** minimum including 0. Checked both minimums against staging through the real RLS path before writing any code. Fixed with `partner_owed_summary()` (migration `20260731100000`), a read-only RPC returning held / below-minimum / net-negative partners as *columns* rather than erasing them, so the screen decides what to show. `payout_eligibility()` is untouched and stays the sole authority on who a run pays. **Second half nobody had spotted:** `markPayoutPaid()` sent no `min_cents`, so the function looked the partner up in `payout_eligibility(5000)` *before* reserving — for a US$27 partner that hit the `eligible.length === 0` early return and returned **HTTP 200 having done nothing**, while the UI printed "Marked paid." Now sends `min_cents: 0` (the reserve call already intended it; the lookup above it disagreed) and reports what actually happened. Mark-paid is offered only on matured money with a positive net and no flag — on a negative net that button would have written off a debt owed *to us*. Also revoked `EXECUTE` from **PUBLIC**, not just `anon`: `CREATE FUNCTION` grants to PUBLIC and `anon` inherits it, so `anon` could call a payouts function (it returned nothing — `is_app_admin()` gates the rows — but the row filter shouldn't be the only defence here). Verified: 611 tests (+21), `tsc` clean, RPC returns the held partner as an authenticated admin and permission-denies `anon`, grants now exactly `authenticated`/`postgres`/`service_role` on both projects, migration applied by hand to prod + staging with both version stamps corrected. `4431fac`.

- **🧭 A preview can be walked around** `bug` `ux` `shipped` — Clicking anything in a preview's navigation ejected the visitor to the partner dashboard. Site links are real root-relative paths (`/about`, `/contact`) — correct on the published host where a Worker serves them, but a preview lives at `upwind.build/?preview=<subdomain>`, so the click dropped the query string and hit the dashboard's router. `ReadOnlySite` also rendered `slug="home"` with no page state, so even a correct link had nowhere to go: a one-page trap on the screen a partner shows a customer. Internal paths now switch page in place; anchors, `tel:`, `mailto:`, external links and unknown paths are untouched. `?page=<slug>` in the URL makes refresh, shared links and the browser Back button work. Verified on production (`tk-steel`) and staging (`couttas-bar-grill`, 4 pages).

- **💸 The refund clawback, closed against real live events** `money` `shipped` — Two live US$59 refunds on 28 Jul reversed nothing: the handler read `obj.invoice` on a charge, a field Stripe removed in the 2025 API, so it returned 200 and wrote no clawback. Both partners stayed credited for money that had been given back — **US$27.34**. The fix matches by CHARGE (every commission row already stores `stripe_charge_id`) and logs when a refund matches no commission, so it can never be silent again. **Resending the two events was blocked by our own idempotency guard** — the event ids were already in `stripe_events` from the buggy delivery, so the replay was acked as a duplicate without processing; the dedup rows had to be dropped first. Verified in prod 2026-07-30: 4 clawback rows (−466, −901, −466, −901), each carrying the charge id of the commission it reverses, `matures_at` set (the old null made every clawback invisible to the payout selector), **net US$0.00**.
- **📧 Custom SMTP + auth rate limits, both environments** `launch` `infra` `shipped` — Postmark SMTP configured and the hourly email limit raised on production AND the staging branch. Staging mattered as much as prod: the user-testing invites point testers at `staging.upwind.build`, and at the built-in mailer's default of a handful per hour a group of testers starting together would have silently locked each other out — which reads as "the tool is broken", not "the mail is throttled".
- **🧪 User-testing round — a form, a scoring model and an admin sender** `launch` `ux` `shipped` — Observed testing without sitting next to anyone: task / hint / success-rating per step, timed from page load, at **testing.upwind.build** (no password — a password is a task in itself and would have cost us testers). Answers are **anonymous** and submitted through a `SECURITY DEFINER` RPC, so `anon` holds no write grant on the tables at all; they collate for analysis. Two tracks, split at Ricky's call rather than one form pretending to serve both. Plus a **pretty HTML invite email with buttons** and a **Send testing invite** tab in the admin area, from `hello@tryupwind.com` via Postmark, with step-by-step instructions. Testers are pointed at **staging** and Stripe's `4242` test card, never at production. Temporary by design: the `research/` directory, the `testing` admin tab, the `send-testing-invite` function and its deploy step all come out together when the round ends.
- **🔎 Lead finder — pick an area, pick an industry, press Run** `growth` `shipped` — Google Places discovery into a `found` holding stage that is deliberately absent from the pipeline board: sales never sees a lead until a human promotes it. `lead_runs` records each run. Rate-limited at 10/hour through `api_rate_limits` rather than by counting rows, so history cannot be deleted to reset the cap — and `authenticated` has no DELETE on that table anyway. **The 29 Jul default-privileges change protected this table for free:** created afterwards, it picked up no anon write grants without anyone remembering the rule existed. That is the difference between fixing an instance and fixing a class.
- **🛒 Buy-a-domain, honestly** `billing` `shipped` — The two-option modal, real availability search and one stated price (**US$5/mo**) are live; the registrar is **Openprovider** after Dynadot failed on `.co.nz`. What is NOT built is the registration itself — see the In Progress card. The offer used to be advertised with nowhere to go; now the search is real and the last leg is a fulfilment ticket.
- **🖥️ Modals fit a 13" laptop, and stop closing themselves** `ux` `shipped` — Two bugs on the same surface, both mine. (1) The card had no `max-height`, and a centred flex item overflows in **both** directions while a `position: fixed` backdrop has nothing to scroll — so on a MacBook Air the "no website yet" step ran off the top AND the bottom, and **Continue was unreachable**. The card now bounds itself to the viewport, scrolls internally, and pins its action row. (2) Highlight text, drag past the card's edge, release — and the modal closed, taking the form with it, because a `click` fires on the nearest common ancestor of mousedown and mouseup. Five backdrops had it; all now close on `onMouseDown` guarded by `e.target === e.currentTarget`, with a test across every backdrop in the app.
- **👥 All @coreshifthq.com staff share one account** `infra` `shipped` — Ricky, Russ and Keitha in **one** Coreshift HQ account: build a site, and anyone can send the offer. Auto-join by email domain already existed; what was broken was which account the dashboard *worked in* — `ensure_personal_account()` did `limit 1` over `members` with **no ORDER BY**, and every staff member had two memberships. Already biting: a `won` prospect had its agency pointed at a personal account. Staff now always resolve to Coreshift HQ; everyone else gets their oldest membership, deterministically. Cleanup moved child rows first — `prospects.agency_account_id` is `ON DELETE CASCADE`, so deleting the personal accounts first would have silently eaten a `won` prospect.
- **🔴 The claim-page preview was dead for logged-out visitors** `bug` `shipped` — "Loading preview…" forever, and the site picture never appeared. `anon` does not hold a table-level SELECT on `sites`; it holds a **column allowlist**. `archived_at` shipped on 29 Jul, the claim page both selects and filters on it, and it was never granted — and Postgres answers `42501` for the **entire row**, not the column. One ungranted column took out the whole read for the duration of the testing round. Granted that column only; `plan` and the Stripe ids stay invisible. The table now carries a comment naming the allowlist, because there was no other way for the next person to know the rule exists.
- **⚡ Cancelled sites stop being served, and alerts stop shouting** `infra` `shipped` — Two failure modes with the same shape: correct code, drowned out. (1) The renderer sets `s-maxage=86400`, so a cancelled site kept serving a **22.5-hour-old 200** while the paywall correctly returned 402 on a miss. Cancel now purges, sharing the code with publish; a purge failure can never fail the webhook, because Stripe would redeliver and re-run the cancellation. That also killed `const ROOT = 'upwind.page'` — hardcoded in a function staging deploys too, so a staging publish purged the **production** zone. The root is now read from the zone being purged: one source for both, so they cannot disagree. (2) An ongoing incident emailed **every 15 minutes** — at-most-once per error row is not at-most-once per fault. One fingerprint now escalates once per 4 hours, using markers written with `alerted_at` pre-stamped so they can never be claimed as errors themselves. Suppressed incidents stay in the digest: quiet, not hidden.
- **🌿 Real environments at last — Supabase branching, wired and proved** `infra` `shipped` — Ricky's call, and the right one: *"this isn't an MVP, we really want to set up for success here."* Everything this week had been walked against **production** because there was nowhere else — the staging branch had sat in `MIGRATIONS_FAILED` since 28 June with 17 tables against prod's 27, missing the entire partner and money layer. **Root cause was not drift:** Supabase branching only reads `supabase/migrations/`, and ours had always lived in `db/migrations/`. It was pointed at an empty room for a month. **Repairing the 33 hand-written migrations to replay from empty was the obvious fix and the wrong one** — several were reconstructed from a database that already existed, so making them replayable is archaeology with no guaranteed end. Ricky's instinct — *"could that be removed by dumping and copying main?"* — was correct: a baseline dumped out of production is **guaranteed** to replay, because it came from something that already runs. 33 files squashed into `20260728000000_baseline.sql` (27 tables, 36 policies); the originals archived with a README explaining why nothing executes them. **Staging rebuilt from it and matches prod exactly** (27 tables, 67 functions, 36 policies, 8/8 key tables). **Production wiped to a clean slate** — Ricky confirmed no real customers, so it now holds his login, his personal account and Coreshift HQ (partner #0, structural) and nothing else; full schema + data backups taken first. **GitHub integration connected** (repo renamed `CoreshiftHQNZ/upwind` first, so it wouldn't need doing twice): working directory `saas-platform`, production branch `main`, auto-branching capped at 3 and only on Supabase file changes. **Proved end to end** with a throwaway PR (#13): a preview branch provisioned its own database, replayed the baseline **into an empty Postgres**, applied the PR's migration on top (28 tables = 27 + the smoke table, 67 functions), and tore itself down on close — with production untouched throughout (27 tables, 1 user, 0 leakage). Also: `seed.sql` (a partner, a finished unsold site, a priced prospect with a fixed claim token — the cafe-owner journey one URL away), and `ENVIRONMENTS.md`. **The CLI is now deliberately linked to STAGING** so an absent-minded `db push` lands somewhere harmless. *(Two dashboard fields saved silently-wrong on the first attempt — the working directory became `.saas-platform` because the `.` was real content not a placeholder, and the staging branch's git-sync silently discarded. Both caught by re-reading rather than trusting the toast; worth knowing when editing these by hand.)*

- **🧹 Dashboard tidy — one rule: anything about a website lives on that website's card** `ux` `dashboard` `shipped` — Ricky flagged the customer dashboard as cluttered. It was structural, not cosmetic: **four bands stacked above the sites** — "X isn't live yet", "put your own web address on it", "finish your site", and a partner contact card — where each belonged to a *specific website* and none of them said which. With two sites they actively contradicted each other: two near-identical green bands about two different sites, sitting above cards that already carried the same actions under different names (*Connect your domain* / *Add custom domain*, *Set my site live* / *Publish · US$49/mo*). Five competing CTAs before you reached a website; even a single-site customer got two banners restating their own card. **Everything site-level moved onto its card**, and the top strip is now reserved for genuinely account-level news — empty most of the time. The card gained the thing it never had: **the site's name, beside its status** — recognition had been resting on the thumbnail, which works right up until two sites are built from the same template and the previews are identical. **One primary action per card**: unpaid opens the go-live modal rather than the two bare price buttons that used to sit there (those called Stripe straight off the card, so the partner's build fee never appeared anywhere the customer could see it before they were charged); Delete drops from a red button at equal weight to a quiet link. **"Finish your site" became the suggestions dropdown agreed in the Screen 8 walk** — a collapsed line inside the card, instead of a full-width orange band that was often one bullet and could never say which site it meant. *(The partner contact card was already in the left sidebar from the white-label work and had simply been left on the page twice.)* **Also found while checking it on a phone:** that sidebar partner card had no rule for the collapsed mobile strip, so a 232px block laid out in a row **pushed the whole dashboard sideways** — horizontal scroll on the device a cafe owner is most likely to open it on. The strip now wraps instead of scrolling, which also stops Sign out hiding off the right edge. Verified at 1280 and 375, 44/44 tests, LIVE.

- **🍰 Journey #3 build — waves 1–6: the cafe owner's path, rebuilt** `partner` `ux` `trust` `billing` `shipped` — Everything logged across Screens 2–10 of the walk, built and verified on production. **Claim page (W1)** is now the landing-page version of the invitation: partner brand bar, the real site embedded **live and scrollable** in the hero, the partner as a person with the line they wrote, the four-step rail, cost split by who receives it, and "Nothing here is a trap" carried over from the email. Signed in, it claims and hands off rather than asking again. **Check-your-email (W1)** names the address, warns about junk, and offers resend + "wrong address?" — the dead end is gone; consent is no longer remembered across agreements (`rememberConsent=false` on the claim page), so accepting Partner Terms can never pre-tick a customer's US$1,449 commitment. **Payment (W2)** is a modal over the customer's own softly-blurred dashboard — no cross, click-out dismisses — with build fee + first period + **Due today** + "Then X" itemised, annual marked *Best value · 2 months free*, the reassurance list, and the partner's phone as a `tel:` link. **Their dashboard (W4–W5)**: partner card under the left nav (identity, not a support channel — see the support-boundary decision), the domain banner cut to two buttons, greeting de-named, editor's stale "Review & publish" copy fixed, tour forked by user type, Publish → **Update my site** once live. **Domain step (W6)** rebuilt from a bare text box: warns that connecting replaces whatever the address shows today, registrar steps in plain language (plus what an ALIAS/ANAME is called when the registrar calls it something else), a green "your email, calendars, Google, Facebook and Instagram are untouched" band, propagation times, and "ask us to do it with you" under every state. Placeholder is now the bare `yourbusiness.com` per the apex-primary policy. "Buy a domain · US$10/mo" no longer opens the connect form — it opens an honest concierge request until #104 lands. **Also shipped from the Screen 5 list:** the partner is named on the Stripe build line ("Website build by Fernoak Digital"), so they survive the handover to a merchant of record the customer has never heard of, and the **promo-code box is gone**.

- **🔴🐛 The build fee was shown to the customer and never charged** `partner` `billing` `money` `shipped` — Found by walking a fresh claim end to end rather than reading the code. `create-checkout` only added the one-off build-fee line item when the request body carried a `prospectId`, and **nothing in the app has ever sent one** — not the pay modal, not the site card, not the editor. So a claimed site's checkout said *Due today US$1,449* and billed **US$499**: the site went live, the customer believed they had paid for the build, and **the partner earned nothing on it**. Silent everywhere, because from Stripe's side nothing failed. The fee now resolves from the **site** (the thing being paid for), guarded on prospect stage so a cancel-and-return pays hosting again and the build fee once. **Verified on the live test-mode checkout: US$999.00 then US$49.00/month, matching the modal exactly.** Same class of bug as the commission accrual — displayed success, silent money failure; both were only findable by checking what actually reached Stripe/the DB. *(Companion fix: the modal read the fee back through a claim token stashed in localStorage, which didn't survive the redirect — new `site_offer(site)` RPC, migration 0029, so the total is right on a refresh, a second device, or a week later.)*

- **🎁 Retail UX pass — Waves 3–5: socials path, go-live moment, walkthrough, partner front door** `ux` `partner` `marketing` `shipped` — Continuing the 17-screen walk. **(1) "No website yet" create path** — the biggest slice of the market (a Google listing + an Instagram + a third-party ordering link, no site) was invisible to us because every entry point assumed a website to crawl. New fourth tile finds them on Google by name+suburb or a pasted Maps link, auto-fills address / phone / hours / category / rating, then gathers socials and "how do customers order right now". **Verified live on prod:** real match for *Nikko Japanese Restaurant*, phone `09 309 8266` pulled from the listing, and the generated hero came out with **`Order online`** + **`Call us — 09 309 8266`** as buttons. New `lookup-business` edge fn (Places v1, same API as the lead engine) that degrades to a manual form when the key is absent. `generate-site` now puts the top one or two links in the **hero** as action-labelled buttons rather than the footer — that applies to every path. **(2) Go-live celebration** — Stripe's `?welcome=1` return was being discarded; it now opens confetti + corner bursts, the address big and shareable, "Go to my site now", and the domain nudge (last easy moment before they've shared the upwind.page link around). Canvas, no deps, reduced-motion aware, and behind **its own error boundary** so a throw degrades to no-confetti rather than an error screen on top of a fresh payment. **(3) "Finish your site"** — deliberately *not* a publish gate (paying early is the goal); a dashboard nudge listing what's still template — default business name, starter wording, placeholder photos, no phone/address, forms still emailing a template address — that clears itself. **(4) Editor walkthrough** — 7 spotlight steps replacing the permanent "click any text" hint, skippable, Esc/arrow keys, replayable from the help icon (now a menu). **(5) Marketing hero** — a looping click-to-edit demo beside the copy: cursor glides in, editing ring appears, words type, a photo drops in, Save → "Your changes are live". Vanilla, pauses off-screen, static under reduced-motion. Hero copy now names the socials-only market. **(6) `/partners` front door** — split screen (pitch + commission ladder left, sign-up right), works signed-out, reuses `become_partner`. Retail is now partner-free except one quiet line on the sign-in card. *(Also: Stripe rebranded — new account, product "Upwind", new $49/$499 prices wired, **14-day trial removed** — checkout no longer says "Lite Websites"/"Coreshift Website". `GOOGLE_PLACES_API_KEY` now set on prod.)*

- **🧭 Retail UX pass — screen-by-screen walk of the self-serve journey (Waves 1–2)** `ux` `dashboard` `editor` `shipped` — Walked all 17 screens of the self-serve retail journey live with Ricky and rebuilt against his notes. **Shipped LIVE:** (1) **🐛 Mobile nav bug** — every published site had *zero* navigation on a phone (`.nav { display:none }` under 860px with nothing replacing it); now a burger + drawer, CSS-only so it can't break on static pages. Fixes every existing site, not just new ones. (2) **Dashboard** — Results / Manage access / Manage billing move to the left sidebar (new `/results` with per-site filter, `/access`, `/billing` routes); **retail is one website** (no "+ New site", no ghost tile, full-width card); green "Visit live site" + a connect-your-domain banner whose copy flexes by plan (included vs **+US$10/mo** if we source it); labelled web address with a slim **＋ Add custom domain** (pencil once connected); **"Edit this site"**. Retail is now **100% partner-free**. (3) **Site card preview** — instead of a screenshot service, the card renders the site's *own* home page through the same components the editor and renderer use: never stale, works on drafts, no cost. Single retail card is side-by-side (preview left, details right). (4) **Editor chrome** — menus regrouped as **Pages** (page *names* + pencil for per-page settings + header menu) / **Design** (theme & colours) / **Settings** (footer, logos & tracking); menus open on hover and close on leave (the stuck-open, overlapping bug is gone); a real **Save changes** that writes a *draft* (the old helper published — genuinely different code path); Versions + help become icons; Preview removed; Ask pulled pending work; responsive fixed at 1180/980 so Publish stops clipping; section hover controls are one solid ink toolbar instead of white-on-white. (5) **New-site flow** — the two nested forks collapse to **one tile grid**; the template step is reframed as *what's on your site* (grey named wireframes from the section catalog, no swatches/search/chips, an "every template includes…" reassurance line, "Skip template"); a new **Your brand** step (logo + primary/secondary colour with plain-English explanations) comes **before** the look step, which then previews in *their* logo and colours with the typeface as a quiet refinement; the free address is **derived, never chosen** (auto `-2` on a clash instead of dead-ending); "Start over" gone; finishes with **Open the editor** *or* **Add my details first** (phone / enquiry email / address / socials → footer, contact-form recipient, social icons). *(Page settings now separate **Page name** from **SEO title** — the menu had been showing titles like "About — Your Company".)*

- **🐛 Partner claim fix — the sale now completes (token survives sign-in)** `partner` `auth` `shipped` — Diagnosed a broken live partner→customer sale (the site never transferred, the partner never saw a payment). Root cause was **front-end only**: the claim page stashed the `?claim=<token>` in localStorage as its *only* way to survive sign-in, but both auth calls (`signInWithGoogle` / `signInWithEmail`) redirected to the **bare origin**. When the customer signed up by magic link and opened the confirmation email on a **different device**, localStorage wasn't there — they landed on the plain dashboard with no claim context, so `claim_prospect` never ran; confused, they built and paid for a *different* site. Fix: redirect to `window.location.href` so the confirmation link returns to `/?claim=<token>` and the claim resumes **cross-device**. Verified the DB side is sound — a rolled-back prod test of `claim_prospect` created the account, moved the site, and set the prospect to `won` cleanly. Once the claim runs, the sold site shows in the partner's dashboard under **Client sites** (Live badge, partner keeps edit access), so "partner sees it's paid" resolves with it. Shipped dev→staging→main. **Needs Ricky to confirm the `upwind.build/**` redirect wildcard (above) for the cross-device path.** *(Two separate go-live gates surfaced by the same test: the "coreshift" the customer saw is the **Stripe receipt/trading name** — gate 2️⃣ below; the junked email is **deliverability** — gate 1️⃣ below. Neither is a code bug.)*
- **🤝 Partner sell flow — the site card IS the workspace** `partner` `dashboard` `shipped` — A partner runs the whole build → share → sell → sold lifecycle from the dashboard site card; **the separate Opportunities/pipeline board is retired for partners** and the Lead Engine (discover/score/cold-outreach) is now **Coreshift-staff-only** (nav + route gated on a team role). Partner-owned unsold cards are reshaped to fill the space: status badge (Draft / Offer sent), a status line ("Offer sent to <email> · US$X build fee — awaiting payment"), and actions **Edit · Share preview · Sell/Resend · Delete**. **Share preview** copies the open `?preview=<subdomain>` link (client views the site, nothing published). **Sell** → build fee + customer email → branded claim email → customer creates their own account + pays (build fee + US$49/mo) → site transfers to them, partner keeps edit access + commission. New RPC `sell_site` (migration 0025) bridges a dashboard site into a prospect for the existing send-claim → claim_prospect → checkout path; owner+partner-only, refuses live/already-claimed sites. **LIVE in production.** *(Also fixed live: `prospect_set_claim` used pgcrypto `gen_random_bytes` off its search_path → send-claim 500'd → no sale email; re-minted the token from `gen_random_uuid` (migration 0026). And the Delete button was clipped by the card edge → row now wraps.)*
- **🗑️ Delete-site action (frees a workspace slot)** `dashboard` `shipped` — Owned, non-live site cards get a **Delete** button → a confirm dialog ("permanently removes the site and its content · frees up a slot · can't be undone") → deletes and reloads so the freed draft slot shows immediately. Backed by a new **`delete_site(p_site)` RPC** (migration 0024, applied to prod): owner-only (`can_manage_account`/`is_app_admin`) with a **money-safety guard** — a site with a live subscription (plan active/past_due or a Stripe sub) is refused ("cancel billing first"), so deletion can't orphan a subscription. Child rows clean up via existing FKs (cascade for content/domains/leads/assets; set-null keeps ledger/prospect/change-request history). **Verified on prod**: a live site is refused with the guard message, counts unchanged; tsc + build clean, 44/44 tests. LIVE on upwind.build.
- **✉️ Transactional emails → Upwind design system** `design` `email` `shipped` — Every email the platform sends now shares one branded shell (`functions/_shared/email.ts`): Upwind palette (#5CA6F2 / #15233A / #F4F6FB), the up·wind wordmark, a rounded brand CTA, and a white-label variant (partner name/logo/accent + a "powered by Upwind" line). Email-safe HTML — tables + inline styles + system fonts. Refactored **8 senders + the unsubscribe page** off their bespoke inline HTML (old `#2b5fb0` blue + "⬡ Upwind"): auth-email (magic-link/signup/recovery), send-invite, create-sale-checkout, notify-support, submit-form (lead notifications) — Upwind-branded; send-invitation, send-claim, monthly-report — white-labelled to the partner. Behaviour unchanged (same recipients, subjects, links, suppression, signature verification, white-label From/ReplyTo). Verified: shared module type-checks + renders (deno run), all files parse clean, no old branding left. **On dev + staging; edge functions reach real inboxes only from `main`.** *(Pairs with the still-open email-deliverability live-send test.)*
- **🎨 Marketing/legal pages reskin → Upwind system** `design` `shipped` — Fixed the terms/legal pages Ricky flagged as still carrying old branding. All six non-home pages (terms, privacy, refunds, platform-agreement, partner-agreement, partners) share **`legal.css`**, which still had the pre-Upwind warm palette — a pink/maroon accent `#b3456b`, cream `#fffdfa` background, Georgia serif + system sans. Updated that one file to the Upwind tokens (`#5CA6F2` accent, `#15233A` ink, `#F4F6FB` soft, white bg) and pulled **Hanken Grotesk + Source Serif 4** in via `@import`, so every linked page reskins at once; also fixed an "a Upwind" → "an Upwind" grammar slip. Verified on-brand in preview (wordmark, blue nav, serif headings, commission table on the new tokens). Legal-entity wording ("CoreShift Limited trading as Upwind") intentionally unchanged. **LIVE in production — promoted staging → main 2026-07-21; verified `tryupwind.com/legal.css` serves the Upwind palette (#5CA6F2 + Hanken/Source Serif, no old pink).**
- **🛠️ Editor improvements — Pay+Publish · live settings · add-section pills · menu hints** `design` `editor` `shipped` — Four editor upgrades (LIVE in production): **(1) Pay + Publish** — an unsubscribed site's Publish button becomes "Pay + Publish" and opens a go-live/subscribe dialog (annual default / monthly) that saves the current edits then routes to Stripe checkout; subscribed sites keep the review-and-publish gate. **(2) Real-time settings** — fixed the bug where panel edits (footer social links, nav, etc.) didn't show until publish; the live preview now applies all *structural* draft edits (collections, images, icons, section options, theme, pages) while inline text/link edits stay in place — verified a footer social icon renders live the moment it's added. **(3) Add-section pills** — redesigned to a dashed blue pill on a hairline rule, shown above *and* below the hovered section; clicking inserts a section at that exact spot. **(4) Menu hints** — every Design ▾ item now carries a one-line description. Verified in the editor preview; tsc + build clean, 44/44 tests.
- **🎨 Design overhaul → Upwind design system (LIVE on staging)** `design` `shipped` — Implemented the Claude Design "Upwind design system overhaul" spec across the app chrome, editor and marketing (client-site themes in `coreshift-starter` untouched). **D0 foundation:** full token scale, **Hanken Grotesk + Source Serif 4**, the wind signature (SVG mark + ambient stroke-draw swooshes), refined buttons/focus/elevation, a zero-dependency motion layer (screen entrance, count-up, swoosh-draw — all reduced-motion guarded; GSAP intentionally skipped to avoid a bundle/lockfile dep). **D1 app shell:** a 232px left sidebar (role-gated nav + user block pinned bottom, white-label partner brand in the rail) replaces the old top bar, wrapping Dashboard / Lead Engine / Admin / Earnings; full-screen Editor/Results keep their own chrome. **D2:** serif greeting, theme-tinted site-card thumbnails + status badges, dashed ghost "create" card, gradient plan banner, reskinned sign-in (card on gradient + swooshes). **D3:** count-up stat cards, serif page titles, pipeline lanes as soft wells + spec score-chip colours. **D4:** editor overlay palette/type/rings retuned to Upwind (every `.csle-*`/`data-cs-edit` hook preserved); marketing hero gains the real typefaces + Source Serif 4 display + ambient swooshes. Presentation-only — no data/auth/route changes. Verified: tsc + `vite build` clean, **44/44 tests pass**, sign-in + marketing hero confirmed in preview; **LIVE in production** (tryupwind.com / upwind.build / *.upwind.page all HTTP 200, production smoke green). **D5 (editor top bar) also done:** the live-edit overlay moved from the floating bottom toolbar to the spec's dark-ink 54px top bar — ← Sites · site name · subdomain / Pages ▾ · Design ▾ · Versions · ✦ Ask / edit-hint · Preview · brand-tint Publish (Design ▾ folds Theme/Menu/Footer/SEO/Settings/Help); verified in the `?template=` editor preview, menus open downward, all `.csle-*` hooks preserved.
- **🪁 Rebrand → Upwind (LIVE in production)** `brand` `launch` `shipped` — The product is now **Upwind** (Coreshift Limited stays the legal entity / merchant of record). Full cutover, live: marketing at **tryupwind.com**, app at **upwind.build**, published sites at **\*.upwind.page**, custom-domain SaaS fallback `sites.upwind.page`, email `hello@tryupwind.com` (Postmark-verified + DMARC). New per-zone Cloudflare routing on the CoreshiftHq account; 7 proxied DNS records + SaaS fallback set via the CF API; old coreshift.page routes retired (523). Brand palette **#5CA6F2 / #F4F6FB**, lowercase "upwind" wordmark + favicon (stopgap SVG until the exact logo/favicon/og.png assets land). ~150 edits across app/marketing/edge-functions/renderer + 4 wrangler configs. Verified: all 3 surfaces HTTP 200, production smoke green. Internal ids (packages, RPCs, worker names) intentionally unchanged.
- **Legal — partner-model compliance (LIVE)** `legal` `shipped` — Lawyer's 5 clause additions applied (Platform Agreement 1.7 "who you contract with" + 1.8 "partner-built sites"; Terms; Privacy "leads"; Refunds "build fee"), and the **Partner Agreement published at /partner-agreement** (CoreShift Limited trading as Upwind, commission-ladder table, footer-linked). Partner signup now requires accepting it. *(Two clauses — principal/agent characterisation + buyer-created/self-billing invoicing — still need accountant/lawyer sign-off before onboarding a real partner.)*
- **New-site flow (redesign)** `shipped` — The onboarding Ricky spec'd: start (bring-existing / start-new) → pick a template → live preview with theme-hover reskin → brand colour + curated font pairing → finalize. **Bring-existing crawls the site and pours the real content into the chosen template's layout** (generate-site follows the template skeleton, never inventing testimonials/stats/logos). Start-from-scratch drops into a blank editor. 14 templates, 17 themes, 6 font pairings all wired in. *(Pending a live smoke-test of the authed crawl→pour path.)*
- **In-page editor** `shipped` — Click-to-edit text and images, rich text with links, nav/footer editors, add-pages, SEO + settings panels, theme + brand-colour switching, curated font pairings, in-memory draft, and a sign-off gate. In real use across the live sites.
- **Section + theme engine** `shipped` — 22 section types and 17 theme styles rendered through one shared code path used by both the editor and the edge renderer, so swapping themes never loses content.
- **Design Engine + Tailwind authoring layer** `shipped` — Sections are now authored in Tailwind v4 utilities bridged to runtime theme tokens (no preflight, so existing themes don't drift), compiled by the editor's Vite plugin and the renderer's CLI from one source. Variety is cheap and theme-swap still keeps content. Canon: the Simplicity Budget (customers pick pictures not parameters; "Ask" is the advanced mode; UI surface frozen).
- **Testimonials: 6-layout calibration** `shipped` — First engine-built section: spotlight, cards, mosaic, portrait, stacked and marquee on one content contract with zero toggles. Reviewable on the board (`?gallery=section&type=testimonials`, `?variant=<id>` to deep-link one).
- **Expressive motion** `shipped` — Micro-motion as a contract: sections expose hooks (scroll reveal, staggered card cascades, hover), each of the 11 themes carries a tuned motion personality (calm / gentle / snappy / precise / hard), and functional micro-interactions (button press, focus rings) are always on. Fast (CSS + a tiny observer), and fully off under reduced-motion. Preview with `?motion=on` on the review board.
- **Instant publish + edge renderer** `shipped` — Publishing is a DB pointer flip + edge-cache purge; one Cloudflare Worker resolves host → tenant → published content → SSR. **15 sites live and published.**
- **Contact forms → leads** `shipped` — Published forms post to a public function that resolves the recipient server-side, stores the lead, and shows it in the dashboard. Proven by real leads.
- **Telemetry** `shipped` — Page-view counting, dashboard error capture, and an editor "ask for a change" feed — all live with real data.
- **Auth + accounts** `shipped` — Google sign-in with automatic personal-account + owner-membership provisioning.
- **Trial + billing gate** `shipped` — 14-day trial from first publish; cancelled/expired sites serve a calm offline page from the renderer.
- **Per-site billing (partner keystone)** `partner` `shipped` — Billing moved from account-level to **per-site**: each published site carries its own plan + Stripe subscription; the renderer gates per site; the webhook resolves the one site by subscription metadata/id (a customer now holds many subs) with legacy fallbacks for grandfathered subs. Migration 0009 backfilled prod (24/24 served-state preserved, existing multi-site free-riders grandfathered). Also fixes the retail multi-site revenue leak. **Verified on the live webhook with signed events — checkout→active, cancel→offline+domain teardown, and per-site isolation (a failed invoice on one site leaves the other active).**
- **Global USD pricing** `partner` `shipped` — US$49/mo · US$499/yr for everyone (Stripe adaptive pricing shows local currency); existing NZ$ subscribers grandfathered. Platform Agreement + UI re-stamped.
- **Partner program — rails + white-label claim flow** `partner` `shipped` — The reseller loop, live end-to-end: register as a partner (`become_partner`, now open self-serve via the signup modal), run the lead engine against your **own** agency (de-anchored — Coreshift HQ seeded as partner #0 so the existing CRM is unchanged), set a one-off build fee, and email the customer a **white-label claim link** (partner's brand, via our Postmark). The customer signs up (T&C), claims the site (moves to their account, partner keeps edit access + referrer attribution), and checks out (build fee + subscription, **no trial**). Commissioned-reseller model: everything is Coreshift revenue; commissions accrue to the partner (P3). Migrations 0010–0011. *(Deferred within P2: full customer-dashboard white-label chrome, referral links, Partner Agreement page — awaiting lawyer.)*
- **Partner ledger + payouts-first dashboard** `partner` `shipped` — Commission ledger (`partner_ledger`, migrations 0012–0013) accrues in **NZD at accrual** (IRD-confirmed accounting): on each paid invoice the webhook reads the Stripe balance transaction for the real net + fee, splits recurring hosting vs one-off build fee, and books a pending commission at the partner's tier % (10%→20% ladder, 1→60+ active subs), maturing +14 days. Refunds/disputes book a clawback. Partner dashboard (`PartnerEarnings.tsx`) leads with the money — next payout, pending, paid, lifetime, tier progress, projected recurring — plus a payout-method form (NZ bank / Wise / PayPal). GST + self-billing captured at partner signup. **Verified on prod: hosting + build-fee accrual idempotent, ledger math correct at 10%.** *(Payout execution — Wise/PayPal batch export + mark-paid — and fraud flags are built as schema/dry-run, gated on Wise/PayPal business accounts + accountant sign-off.)*
- **Retention email (proof-of-value)** `partner` `retention` `shipped` — Monthly per-site "your site had X visits, captured Y enquiries" email (white-label per partner), the year-2-cliff defence for retail and partner renewals. `monthly-report` edge fn is cron-secret-guarded, deployed public, and **scheduled live via pg_cron (1st of each month)**. Verified on prod (dry-run: 11 eligible sites, correct subject lines, wrong-secret → 403). Partner marketing landing page + interactive earnings calculator live at **/partners** (footer-linked).
- **Lead enrichment + richer generation inputs** `partner` `shipped` — `discover-leads` now pulls Google Places details (phone, opening hours, rating, address, category, maps URL) and stores them on the prospect (`prospects.enrichment`, migration 0016); `crawl-site` extracts social profiles + contact (tel/mailto); `generate-site` folds enrichment, socials and extra links (menu / online-ordering / booking) into the build so new sites carry real hours/phone/address, footer socials and featured links. Retail onboarding gained an "extra links" field; the lead pipeline shows Places facts + a per-lead extra-links editor.
- **Admin business metrics** `admin` `shipped` — New **Business** tab in the admin dashboard: MRR + churn by channel (retail / partner / grandfathered), active-site mix (trial/pending/past-due), partner count + activation + avg time-to-first-sale, and commission liability/paid (NZD). Backed by the app-admin-gated `admin_business_metrics()` RPC (migrations 0017/0018). Live prod snapshot: 22 active sites (21 grandfathered legacy free-riders + 1 paying retail), 1 partner (HQ), MRR est US$49.
- **Partner payout execution (P6)** `partner` `shipped` `needs-ricky` — The money-out step, live: `run-payout-batch` nets each partner's matured, unpaid commission and pays it via a **swappable provider rail**. **Wise** is fully built (recipient → quote → transfer → fund from balance) and **defaults to the Wise sandbox** so a misconfig can't move real money; PayPal / NZ-bank are "manual" (admin sends, then marks paid in one click). Admin **Payouts tab**: dry-run preview (who's payable + who's skipped and why), one-click run (with confirm), manual mark-paid, batch history. Idempotent, server-recomputed net, min ~NZ$85 (≈US$50). Migration 0019. **Gated on Ricky:** provide the Wise API token + profile id (sandbox to rehearse, then live) → I set the secrets and we run a sandbox payout end-to-end.
- **Partner UX completion (P5)** `partner` `shipped` — Three pieces closing the partner loop (migration 0018): **(1) Accurate MRR** — `sites.billing_interval` set by the webhook from the Stripe price; MRR now monthly-equivalent (annual at US$499/12). **(2) Referral links** — every partner gets a copyable `?ref=CODE` link; a signup through it stamps `accounts.referrer_partner_account_id` (via `ensure_personal_account(p_ref_code)`), and once that self-serve site becomes a paying sub the webhook inherits `partner_account_id` so it accrues commission at the partner's tier. **(3) White-label dashboard chrome** — partner-sold/referred customers see the partner's logo/name ("powered by Coreshift") in the dashboard top bar, a "your website partner" contact card, and a "contact your partner first" note in support; `my_website_partner()` resolves branding (HQ excluded).
- **Workspace / quota model — "draft" replaces the trial (P7)** `billing` `shipped` — Packaging shift (Ricky, 2026-07-21): every account gets free, private **draft** sites (retail **1**, partner **10**; staff unlimited) — fully editable + previewable, never publicly served. **Publishing = subscribing** (plan→active), which serves the site publicly on its `*.coreshift.page` subdomain, includes a custom domain at **no extra charge**, and **frees the draft slot** (live sites don't count against quota). A cancelled site falls back into the workspace (counts again, editable, re-subscribe to relive — so churn metrics still read off `cancelled`). This **replaced the 14-day trial entirely**, which also dissolved the trial-reset gaming bug + the missing-trial-warning gap. Migration 0021 (applied prod, verified: 22 active still served, trial sites → draft/offline); dashboard shows Draft/Live badges, "X of Y draft sites used", and gates "+ New site" at quota. `site_is_served` = active/past_due only; `add_site` enforces the quota.
- **Real app routes (P8)** `infra` `shipped` — The dashboard was a state-driven SPA with one URL; now it has real routes (react-router-dom): `/`, `/admin` (+`/admin/payouts`, `/business`, etc.), `/partner`, `/earnings`, `/editor/:id`, `/results/:id` — deep-linkable, browser-back works, role-guarded, SPA fallback configured. Every pre-auth entry point (`?claim`/`?ref`/`?preview`/`?template`/`?gallery`/`?start`) and the auth roundtrip preserved. 44 tests pass.
- **Full security + money audit → P0 hardening** `security` `launch` `shipped` — Exhaustive audit (4 parallel review agents across security / money-paths / engineering-ops / product-funnel, plus Supabase advisors). Fixed the six P0 defects, all verified: **(1)** clawbacks were structurally inert (`matures_at` never set → refunds never reduced a payout); now set + partial refunds prorate + accrual failures log. **(2)** payout double-pay race → rewritten as two-phase **reserve→pay→finalize/void** RPCs with `FOR UPDATE SKIP LOCKED`; amount reserved==sent==booked, idempotency key = batch id (migration 0022, verified with a zero-residue rollback test on prod). **(3)** commission currency corruption (USD booked as NZD when the balance transaction lagged) → now accrues only the settled NZD, defers+logs otherwise; Earnings dashboard relabelled **NZ$**, monthly cadence. **(4)** `sites` was fully anon-readable (every tenant's plan / Stripe sub id / partner attribution leaked) → anon SELECT restricted to non-financial columns (migration 0023, verified via `SET ROLE anon`). **(5)** partners could raise their own `custom_pct` / un-suspend themselves → `partner_profiles` UPDATE restricted to branding columns only. **(6)** `unsubscribe` had deployed JWT-closed (cold-outreach links 401'd) → pinned public (verified live 200); + self-referral guard + double-subscription guard. Migrations 0022/0023 + 0020 (prod-only RPC bodies reconstructed for DR). Note: the two security holes (#4/#5) were DB migrations applied to prod immediately; the money-logic (#1/#2/#3) shipped with this deploy.
- **Billing hardened + self-serve portal** `launch` `shipped` — Stripe webhook now has a replay guard, idempotent event dedup, persists the subscription id, and handles payment-failed/succeeded. Customers get a "Manage billing" button (Stripe portal) to update card / cancel themselves. Real end-to-end sale still to be run in the rehearsal.
- **Legal & trust pages** `launch` `shipped` — Terms, Privacy (NZ Privacy Act + real sub-processors), and a plain-English Refunds policy, served at /terms, /privacy, /refunds and linked from the footer. Pending a final human/legal review.
- **Partner Program — Track 0: global reprice to US$49/mo · US$499/yr** `launch` `shipped` — One global USD price for everyone (was NZ$100/999), shown in local currency via Stripe Adaptive Pricing. New Stripe prices live in test mode, env secrets swapped (prod + staging), dashboard/marketing/legal copy updated, terms version bumped (new signups accept the USD agreement). Existing NZ$ subscribers grandfathered untouched. First step of the approved Partner Program plan (reseller model, white-label, commission ladder, payouts-first partner dashboard — P1–P4 to follow).
- **Platform Agreement + signup acceptance** `launch` `shipped` — The lawyer's Live Edit Platform & Hosting Agreement published as a click-through page (/platform-agreement), reconciled to the real product: electronic acceptance (no party details/signatures), cancel-anytime (not a 12-month lock-in), no setup fee, corrected sub-processor list. Sign-in now has a required "I agree to the Platform Agreement + Privacy Policy" checkbox (hyperlinks, gates Google + magic-link); acceptance is recorded server-side with a version stamp (migration 0008, prod+staging). Sub-processors corrected across the agreement **and** the live privacy page (Anthropic not Open Router; +Railway +Postmark; −Higgs Field). **Open points still need Ricky↔lawyer sign-off** (see the go-live gates).
- **Custom-domain lifecycle** `launch` `shipped` — Full flow now: connect (custom hostname) → auto-poll status (pending→live once Cloudflare issues SSL) → per-domain remove → teardown-on-cancel. **Verified live in the rehearsal — `theboys.co.nz` went active.**
- **Dress rehearsal — core paths verified** `launch` `shipped` — 2026-07-16 test run: onboarding (crawl+generate) → publish → lead captured → custom domain live → **test checkout → webhook → account active** (Stripe customer + subscription id persisted, event recorded / idempotent). Billing + custom domains proven in test mode. Remaining before opening: DMARC record + flip Stripe to live keys.
- **DB baseline in source control** `launch` `infra` `shipped` — Captured the 20 prod tables + constraints + RLS + all policies + triggers into a re-runnable baseline migration; functions pointered to prod/0005 with a `supabase db dump` note for byte-exactness. Closes the "schema only in prod" gap.
- **Marketing site + signup funnel** `shipped` — coreshift.page carries a pasted URL through sign-in into the "bring my site" onboarding.
- **CI auto-deploy** `infra` `shipped` — Push to main deploys the dashboard, renderer, marketing, and edge functions to Cloudflare/Supabase, gated on typecheck + tests + a prod smoke test.
- **P0: content-write RPCs locked down** `security` `shipped` — `publish_site`/`save_draft`/`rollback_site`/`provision_site` now enforce owner checks and are no longer callable by `anon`. Hotfixed in prod + codified in PR #7.
- **Stored-XSS fix: URL guards + tokenizer sanitizer** `security` `shipped` — Link/image fields go through a scheme allow-list (safeUrl/safeSrc) and rich text through a parser-style sanitizer; blocks javascript: hrefs and unterminated-tag bypasses, with tests. PR #8.
- **SSRF hardening: DNS-rebinding guard** `security` `shipped` — Crawler/scorer/generator resolve the host over DNS-over-HTTPS and reject private/reserved IPs (not just the hostname string); generate-site asset fetch is guarded too. PR #9.

## 🟡 In Progress

- **Domain purchase + auto-connect (P9)** `billing` `revenue` — **Registrar decision made: Openprovider, NOT Dynadot.** Dynadot was rejected on 2026-07-30 — it cannot sell `.co.nz`, and NZ is the home market. Openprovider covers `.co.nz`, `.nz`, `.com`, `.co`, `.io`, `.co.uk`; `.com.au` is deferred (auDA requires an Australian presence). **Price is US$5/month, not US$10** — Ricky: *"$10/mth is a bit exhaustive."* **Shipped:** the two-option modal (own one / get one for me), live availability search through `_shared/domainProvider.ts`, one price from `_shared/domainPricing.ts`, and `domain-search` behind `verify_jwt = true`. **Not built, deliberately:** registration and the recurring charge. Blocked on the Openprovider balance (US$0.00) plus a registrant-details form; policy agreed — domains are registered in the **customer's** name. Optional IP allowlisting is available on the Openprovider account if we want it.
- **Money-path tests** `infra` `money` — Replay recorded Stripe events against the webhook. Would have caught the uncharged build fee, the currency mismatch AND the unstamped prospect. Turns "we fixed nine bugs" into "those nine cannot come back". Biggest remaining engineering item, about a day.
- **Email deliverability** `launch` `infra` — Postmark is wired for leads, auth, support and invites, but no inbox placement has been confirmed since the token swap. **Unblocked 2026-07-30:** custom SMTP configured and the hourly limit raised on prod and staging, and magic-link delivery is now proved by real sign-ins. What remains is only inbox PLACEMENT — does it land in the inbox or the spam folder — on iCloud and Gmail, unchecked since the token swap.
- ~~**Magic-link login**~~ ✅ **proved in production 2026-07-30** — `auth.users` shows 9 sign-ins in 24 hours, four of the five most recent with `provider: email` (magic link) and the rest Google. This card sat open for weeks waiting for something that had already happened.
- **Team invites / agency linking** `auth` — Partly wired; the invite-accept surface still needs connecting and one real run-through. **Staff sharing is done separately and is live:** any `@coreshifthq.com` address auto-joins the one Coreshift HQ account and gets CRM role `admin` with no invite at all.

## 🚫 Blocked — remaining go-live gates (need Ricky)

- **🟠 Stripe product description still reads “Description goes here”** `launch` `needs-ricky` — Visible to every paying customer. Expanding **View details** on the checkout page shows the recurring line as *Upwind — “Description goes here” — Billed monthly*, next to a build line that now reads properly (“Website build by Fernoak Digital (one-off)”). It's the placeholder left on the product when the new Upwind Stripe account was set up, and it's a Dashboard field, not code — **Stripe → Product catalogue → Upwind → Description**. Needs doing in **both** test and live mode (they're separate objects). Suggested: *“Your website, hosted, live and editable — unlimited changes.”*

- ~~**🔴 Cross-tenant site listing — any signed-in user reads every site row**~~ ✅ **stale — verified fixed 2026-07-28.** The blanket `public reads sites USING (true)` policy is now scoped to **`{anon}` only**; `authenticated` goes through `manage sites` with `USING (can_manage_site(id))`, so a signed-in user sees only their own rows. Column grants back it up: `anon` can select `id, account_id, name, subdomain, theme, published_version_id, created_at` and **nothing financial** — no `plan`, no `stripe_subscription_id`, no `partner_account_id`. That is the deliberate shape, because signed-out preview links need the by-subdomain lookup. *(The original sighting — a new partner seeing another customer's site — turned out to be Ricky on the same session, and the policy work landed with the audit batch. Checked directly against `pg_policies` + `information_schema.column_privileges`, not inferred.)*

- ~~**Prod auth Site URL → app.coreshift.page**~~ ✅ resolved — Site URL fixed to `https://upwind.build`; Google sign-in verified (302 → Google, `redirect_to=upwind.build` preserved). **Carry-forward for Ricky to confirm:** the Redirect URLs allow-list must include the wildcard **`https://upwind.build/**`** (not just the bare host). The partner-claim fix (below) now returns users to `/?claim=<token>` after sign-in; if the allow-list lacks the wildcard, Supabase silently falls back to the bare Site URL and the cross-device claim path degrades to localStorage-only (same-browser). One line to add.

- **🟡 1️⃣ Email deliverability — auth records are all in place; needs an inbox-placement re-test** `launch` `infra` — **Correction (2026-07-27): the previous entry on this card was wrong.** It claimed `tryupwind.com` had no SPF and no Postmark DKIM / Return-Path CNAMEs. That came from a DNS check run against *guessed* hostnames. The real records are live and Postmark shows both **Verified**: DKIM at `20260721045428pm._domainkey.tryupwind.com`, Return-Path `return.tryupwind.com → pm.mtasv.net`, DMARC `p=none` with `rua`. DMARC therefore passes on **both** axes — DKIM signs `d=tryupwind.com` (aligned), and the envelope sender `return.tryupwind.com` inherits Postmark's SPF and is relaxed-aligned to the org domain. **What was actually broken:** `POSTMARK_TOKEN` still pointed at the *old* Postmark server, so every send bypassed the new "Upwind" server (which read "never used"). Token repointed 2026-07-27 and confirmed — `auth-email` returned 200 on a live OTP send against the new token. **Remaining:** one real inbox-placement test to iCloud (`@me.com`) + Gmail from the new server, since the original junk-folder report predates both the verified records and the token switch. **Deliberately NOT done:** adding an apex SPF TXT to `tryupwind.com`. Postmark does not need one (the Return-Path covers alignment), and the apex carries a `google-site-verification`, so a naive `v=spf1 include:spf.mtasv.net ~all` would start failing SPF for anything sending as `@tryupwind.com` via Google Workspace. If an apex SPF is wanted, first enumerate every sender for the domain and build one record covering all of them.
- ~~**2️⃣ Flip Stripe to live keys + trading name**~~ ✅ done — rebranded onto the new Stripe account: new secret/publishable keys, new webhook signing secret, statement descriptor set to **Upwind**, and a single "Upwind" product with two prices (**$49/mo `price_1Txek8HtrmVIubMC9mhLAwZ8`**, **$499/yr `price_1TxemUHtrmVIubMCMqiPL5N8`**) replacing the old account's "Lite Websites / Try Coreshift Website" naming that was leaking into checkout. `STRIPE_PRICE_ID`/`_ANNUAL` repointed at the new prices — they still held the old account's IDs after the key swap, which would have failed every checkout. **Trial removed** (the free build is the trial). Full purchase walkthrough still to be run.

- ~~**3️⃣ Supabase Auth custom domain `auth.upwind.build`**~~ ✅ done — customers no longer see `nxrmpeilqxkfshveezfr.supabase.co` when they sign in. Add-on enabled; `CNAME auth → nxrmpeilqxkfshveezfr.supabase.co` (DNS-only) + ownership TXT via the CF API; certificate issued; `domains activate` → `5_services_reconfigured`; both hostnames now advertise `https://auth.upwind.build/auth/v1/callback`; browser client repointed and shipped (`c6e7473`). Verified against the deployed bundle: 1× `auth.upwind.build`, 0× project ref. Google Auth Platform side: app name **Upwind**, published **In production**, External, non-sensitive scopes only (`openid`, `userinfo.email`, `userinfo.profile`) — so no Google verification review, and the 100-user cap does not apply (it only bites unapproved *sensitive* scopes). *Two gotchas worth keeping:* (a) the `_acme-challenge.auth.upwind.build` TXT Supabase hands you is **never served** by Cloudflare — CF intercepts that exact name once the hostname is an active Cloudflare-for-SaaS custom hostname and settles DCV internally; a control TXT under the same node resolves in 15s, so the record is not the problem, just wait and re-verify. (b) Activation changes the OAuth callback, so the new redirect URI must be registered on the Google client **first** — verifiable without touching anything by requesting Google's authorize endpoint with the new `redirect_uri` and checking for `redirect_uri_mismatch`. Staging is unaffected (its workflow passes `STAGING_SUPABASE_URL`); the project URL still works as a fallback.
- **3️⃣ Lawyer sign-off on the Platform Agreement** `launch` `legal` `needs-ricky` — The click-through agreement is live + accepted at signup, but reconciled by us from the draft. Send the lawyer the corrections: (a) 5 open points — 4 confirmed from the product (offline-on-cancel, customer domain, customer content, V2 out of scope), setup fee = none; (b) we reconciled the 12-month term → cancel-anytime to match the product/marketing; (c) corrected sub-processor list (Anthropic, +Railway, +Postmark, −Higgs Field); (d) clause 13.2 promises a self-serve content export we haven't built yet — build it or lean on the assisted-export clause.
- ~~Cloudflare for SaaS~~ ✅ done — verified live (`theboys.co.nz` active).

- **🔴 Prod's `*_TEST` Stripe secrets belong to a different Stripe account** `money` `security` `needs-ricky` — Fragment `HXnhNJQhs8`, not Upwind's `HtrmVIubMC`. Dormant while `STRIPE_MODE=live`, live ammunition if it ever flips — and it has already caused one misrouted production payment. Replace or delete them.
- **🟠 Custom SMTP is off on production** `launch` `needs-ricky` — Auth email is rate-limited to a handful per hour by Supabase's built-in mailer, and the magic link is the entire front door, so the throttle is total and silent. The Send Email Hook delivers via Postmark but does not change that counter — it is a GoTrue setting, not a sender setting. Turn on custom SMTP with the Postmark credentials, then raise the limit explicitly. 15 minutes.
- **🟡 Three surfaces that have never been seen working** `launch` `needs-ricky` — 15 minutes total: reply to a support ticket as admin and confirm the email arrives in the customer's inbox (the reply leg shipped and has never been *seen* to land); open `/help` and read two of the 8 articles on screen; try to archive a live paying site and confirm it refuses with *"This site is live on a subscription."* (proved in SQL, never through the UI).
- ~~**Minimum partner age 18**~~ ✅ **REJECTED by Ricky 2026-07-30 — "we don't need one."** No work: there is no age field, no gate and no mention of 18 anywhere in the signup, the app or the partner agreement brief, so nothing had to be removed. Partner recruitment is unblocked.
- **🟡 One Stripe key set per environment — approve or reject** `infra` `needs-ricky` — `SCOPE-one-key-set-per-environment.md` in the repo. Removes `STRIPE_MODE` and the `*_TEST` twin sets so any future developer can see exactly which keys an environment uses. Needs **two verification purchases**, so not on a demo day.

## 🔵 This Week — launch wave

- **🔴 `discover-leads` collects prospect PII with no IPP 2 / IPP 3 basis** `privacy` `PIA` `IPP-2` `IPP-3` `places.googleapis.com` — Found on 31 Jul while answering legal's PIA questions — nobody had asked about this one. `discover-leads` and `lookup-business` call `places.googleapis.com` to collect business contact details for prospects who have not consented and have no existing relationship with us. Under IPP 2 that is collection from a source other than the individual concerned, and under IPP 3 there is no notification. For the sole traders and owner-operators Upwind actually sells to, a business email and phone number **is** personal information. Google Places is also not on the sub-processor list at all. Of everything the PIA turned up, this is the one with real exposure. Needs a decision from the Privacy Officer on lawful basis + notification before the lead-finder is used at any volume, and Google Places added to Section 4.
- **Wise belongs on the sub-processor list now, not at first payout** `privacy` `PIA` `M5` `partner_payout_details` `WISE_API_TOKEN` — Legal's PIA asked us to "confirm the payout rails remain un-integrated". They are not — `run-payout-batch` implements the full Wise rail (recipient → quote → transfer → fund) against `api.wise.com` and ships today. It has simply never run: `partner_payout_details` and `payout_batches` are both 0 rows in prod (verified 31 Jul), and the base URL defaults to the Wise sandbox. The trap: the trigger for Wise becoming a live sub-processor is the **first row in `partner_payout_details`**, which a config change alone (`WISE_API_TOKEN` + `WISE_PROFILE`) can produce — no code release, so no release review will catch it. List Wise now marked not-yet-active, with its DPA and processing locations confirmed before the first payout rather than after.

- **Partner journey walk + W1–W4 rebuild** `partner` `ux` `shipped` — Walked journey #2 end to end as a real self-serve partner on production (`Fernoak Digital`, ref `5f67a99d`) and rebuilt what it exposed. **Root cause of nearly every finding: the partner dashboard was the retail dashboard.** Retail is someone looking at their own website; a partner is a third party building for someone else, so the first-person copy was pointed at the wrong person — most visibly a banner telling new partners to *"publish it for US$49/mo"*, money the partner never pays. Shipped `32488a8` → `bdfd94f`: partner front door rebuilt around the offer (headline "You sell it. We build it. You get paid.", percentage ladder replaced with a live earnings calculator ported from the marketing page so there's one ladder not two, consent moved above both auth buttons, per-route page title); partner onboarding taken full-width with a 266-country type-to-filter picker (country now persisted — migration 0027 — since `tax_residency` drives GST but payout rails differ by country); nav re-voiced (Sites→**Clients**, Manage access→**Your team**, Manage billing hidden for partners who are never billed by us); new **Your brand** page giving partners the first-ever surface for the white-label fields their customers actually meet, with a live preview of the client dashboard + invitation email; create-site flow forked out of first person. **Earnings truth pass:** "1 more active site → 10%" at zero sites (one more site to reach the rate already shown), the payout minimum stated as both "US$50" and "NZ$85", a payout form that asked to be filled in while saying to fill it in later, and an empty state directing partners to the staff-only Lead Engine pipeline — which bounces. **Also fixed:** auth emails still carried the raw project ref because the link was built from the reserved `SUPABASE_URL` (`AUTH_PUBLIC_URL` added), and `/billing` showed a raw *"Edge Function returned a non-2xx status code"* to anyone without a Stripe customer. *Checked and found already safe:* partner writes to `partner_profiles` are restricted by column-level grants, so `custom_pct`, `status` and `ref_code` are not partner-writable.

- **🚧 Journey #3 · Screen 1 — claim invitation email (design signed off, NOT shippable yet)** `partner` `ux` `trust` `needs-ricky` — Redrafted from a short "claim it" note into the journey's biggest trust document: shows the whole path before the customer takes step one. Four numbered steps (create your login → claim & subscribe → put your own web address on it → change anything, whenever, as much as you want), cost table folded into step 2 where the money actually moves and split by who receives it (build fee → partner, hosting → Upwind), CTA repeated inside step 1 next to the instruction it belongs to, and the reassurance block promoted to a full band — **"Nothing here is a trap"** — with four ticked cards: it's yours, nothing you already have gets touched, you can stop any month, you're not on your own with it. Draft: `scratchpad/claim-email-preview.html`.
  - **⛔ SHIP GATES — do not send this to a real customer until all three are closed:**
  - **(a) Screenshots — RICKY IS SUPPLYING THESE. Remind him before shipping.** Three static slots: *create your login*, *payment page*, *click-to-edit*. Shoot them after the claim page and checkout are redesigned, or they'll be pictures of screens we already changed.
  - **(b) Hero image needs building, not shooting.** It's a picture of *that customer's* home page, so it must be generated per send. No screenshot pipeline exists — `SiteThumb` dodges this by live-rendering React in the browser, which email can't do. Recommended: Cloudflare **Browser Rendering** (the wrangler token already carries `browser (write)`, currently unused) → screenshot `<sub>.upwind.page` → R2 → link from `send-claim`.
  - **(c) Sell dialog must capture two new fields.** The email says *"Hi Marama — great to meet you today"*, but the dialog never asks for a contact name or a personal line, so every send would read "Hi there" hours after a face-to-face conversation. `prospects.contact_name` and `prospects.invite_message` already exist and are simply never populated.

- ~~**🚧 Journey #3 · Screen 2 — claim page**~~ ✅ **built (wave 1)** — original notes kept below `partner` `ux` `trust` — Was two stacked half-cards with a dead gap: no website, no price, and Upwind's logo out-branding the partner the customer had actually met. Redesigned as a **landing-page version of the invitation email**, in the partner's brand on our domain — Ricky's call, and it's the right one for a white-label promise. Draft: `scratchpad/claim-page-mockup.html`. Decisions: (1) hero embeds the **real site live and scrollable** in a browser frame — the one thing the page can do that the email can't — with "open it full screen"; (2) the partner appears as a person, avatar plus the line they wrote in the Sell dialog; (3) **step 1 is "Create your Upwind account", not "sign in"** — these are new users, and the card explains *why* an account exists: "it puts the website in your name, not the partner's and not ours"; (4) the email's four-step rail carries onto the page showing them at step 1, so the journey doesn't restart; (5) price sits beside the action, split by who receives it, with "nothing is taken today"; (6) "Nothing here is a trap" repeats from the email on purpose — anxious people re-read.
  - **Build notes:** the live embed needs the **preview gate open for claim links** (sites sit behind `previewGateStatus` + optional password today, so the hero would render a lock screen). Google's own screen will say "Sign in to Upwind" after a button that said "create an account" — not fixable by us, just a known jar.
  - ~~**🔴 Consent integrity**~~ ✅ **done separately — `20260731120000`, and the real defect was worse than this note.** The cross-contamination described here was already mitigated (`rememberConsent={false}` on both claim-page sign-in forms). What was actually broken: **(a)** the Partner Agreement had never been recorded as accepted by anyone — all 14 prod rows in `terms_acceptances` are `platform_agreement`, because the only writer hardcoded that document, so partners ticked a box naming the Partner Terms and nothing recorded it; **(b)** the rows that existed were not evidence — `recordTermsAcceptance()` ran under `if (session)` on every sign-in and every reload, so the 13 rows at `2026-07-17` are 13 sign-ins, and bumping `TERMS_VERSION` would have filed a fresh "accepted" row for every existing user for a document none had seen. Consent is now noted on the act (submit, not tick), carried across the magic-link roundtrip, written once a session exists, and silent when nothing is pending. `kind` is derived server-side from the document; unknown documents refused. **Not backfilled** — inventing an acceptance row for a document under legal review is the exact failure being fixed. This card's build just calls `noteConsentGiven()`.

- **🚧 Journey #3 · Screen 4 — "confirm your plan" (redesign agreed, not built)** `partner` `ux` `trust` `billing` — The commitment moment, and today a lone card floating dead-centre in white. Keeps: both prices stated, partner branding, annual as visual default, USD note. **Agreed changes:** (1) **show the annual saving** — $499 vs $588 is $89 / two months free, currently unstated and the cheapest conversion win in the journey; (2) **line-item it as "Pay today" and then the monthly/annual charge** — nothing totals the US$1,299 actually leaving their account on the annual path, and someone clicking "Continue — annual" expecting $499 gets the shock on Stripe's page (the most likely source of refund requests); (3) **"Confirm your plan" → "Choose how you'd like to pay"**; (4) add the reassurance promised in the email — cancel anytime, it's yours, nothing you already have gets touched — since this is the point of maximum anxiety and none of it is here; (5) **"Need a hand? Call Sam 021 000 000"** centred at the bottom as a `tel:` link, pulled from the partner's support phone.
  - **🔑 The structural one — Ricky's call: this stops being a standalone page and becomes a modal over their own dashboard.** After claiming, land them on their dashboard with the payment modal above it and the dashboard softly blurred behind. No cross or cancel button, but clicking outside dismisses it. That single change fixes three separate findings at once: the site no longer transfers to them *silently* (they can see the thing they now own sitting behind the modal), the "lost modal in a field of white" framing goes away, and abandoning has somewhere to land instead of a dead tab.
  - **Consequence to build:** a **claimed-but-unsubscribed** dashboard state needs a genuinely prominent "Set your site live" CTA, because that is now the recovery path for everyone who dismisses the modal. That state exists in the data already (`plan='pending'`, stage **Claimed — not paid**) but has no customer-side treatment.
  - **🔴 Turn Postmark click tracking OFF for auth + claim email.** The magic link arrived wrapped as `track.pstmrk.it/...`. Two problems: link-scanners and some mail apps pre-fetch tracked URLs, which can **burn a one-time sign-in token before the human clicks**; and a cautious cafe owner hovering the link sees a tracking domain rather than `upwind.build`, which is exactly what a phishing link looks like. *(Confirmed working in the same test: the underlying link is now `auth.upwind.build`, so the custom-domain fix holds end to end.)*

- ~~**🚧 Journey #3 · Screen 5 — Stripe checkout**~~ ✅ **all three changes built** — original notes kept below `partner` `billing` `trust` — **First live proof the Stripe rebrand worked:** the page reads **"Subscribe to Upwind"** with the swoosh logo, and the old "Lite Websites / Try Coreshift Website" leak that kicked this whole thread off is gone — that red gate is closed. Email pre-filled, country correct, Link + Amex/UnionPay offered, and Stripe states **$1,299.00 then $499.00 per year**, which is precisely the total Screen 4 refuses to show. Statement descriptor added by Ricky. **Changes:** (1) **🔴 the partner disappears entirely at the moment of payment** — every prior screen carried Fernoak, then the cafe owner pays a company they have never heard of. We are merchant of record so their logo can't replace ours, but Checkout takes a description line: add *"Website built by Fernoak Digital"* so the connection survives the handover. (2) **🔴 the build fee is invisible** — one total, no breakout of US$800 build + US$499 hosting; Stripe keeps line items under a collapsed **Details ▾**, so expand it or carry the split in the description. (3) **Kill the promo box** — on a partner-negotiated price "Add code" invites *"what code? am I not getting the best price?"*. One line: `create-checkout/index.ts:127` sets `allow_promotion_codes: 'true'`; removing it removes the field. Nice-to-have: tell them what shows on their bank statement, and that the site goes live straight after paying.

- ~~**🚧 Journey #3 · Screens 6–8 — post-payment**~~ ✅ **built (waves 4–5)** — original notes kept below `partner` `ux` `trust` — Real test purchase completed end to end: payment → webhook → site live → celebration → dashboard, in about four seconds, site returns 200. **Screen 7 (celebration) — shipped already:** upsell removed (asking for another US$10/mo during the one unambiguously good moment cheapened it; the domain prompt waits on the dashboard instead) and "Go to my site now" opens a new tab *and* dismisses the celebration, so returning lands on the dashboard rather than a party they have already had. **Screen 8 (their dashboard) — agreed changes:** (1) greeting drops the name entirely — it was rendering the email local-part ("Nzricky") as though it were a person; (2) **"Your Upwind Partner" card under the left nav** with the partner's logo, name and contact details — Fernoak was invisible as a *contact* on the dashboard, the Stripe checkout and the billing portal, three screens in a row, while the whole trust model says contact your partner first; (3) the orange "Finish your site" banner becomes a **suggestions dropdown on the site card** instead of a second competing CTA; (4) the green domain banner is far too wordy — cut to two buttons, **"Connect your domain"** and **"Buy a domain and connect $10/mth"**; (5) "Your website" subtitle deleted as fluff. ✅ **Billing portal verified working** for the customer: Upwind $49/mo, next billing 27 Aug, card on file, invoice history, cancel available.
  - **Backlog — support stack (build shortly after launch, Ricky):** the dashboard chat bubble is **Upwind** support, not the partner's. Needs the whole thing before it is honest: wiki, help docs, agentic chat, and only then escalation to a human ticket.
  - **Parking lot:** Stripe's own portal offers **Cancel subscription in one click with no save-the-customer step** — the in-app billing page with a retention flow is still unbuilt.

- ~~**🚧 Journey #3 · Screen 9 — the editor, first look**~~ ✅ **built (wave 5)** — original notes kept below `partner` `ux` — Tour fires unprompted and opens well: *"This is your website — have a poke around… You really can't break it."* Chrome from the retail restructure holds up. **Fixes:** (1) **stale instruction copy** — the editor still tells people to "hit Review & publish", a button that no longer exists; (2) **fork the tour by user type** — step 6 says "Publish is what puts your changes in front of visitors", which is right for a customer and wrong for a partner, whose button now reads "Send this offer"; (3) **"Publish" → "Update my site"** once a site is live — after paying, their mental model is *save my changes*, not *publish*. Seven tour steps confirmed fine for a first run. *Site content itself is throwaway test data — ignored.*
  - **🔑 Support boundary, decided:** **no "ask your partner" anywhere post-sale.** Once payment clears we want the partner selling, not supporting; Upwind's own help button is the only support route. Contact details for the partner belong pre-sale — invitation and claim page — where they are still closing. ⚠️ This constrains the Screen 8 partner card: it should establish *who built it*, not offer a support channel, or customers will ring the partner anyway.

- ~~**🚧 Journey #3 · Screen 10 — connect a domain**~~ ✅ **built (wave 6)** — original notes kept below `partner` `ux` `domains` — Today it is a single text box. Good: it names their site and promises "your free address keeps working too", which is the right anxiety-killer up front. **Everything else specified in the conversion workstream is missing.** Fixes agreed: (1) **basic step-by-step registrar instructions**, low-jargon, with a **"contact us to help you"** route — this is where a screenshare with the customer happens, and it also carries the "ask Upwind to do it for you" path; (2) **warn that connecting a domain replaces whatever it currently points at** — if it is on a Wix site today, the public sees this instead; (3) **placeholder is `www.yourbusiness.com`**, which teaches the opposite of the apex-primary + www-CNAME policy — fix it; (4) add a **"don't worry" section**: email, calendars and everything else on the domain are untouched (this is exactly why the policy is A/CNAME-only, never nameservers); (5) **state propagation times**.
  - **✅ Superseded 2026-07-30 — the upsell now goes somewhere, and the price changed.** It was US$10/mo advertised with nowhere to do it; it is now **US$5/mo** with a real two-option modal and live availability search behind it. Registration itself is still a fulfilment ticket — see the P9 card in In Progress. Adding the second button surfaces the offer; **fulfilling it is task #104 (P9 · domain purchase + auto-connect)** — domain search, registrar purchase, auto-connect and the +$10 billing line, none of which exist. A Dynadot MCP is available, so the registrar side is reachable.

- **🎯 Next: journey #3 — the cafe owner** `partner` `ux` `trust` — Ricky rejoins here. The scenario to design for: someone walks into a cafe with a finished website on an iPad and says *"want this?"* — and the owner could go from greeting to a live website **inside an hour**. Powerful, and frightening in equal measure, so the claim → checkout → live → their-dashboard path has to be kid-gloved: no jargon, no surprise charges, obvious who to call, obvious how to undo. Doubles as the missing full purchase test on the new Stripe account and the first real partner-branded email send.

- **Go-live dress rehearsal** `launch` — The last step. One clean end-to-end run as a brand-new customer: sign up → generate/edit a site → publish → confirm the lead email lands → connect a custom domain → hit the trial gate → pay → confirm the site stays live. Fix whatever it surfaces. Runs once the three gates above are cleared (test mode first, then flip Stripe live).

## ⚪ Backlog

- **Wire CI security scanning — secret scanning, dependency audit, DAST** `PIA` `security` `CI` — PIA Section 5, and legal is right: `.github/workflows` holds four workflows and **all four are deploys** (deploy-production, deploy-staging, deploy-functions, deploy-edit-service). No secret scanning, no dependency audit, no DAST — verified 31 Jul. The other CoreShift apps have these. A few hours of work and the cheapest of the three PIA build items; do this one first so the App Audit has something to record.
- **Run and record the Upwind App Audit** `PIA` `security` `Section-5` — PIA Section 5 wants to reference an App Audit record and none exists anywhere in the repo (verified 31 Jul). Sequence it **after** the CI scanning card so the audit has real scan output to record rather than a note saying scanning isn't wired.
- **Actually enforce the stated retention periods** `PIA` `retention` `Section-6` `pg_cron` — PIA Section 6. Verified against prod 31 Jul: three `pg_cron` jobs are scheduled and **all three are observability** — alert-errors (*/15), uptime-check (*/5), monthly-report (1st). Nothing deletes or prunes anything. `error_logs` is unbounded (10 rows, oldest 28 Jul — simply never pruned). Note the preview-archive sweep was *deliberately* left unbuilt with its predicate already written into `20260729120000_preview_site_archive.sql`, because an unattended process taking a live customer's site dark is the one failure here that doesn't fail safe — that decision stands and should be recorded as a decision, not a gap. **This carries the real deadline of the three PIA build items:** stated retention periods that nothing enforces become a live compliance gap the moment the policy is published.
- **Strip PII from the crawl blob before it goes to Anthropic** `privacy` `Anthropic` `crawl-site` `generate-site` — The cheapest real risk reduction available, and unlike ZDR it is entirely ours to control. `generate-site` currently ships up to **24,000 characters** of unfiltered scrape to Anthropic — `crawl-site` takes `main`/`body` textContent across the homepage and internal pages, which in practice sweeps up staff bios, named testimonials, client names, direct-dial numbers and personal email addresses. Plus a `team` field explicitly formatted `"Name — Role"` per line. There is no redaction, sanitisation or PII filtering anywhere before the call (grepped 31 Jul — zero). Stripping obvious `mailto:` / `tel:` patterns and name-dense bio blocks would cut the third-party PII surface regardless of what any provider retains.
- **Declare Openprovider as a sub-processor before registration ships** `PIA` `privacy` `M7` `api.openprovider.eu` — `domain-search` calls `api.openprovider.eu` today and it is not on the sub-processor list. Right now it transmits only the domain string being checked, so arguably no personal data passes — but domain **registration** is the obvious next feature (M7) and registration transmits full registrant WHOIS details. Get it listed now with that boundary written down explicitly, so the line moves before the feature does rather than after.

- **Design Engine — Phase B: 7 sections migrated** `enhancement` `shipped` — Hero, Feature grid, Gallery, Rich text, CTA, Team and Contact form rebuilt in the Tailwind engine (parallel factory run), each with 4–6 real variants + the kill list (columns→smart, hero-buttons→emptiness-hides, etc.); contracts preserved, form capture intact. On the review board.
- **Design Engine — Wave 2: 14 new section types** `enhancement` `shipped` — Pricing, FAQ, Stats, Logo bar, Process, Services, Locations, Video, Announcement banner, Comparison, Timeline, Before/After, Articles and Awards — parallel factory run, each authored in the Tailwind engine with 1–3 real variants on one content contract, scroll-reveal + stagger motion, form/collection hooks intact. Registered in the catalog **and** taught to the AI onboarding generator so "make me a site" can emit them. **Section library now 22 types.** On the review board (`?gallery=section&type=<name>`).
- **Design Engine — Wave 3: 6 new flagship themes** `enhancement` `shipped` — Nocturne (dark-native cinematic — the first dark-default theme), Grid (Swiss/international, dense), Bloom (organic wellness), Heritage (craft/establishment authority), Pop (playful flat DTC) and Atelier (fashion-sparse) — parallel factory run, each a full contract-v2 colour-agnostic style language (neutral + dark scale, type scale, spacing rhythm, imagery + motion tokens, signature css) on the same section contract. Taught to the AI generator too. **Theme library now 17.** On the review board (`?gallery=section&type=hero&theme=<id>`).
- **Design Engine — Wave 4: 8 industry site templates** `enhancement` `shipped` — Full multi-page starter sites, each an archetype paired with a best-fit theme and built from the whole library: Bistro (restaurant/nocturne), Trade Co (builder/corporate), Law & Co (legal/heritage), Serenity (wellness/bloom), Peak Fitness (gym/bold), Lens (photographer/atelier), Corner Shop (retail/pop), Consult (agency/grid). Home + 2–3 pages of realistic NZ small-business copy across pricing/faq/stats/services/process/testimonials/etc. **Template library now 14.** Preview any at `?template=<id>`. Also fixed a latent bug: the unregistered cta variant `band` (rendered via fallback) → `banner` across all templates. This unblocks the new-site flow's template-hover-reskin picker (L9).
- **Site generation quality** `enhancement` `post-launch` — "Make me a site" works but runs slow and is limited to a few theme styles; speed it up, widen the theme range, sanitise generated copy/links before publish.
- **Custom-domain lifecycle** `enhancement` — Verify / retry / remove a hostname; purge cache on custom-domain go-live; tear down on cancel.
- **Account lifecycle** `enhancement` — Rename / leave / transfer-ownership / delete-account (GDPR), a co-owner / recovery path, and an access-change audit trail.
- **RLS/telemetry hardening** `security` — Tighten the `sites` anon read (tenant enumeration), disable `site-assets` bucket listing, lengthen the preview password, add view-count integrity.
- **Test coverage** `infra` — No tests yet for edge functions, RLS/multitenancy, checkout/webhook, or href sanitization.
- **Lazy-load the editor** `enhancement` — Code-split the overlay so it never ships to end-visitors on published sites.
- **Branch flow** `infra` — dev/staging are behind main; reconcile to match the local → dev → staging → main standard.

## 🅿️ Parking Lot — consciously deferred, needs a decision

- **Three memberless prod accounts, all leftover test data** `infra` `cleanup` — Surfaced (not caused) by `20260731110000`, which reports them rather than deleting them: **Fernoak Digital** `5eed0001…` (seed, no site), **TK Steel** `0f69bf13…` (has a site; Russ's membership was removed by the 30 Jul consolidation), **Best Chef 厨匠** `18ba231a…` (has a site; Ricky's test claim, membership swept). All three are unpaid `trial` accounts with no Stripe subscription, so no customer and no money is involved — but two of them own sites nobody can manage, and TK Steel carries the Stripe and ledger history behind the US$27.34 clawback. **Deleting an account cascades** `prospects.agency_account_id` and `account_links.agency_account_id`, and a cascade has eaten a seed site here before, so this needs the move-children-first treatment, not a `delete from accounts`. Not urgent, but it should not still be true when a real customer's data is in the same tables.

- **Re-consent when `TERMS_VERSION` changes** `legal` `product` — Now that consent is only recorded on the act, bumping the version no longer silently marks everyone as having accepted (which is the fix). But the flip side is that after a bump, existing users hold consent to the *old* version only, and nothing asks them to accept the new one. That gap is now visible in `terms_acceptances` instead of being papered over. Needs a product call from Ricky on the prompt: a blocking modal on next sign-in, or a banner. Also the honest route to backfilling the Partner Agreement rows that were never captured — ask the existing partners to accept it, rather than inventing rows.

- **Deferred out of the partner W1–W4 rebuild** `partner` — six things deliberately left, none blocking journey #3. (a) **Payout cadence is contradictory at the source** — the code pays monthly on the 1st, the partner program plan specifies fortnightly batches with a 14-day maturation; copy now matches the code, but one of the two is wrong and it's a promise to partners, so **Ricky/accountant decide**. (b) **Per-client Results** — only the empty state was re-voiced; a partner still can't hand a client "here's what your site did last month", which is the strongest renewal lever we have. (c) **Build-fee guidance** in the Sell dialog — no anchor like "partners typically charge $600–$2,000", so partners will guess low. (d) **Preview of the invitation** before sending — the partner brand page previews it, the Sell dialog doesn't. (e) **Who does a partner call?** The support widget routes a *client* to their partner; there's no equivalent path for the partner themselves. (f) **Client name on the site card** — cards show Draft / Offer sent but not who the client is, which stops scaling around ~10 sites.

- **Deferred out of the journey #3 build (waves 1–6)** `partner` `ux` `deferred` — Five things consciously left. (a) **`www.` is not auto-paired** — connecting `yourbusiness.com` does not also create `www.yourbusiness.com`; the modal tells them to connect it as a second address, which works but is a step a customer shouldn't have to think about. Auto-creating both custom hostnames is the fix; the reason it isn't done is that the unadded one would sit "pending" forever in the Connected list. (b) **Registrar steps only appear after they submit** — the records don't exist until then, so someone who wants to read the instructions *before* committing can't. (c) **The domain buttons key off `liveSite`** — an account with two sites (one live, one claimed-unpaid, exactly the fixture state) gets a banner about the wrong site. Real customers have one site, so this is a test-account artefact, but it will bite the first customer who buys a second. (d) **"Buy a domain" is a support ticket, not a purchase** — honest, but it is a human in the loop until #104. (e) **The build-fee commission accrual has never been observed end to end** — hosting commission is verified on prod, but no completed payment has ever carried a build fee (because of the bug above), so `build_fee_commission` ledger rows are still theory. Worth one test-mode purchase to confirm before a real partner sells anything.

Things left out of a shipped feature on purpose — not forgotten, parked here for Ricky to review and decide whether they're worth doing. Move up to Backlog / This Week if we want them.

- **Google Places photos into generated sites** `enrichment` `deferred` — We capture Places hours/phone/rating/address, but **not photos**. The Places Photo API returns a photo *reference* needing a second media call, and embedding our API key in public image URLs would leak it. To do it safely we'd resolve each photo server-side into our own Storage during generation. Value: richer auto-generated galleries for imported/discovered businesses. Cost: extra API calls + Storage + build-time latency.
- **Partner payout fraud flags (automated)** `partner` `deferred` — The obvious **self-referral** case is now blocked (a user can't attribute to a partner account they belong to), and manual `partner_flags` are read by the payout runner. Still schema-only: **automated** velocity holds (new partner + anomalous conversions), card-fingerprint/IP self-referral detection, and details-change holds. Gated on real volume + accountant sign-off. ⬆ Priority raised by the global partner push — one motivated bad actor at scale is now the threat model.
- **PayPal payout rail (auto)** `partner` `deferred` — PayPal payouts are "manual" today (admin sends, then marks paid). Auto-execution via the PayPal Payouts API is a fast-follow if partners prefer PayPal at volume.
- **Routefusion — embedded global payouts** `partner` `deferred` — If the global push wants payouts *embedded natively* in the partner dashboard (own the flow + FX margin), Routefusion is the strongest candidate — the payout layer is built behind a swappable interface so it's a rail swap, not a rebuild. Action: get a Routefusion (and Trolley, for contrast) sales quote. Note: the US-tax-form automation those platforms lead with is largely irrelevant to a NZ payer.
- **Partner education platform** `partner` `deferred` — A "Resources" slot in the partner dashboard was reserved for a future training/education platform. Backlog by Ricky's own call — parked here so it's visible.
- **Self-serve content export** `legal` `deferred` — Platform Agreement clause 13.2 promises a self-serve content export we haven't built (currently assisted-export). Either build it or lean on the assisted-export clause (flagged to lawyer).
- **Exact Upwind brand assets** `brand` `deferred` `needs-ricky` — Live logo/favicon are a stopgap SVG (blue swish + "upwind" wordmark). Drop the real `logo.svg` + `favicon` + a 1200×630 `og.png` into `marketing/public/` and I'll wire the actual mark into the headers + share cards. Colours (#5CA6F2 / #F4F6FB) are exact.
- **Re-attach test custom domains on upwind.page** `infra` `deferred` — Any test custom hostnames (e.g. theboys.co.nz) were on the retired coreshift.page SaaS zone; they'd need re-adding via connect-domain on the upwind.page zone. Test data — do if a demo needs it.
- **Editor polish: publish-success swoosh** `design` `deferred` — The last small bit from the editor design: a publish → **success-swoosh** animation with the button settling to "● Live" after a site goes live. Nice-to-have; the publish + Pay+Publish flows work today. *(The dashed "add a section" hover pill it used to be paired with is now shipped — see the editor-improvements card in Done.)*
- **Marketing "included" grid: emoji → line icons** `design` `deferred` — The spec calls for replacing the emoji icons in the marketing "what's included" grid with 1.6px-stroke line icons. Low priority — the emojis render fine; the rest of the marketing page is on the new type/motion system.
- **Stale CI job-name labels (coreshift.page)** `infra` `deferred` — The GitHub Actions workflow **display names** still read `app-staging.coreshift.page` / `coreshift.page` (deploy-staging.yml, deploy-production.yml `name:` fields + comments). Purely cosmetic — the actual `wrangler.*.toml` routes correctly target `staging.upwind.build` / `upwind.build` / `*.upwind.page` and deploys land in the right place — but the labels mislead during ops. Rename when convenient (editing the workflow file triggers a redeploy, so batch it with other CI work).
- **App bundle code-split** `infra` `deferred` — The dashboard bundle is ~944 kB (one chunk) — the editor, full section/theme registries, lucide icons and admin/partner views all ship to every user. `React.lazy` the Editor / Admin / Pipeline routes + dynamic-import the renderer/overlay. Works fine today; splitting speeds first load.

### Audit follow-ups (P1 — do before scale / before real partners)

- **Edge-function observability + alerting** `infra` `audit-p1` — Accrual + payout failures now write to `error_logs`, but there's still **no alerting** — a failing webhook or a silently-failing `monthly-report`/pg_cron would go unnoticed until someone complains. Add an alert on new `error_logs` rows (email/Slack) and make `monthly-report` surface wholesale failures instead of returning 200.
- **CI gates on edge functions + money-path tests** `infra` `audit-p1` — `deploy-functions.yml` deploys with **no typecheck/test/lint**; there are **zero tests** on webhook branch logic, `accrue_commission`, the payout reserve/finalize RPCs, or RLS. Add a Deno test harness + a typecheck gate so money/auth code can't deploy broken.
- ~~**Disaster-recovery / staging drift**~~ ✅ **resolved 2026-07-28** — staging rebuilt from the squashed baseline and matches production exactly; a clean-DB rebuild is now proven every time a preview branch is created, not merely hoped for. See the branching card in Done.
- **Rate limits / quotas on paid-API functions** `security` `cost` `audit-p1` — `generate-site` (Anthropic), `discover-leads` (Google Places), `crawl-site` and `become_partner` have **no per-user rate limit or quota** — a signed-up user can loop them to burn budget. ⬆ More urgent with open global partner signup. Add per-account daily quotas + velocity caps.
- **`claim_token` expiry + single-use** `security` `audit-p1` — Partner claim links never expire and the token stays reusable after conversion (grants account ownership); it travels in a URL query string (history/referrer leak). Add expiry (7–14d) + null-on-claim + prefer a one-time POST exchange.
- **`send-invite` relay + `auth-email` fail-open** `security` `audit-p1` — `send-invite` sends from Coreshift's Postmark domain with attacker-controlled recipient/CTA (phishing vector); derive fields server-side from an owned invite row. `auth-email` returns `true` when its hook secret is unset (fail-open) — make it fail closed + constant-time compare.
- **Cancelled site served up to 24h from edge cache** `billing` `audit-p1` — The renderer checks the CF cache before `site_is_served`, and `subscription.deleted` doesn't purge — a cancelled site keeps serving real content for up to 24h. Call `purge-cache` on cancel (+ on going-live).
- **Out-of-order webhook resurrect** `billing` `audit-p1` — A late `subscription.updated(active)` after `deleted` can flip a cancelled site back to active (Stripe doesn't guarantee order). Needs an event-timestamp / status guard (deferred — a naive guard would break legitimate re-subscribe).
- **`request_access` / `invite_agency` overload ambiguity** `bug` `audit-p1` — Each has a 2-arg + 3-arg-with-default overload → Postgres throws "function is not unique" when called with exactly two args. Drop the legacy 2-arg forms.

### Audit follow-ups (P2 — polish / conversion / scale)

- **Marketing: social proof + SEO batch** `marketing` `audit-p2` — Homepage has **zero third-party proof** (testimonials/logos/counts) — first-order conversion drag. Also: the SEO batch (robots.txt, sitemap.xml, OG/Twitter cards, Organization/SoftwareApplication/FAQ JSON-LD) is **built but held** pending a 1200×630 `og.png` share card — produce it and ship. Add testimonials once real ones exist.
- **Post-checkout "you're live" moment** `funnel` `audit-p2` — `?welcome=1`/`?billing=cancelled` return params are unhandled — a paying customer lands on a plain dashboard with no confirmation, a cancelled checkout gets no recovery nudge. Cheap activation win.
- **Two overlapping legal docs** `legal` `audit-p2` — `/terms` (surfaced everywhere) vs `/platform-agreement` (the actually-binding one, nearly unreachable from marketing). Decide the canonical one, cross-link or merge.
- **Trial-warning UX now moot, but check dunning** `funnel` `audit-p2` — The trial-expiry surprise-charge risk is gone (draft model replaced the trial), but there's still no custom dunning beyond Stripe's on `past_due`, no win-back on cancel, and the happy-customer referral-link ask isn't surfaced to retail. Lifecycle growth loops.
- **Supabase advisor lints** `perf` `security` `audit-p2` — ~113 perf lints (unindexed FKs on the busy tables, RLS policies re-evaluating `auth.uid()` per row — wrap in `(select auth.uid())`), plus: `site-assets` bucket allows public listing, Auth leaked-password protection is off, `pg_net` in public schema. None urgent; batch before scale.
- **Weak preview password + Video `safeUrl` gap** `security` `audit-p2` — Prospect-preview password is 6 hex chars (brute-forceable, anon, unthrottled) — lengthen / rely on the 64-hex token + throttle. The Video section renders `href` without `safeUrl` (stored-XSS on published sites) — wrap it like every other section.
