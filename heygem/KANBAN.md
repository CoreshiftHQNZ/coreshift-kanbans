# HeyGem — Kanban

> Visual rollout state. Edit this file and run `node tools/build.js heygem` from the coreshift-kanbans repo root to refresh the published board.
>
> **Card format:** `- **Title** \`tag\` \`tag\` — Description.`

---

## ✅ Done

- **Ratings, Stripe webhook + payment visibility for Gems (verified)** `phase-r5` `phase-r4` `shipped` — Closes the core loop: completed jobs now prompt the customer for stars + feedback + optional photo via a new `submit_rating` RPC (the old direct-INSERT policy on `ratings` is gone — it couldn't enforce that the job was actually finished). New `stripe-webhook` edge function reconciles everything Stripe decides *after* the initial charge POST — async success, later decline, refund, dispute, detached card — signature-verified (HMAC over the raw body, 300s replay window, constant-time compare) and idempotent via a `stripe_events` ledger. **Two real double-charge bugs found and fixed in the existing live code:** `stripe-charge-job` sent no Stripe `Idempotency-Key`, so a double-clicked retry could create two real PaymentIntents; and it discarded the PaymentIntent Stripe attaches to a 402, orphaning declined charge rows. Receipt emails are now gated on an atomic compare-and-set so a race can't send two. Gems see payment state per job (paid / failed / refunded / disputed / processing) with a Retry button, ratings in-thread, and a "completed but not paid" signal on the Done tab; CRM shows avg rating + outstanding charges. Verified live: signed/unsigned/replayed/stale webhook probes all behave, migrations applied, all three apps build clean.
- **Gem workspace: assignment · My jobs · typing · chat images · auto Gem fee (verified)** `phase-r3` `shipped` — Opening a job auto-assigns it to the Gem, with a My jobs tab + count badge. Chat scrolls the message list only (header/composer pinned). "Gem is typing…" shown to the customer via a realtime broadcast channel. Gems can attach images (public `chat-images` bucket). Every quote auto-carries a 15% Gem fee as its own line item, through the composer, the customer quote and the receipt.
- **Archive flow · card-modal fix · better new-request prompt (verified)** `phase-r2` `shipped` — A declined or quiet request can be tidied away by either side (new `archived` status + `archive_job` RPC). Team workspace is New / Active / Archive / Done. Fixed the "add card modal pops up then vanishes" — the Payment Element was failing to load during the live-key cutover; it now shows a clear error instead of blanking. New-request sheet prompts for pax / budget / delivery location / when instead of pre-filled idea chips.
- **Stripe switched to LIVE (verified)** `phase-r4` `shipped` — HeyGem takes real payments. Vault `stripe_secret_key` swapped to the live secret; the customer app's publishable key set to `pk_live` on Railway and confirmed in the rebuilt bundle. All test-mode Stripe state (customer IDs + saved cards) cleared so live mode creates fresh customers. Account verified read-only: NZ / NZD, charges + payouts enabled. Local dev stays on test keys.
- **Team panel: any @heygem.co.nz email can sign in (verified)** `phase-r3` `shipped` — `is_gem()` now authorizes any authenticated `@heygem.co.nz` user, and `ensure_gem()` auto-provisions their gems row on load. Auth switched from password to self-serve magic link, guarded to the domain. Verified a brand-new @heygem.co.nz user gets full access and a non-@heygem.co.nz user is blocked. Revoke by offboarding the email.
- **Marketing site live — coming-soon off, CTAs into the app (verified)** `phase-r1` `shipped` — heygem.co.nz is a live landing page, not a waitlist: coming-soon pill and early-access form removed, Get started + Log in CTAs into app.heygem.co.nz. Customer auth reworked signup-first with a value-prop screen; the marketing Log in link deep-links via `?mode=login`.
- **Job lifecycle + numbers, and a Gems-only CRM (verified)** `phase-r2` `shipped` — Every job carries one persistent number for its whole life: Enquiry #1024 → Quote #1024 → Order #1024. Collapsed the old 9-status machine; approval opens the order directly as In process; payment state stays in `charges` (no `charged` status). New Customers tab: editable company facts, multiple contacts, gem-only internal notes (separate table so they can't leak to the customer), full order history with incl-GST totals. Verified end-to-end on both apps with a real $506 test charge + receipt.
- **Approval confirm dialog · GST receipt email · account menu (verified)** `phase-r4` `shipped` — Approve opens a confirmation showing the incl-GST total and explaining the card is charged only on completion. Charge-on-complete emails a branded Postmark receipt with line items + Subtotal / GST 15% / Total paid + card last4. New Account screen to change payment method and browse past receipts, each expandable to its per-quote line-item breakdown.
- **Auth + lead emails via Postmark (branded, verified)** `infra` `shipped` — Supabase Auth SMTP switched from built-in to Postmark; branded amethyst templates (magic link / confirm / recovery) live + verified rendering. Email rate limit raised 2→100/hr (the 2/hr default was silently throttling sign-ins). Lead-notification emails switched on (notify-lead edge secrets) and verified end-to-end. site_url + redirect allowlist set (customer magic-link login unblocked).
- **R4: Stripe pay-per-job (test mode) verified** `phase-r4` `shipped` — Card on file at first approval (SetupIntent + Stripe Elements), off-session charge when the Gem marks complete, idempotent. Stripe secret in Supabase Vault (service-role RPC, no plaintext env); 3 edge functions + a charge-on-complete trigger. **Verified with a real Stripe test charge**: complete → PaymentIntent succeeded → job “charged” → customer UI “Paid” live ($95 + GST = $109.25). Live keys still gated on company registration; Payment Element to eyeball on the HTTPS deploy (preview iframe shows it blank).
- **R3: Gem web workspace verified** `phase-r3` `shipped` — apps/staff: password sign-in + is_gem gate, master-detail workspace (New/Active/Done queues across all businesses), per-job Gem thread, quote composer (ex-GST + live GST preview), status actions. Gem RPCs: claim_job, send_quote, set_in_progress, complete_job. Drove the whole loop through the real UI — open request → send quote → (customer approves, live) → Start → Mark complete. Email also switched Resend → Postmark.
- **R2: schema v2 (jobs/chat) verified** `phase-r2` `shipped` — businesses → profiles, pooled gems, jobs (status machine), per-job message threads, quotes, ratings, push devices. RLS throughout (cross-business isolation proven in a rolled-back impersonation test); customer transitions via SECURITY DEFINER RPCs (setup_account, create_job, approve/decline_quote, cancel_job); Realtime on jobs/messages/quotes.
- **R2: customer chat app verified** `phase-r2` `shipped` — apps/customer: magic-link sign-in, onboarding, job list (active/archived), live per-job thread with the quote card (GST breakdown) + approve/decline. Drove the whole loop through the real UI with a seeded session — request → live Gem quote via Realtime → approve → "On it". Not deployed yet (app.heygem.co.nz comes with the Railway consolidation).
- **R1: coming-soon splash LIVE** `phase-r1` `shipped` — heygem.co.nz + staging now serve a "Business Concierge & Errands" coming-soon page. Example requests rendered as chat bubbles (morning tea, Terry's $500 gift, branded water bottles, Christmas party, team building, Sydney conference travel). Early-access form (name + email + "what would you ask your Gem?") → leads table with new `email` column, source `coming_soon`; notify pipeline updated + verified e2e. Old plans-model marketing removed.
- **Apple Developer account: DONE** `phase-r5` `unblocked` — Registered under CORESHIFT LIMITED (D-U-N-S on file). Apple-side store submission is no longer gated; Google Play org account still to set up.
- **PIVOT: concierge model locked** `pivot` `decision` — HeyGem is now a chat-first concierge/errands service for NZ small businesses ("organise team morning tea for Friday"). Thread-per-job chat with auto status/archiving; all-in quote in chat → approve → card charged on completion → push for star rating + feedback + photo. Purely pay-per-job (weekly plans + $1/min dead). Customers get web + native iOS/Android from one React codebase via Capacitor; Gems stay on a web workspace.
- **Foundations (pre-pivot, all survive)** `phase-0` `shipped` — Monorepo + @heygem/ui design system (amethyst brand, real logo/favicon/hero art), GitHub `CoreshiftHQNZ/heygem` (dev→staging→main), Supabase project (ap-southeast-2), Railway deploys.
- **Marketing site LIVE** `phase-0` `shipped` — heygem.co.nz (production/main) + staging.heygem.co.nz (staging), HTTP 200, env inlined. Sells the OLD model — repositioning is Phase R1.
- **Lead capture + notifications** `phase-0` `shipped` — Callback form → Supabase `leads` (anon-insert RLS), `notify-lead` edge function + pg_net trigger, verified e2e. Emails activate when `RESEND_API_KEY` + `LEAD_NOTIFY_TO` secrets are set.
- **Blank-page deploy fixed** `phase-0` `fix` `shipped` — Vite env vars were missing on Railway services; vars set on both services/envs and the Supabase client made lazy/resilient (missing env degrades the lead form, never blanks the page).

## 🟡 In Progress

- **Phase R5: native apps + push + ratings** `phase-r5` — **Rating flow is DONE** (stars + feedback + photo, live). Remaining: wrap the customer app with Capacitor (iOS/Android) and FCM/APNs push (new quote / job done / rate it). Apple account ready; Google Play account and the local native toolchain still to sort.

## 🚫 Blocked

- **Google Play org account** `phase-r5` `needs-ricky` — Apple is DONE (CORESHIFT LIMITED + D-U-N-S). Remaining: Google Play Console org account (US$25 + identity verification, wants the same D-U-N-S). Only gates Android store submission, not the build.

## 🔵 This Week

- **Capacitor wrap** `phase-r5` `blocked-toolchain` — Add Capacitor to the customer app; first iOS/Android builds. **Build machine can't produce a binary yet:** no Xcode (command-line tools only), no Android SDK, no Java runtime. Push also needs a Firebase project + `google-services.json` (FCM) and an APNs auth key (.p8). Scaffolding and device-registration plumbing can be written before any of that lands; verification can't.
- **Google Play account** `phase-r5` `needs-ricky` — US$25 + verification under CORESHIFT LIMITED.

## ⚪ Backlog

- **Gem web workspace** `phase-r3` — Pooled request queue, claim, chat, quote composer, mark complete (fires the charge). Lead inbox moves here from Supabase Studio.
- **Stripe pay-per-job** `phase-r4` — Card on file at first approval (SetupIntent), off-session charge on completion, GST receipts, decline handling. Test mode until registered.
- **Capacitor wrap + push + ratings** `phase-r5` — iOS/Android builds from the same React app, FCM/APNs push (quote/done/rate), rating flow with photo upload, store assets + submission.
- **Consolidate Railway to one service** `infra` `tech-debt` — Two services exist (`marketing` → heygem.co.nz prod; `heygem` → staging.heygem.co.nz staging). Collapse to one service with two environments.
- **SEO + analytics** `phase-r1` — Meta/OG, sitemap, analytics on the repositioned site.
- **Real testimonials & stats** `content` — Replace placeholders with concierge-flavoured proof once first customers land.
- **Brand polish pass** `low-priority` `content` — On-dark logo variant, brand guide, type refinements when a designer is involved.
