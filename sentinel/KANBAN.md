# Coreshift HQ Ops — Kanban

> Visual rollout state. Edit this file and run `update-kanban.cmd` to refresh `ops-kanban.html`.
>
> Card format: `- **Title** \`tag\` \`tag\` — Description.`

---

## ✅ Done

- **Report Issue widget V1** `phase-0` `shipped` — Live on staging.keycontent.ai with Bug / Suggestion / Question types, screenshot upload, and type-aware placeholders.
- **Postmark rewire** `phase-0` `shipped` — Edge Function migrated from Resend to Postmark with verified sender.
- **app_id field** `phase-0` `shipped` — Forward-compatible field for the Sentinel multi-app vision.
- **CEO Pitch Deck** `deliverable` — 14 slides including the "P0–P3, Elevated" elevation slide.
- **HTML Ops Playbook** `deliverable` — Single-file viewer for the 5 HOW-WE-DO docs.
- **ROADMAP.md** `deliverable` — Master rollout plan with Sentinel future state baked in.
- **HOW-WE-DO-PRIORITY.md** `playbook` — P0–P3 + user-impact narrative + blast radius elevation.
- **HOW-WE-DO-PR-REVIEWS.md** `playbook` — Behavior-based, code-free review checklist.
- **HOW-WE-DO-INCIDENTS.md** `playbook` — Incident runbook with the 1 AM Rule.
- **HOW-WE-DO-DEPLOYS.md** `playbook` — Three rules: staging, rollback, gradual.
- **HOW-WE-DO-BUG-REPORTS.md** `playbook` — Codifies the Phase 0 widget pipeline.
- **HOW-WE-DO-APP-AUDITS.md** `playbook` — Onboarding gate + quarterly re-audit checklist. Captures the first KeyContent audit inline.
- **Register APP-AUDITS in playbook renderer** `tooling` `shipped` — PLAY 6 now renders in ops-playbook.html (73.4 KB).
- **HIBP password protection** `audit` `ops` `shipped` — Enabled on staging + prod. `get_advisors` confirms no more leaked-password warnings.
- **Sentinel kanban published** `infra` `shipped` — Live at coreshifthqnz.github.io/coreshift-kanbans/sentinel/ alongside DigitalArchitect. Includes kanban + playbook + roadmap. Pitch deck deliberately internal.
- **Cloudflare org access** `infra` `shipped` — Operator now has org access. Unblocks Sentry env vars, gradual rollout, alert integration, and all downstream Phase 1+ Cloudflare work.
- **GitHub branch protection** `audit` `ops` `shipped` — Rules added on `main` + `staging`: PR required (0 approvals — 2-person team), linear history, force-push blocked, deletion blocked. Admin bypass left enabled (judgment-call exception for Ricky).
- **Supabase backup retention verified** `audit` `ops` `shipped` — Prod has 8 days of daily physical DB backups. Storage objects NOT included (see Phase 1 Week 5 follow-up for that gap).
- **Sentry env vars in Railway** `phase-1-w1` `shipped` — `VITE_SENTRY_DSN` + `VITE_SENTRY_ENVIRONMENT=staging` planted in Railway staging environment. Frontend builds on Railway (not Cloudflare Pages — doc correction needed; see backlog).
- **Sentry SDK installation** `phase-1-w1` `shipped` — Both `keycontent-frontend` and `keycontent-backend` actively receiving events on staging. Release tracking confirmed (commit SHA tagged on deploys). Crash-free sessions: 100%. **Phase 1 Week 1 complete.**
- **Better Stack uptime monitors** `phase-1-w2` `shipped` — Free tier, 3-min checks across 2 regions for production + staging. Bonus monitor on coreshifthq.com. Email alerts to operator.
- **Public status page (Better Stack subdomain)** `phase-1-w2` `shipped` — Live at https://keycontent.betteruptime.com showing production status only. Custom domain (status.keycontent.ai) deferred — requires Better Stack paid tier.
- **SSL/TLS Full (Strict)** `audit` `ops` `shipped` — Cloudflare encryption mode bumped from Full → Full (Strict) for `keycontent.ai`. End-to-end cert validation now enforced between CF and Railway origin.
- **Cloudflare alert integration** `phase-1-w2` `shipped` — HTTP DDoS Attack Alert + Universal SSL Alert configured at account level. Email destination: abe@coreshifthq.com. Both enabled. **Phase 1 Week 2 complete.**
- **HOW-WE-DO-DEPLOYS v2 (Railway revision)** `docs` `shipped` — Standard Deploy Pipeline, Three Rules (Rule 3 reframed from "gradual" → "observable"), Rollback Procedures, and V0 promotion example all updated to match Railway reality. Cloudflare's role narrowed to DNS+CDN+WAF.
- **HOW-WE-DO-PR-REVIEWS v2 (Railway revision)** `docs` `shipped` — 5-step review replaced with Two-Phase Review: Phase A (pre-merge, description + files-changed + clarifying Qs) + Phase B (post-merge-to-staging behavior testing). Safety net retitled to "Sentry + Better Stack + Railway fast rollback."
- **Cloudflare Bot Fight Mode** `audit` `ops` `shipped` — Enabled on `keycontent.ai` zone with JS Detections on. Free-tier bot/abuse defense for auth endpoints + general traffic.
- **GitHub labels** `phase-1-w1` `shipped` — 13 new labels (P0-P3, radius:*, status types) live in the repo.
- **GitHub issue templates** `phase-1-w1` `shipped` — Bug / Feature / Question YAML forms merged via PR #70.
- **GitHub project board** `phase-1-w1` `shipped` — KeyContent Triage with auto-add workflow.
- **Sentry projects** `phase-1-w1` `shipped` — keycontent-frontend + keycontent-backend created and named.
- **Sentry secrets (Supabase staging)** `phase-1-w1` `shipped` — SENTRY_DSN_EDGE + SENTRY_ENVIRONMENT planted.
- **Sentry secrets (GitHub Actions)** `phase-1-w1` `shipped` — SENTRY_AUTH_TOKEN + SENTRY_ORG + SENTRY_PROJECT planted.
- **Postmark domain verification** `ops` `shipped` — `keycontent.ai` fully verified (DKIM + Return-Path); `hello@keycontent.ai` signature live.
- **Gmail inbox rules** `ops` `shipped` — `KeyContent/Bug` (red+star+important), `/Suggestion` (yellow), `/Question` (blue+important) labels and subject filters live.
- **Briefing template** `phase-1-w4` `shipped` — `briefs/templates/BRIEF-template.md` created. Reusable skeleton for future briefs based on patterns across RLS / Sentry / bug-reports briefs.
- **HOW-WE-DO-TRIAGE.md (Triage rhythm)** `phase-1-w4` `shipped` — PLAY 7 in the playbook. Codifies the 10-min morning ritual. Type-aware shortcuts for Suggestion/Question.
- **Phase 2: `bug_reports` table + user ack email** `phase-2` `shipped` — Widget reports now persist as rows. Users get immediate "we got it" email on submit. Foundation in place for Sentinel Dashboard.
- **PR review checklist in repo** `phase-1-w4` `shipped` — `docs/standards/pr-reviews.md` published in keycontent-app. **Phase 1 Week 4 complete.**
- **Phase 1 W5: in-repo docs bundle** `phase-1-w5` `shipped` — `docs/standards/priority.md`, `docs/standards/incidents.md`, and `OPERATOR.md` all published. **Phase 1 fully complete.**
- **Sentinel architecture locked** `phase-3` `shipped` — Architecture decisions agreed: new `sentinel-app` repo, React+Vite+Express+Supabase stack mirroring KC, Railway-hosted at sentinel.coreshifthq.com, own Supabase project for auth+audit, reads `bug_reports` from KC via service-role, hardcoded email allowlist for V1.
- **Sentinel V1 — Brief 1: scaffold + auth** `phase-3` `shipped` — PR #1 merged 2026-05-15 (commit 79dd540). Repo scaffolded with KC-style structure, 13/13 tests passing, auth shell + empty 5-column dashboard live. Two review-fix commits before merge.
- **Brief 1 operator follow-up** `phase-3` `ops` `shipped` — Supabase redirect URLs (`/auth/callback` × 3), Railway env vars verified, GH Actions workflow permissions = R+W, `dev` branch confirmed, watch-branches verified. Sign-in tests pass on staging: allowlisted email → `/dashboard`, non-allowlisted → `/access-denied`. Lessons learned (worth documenting): Cloudflare orange-cloud proxy hides CNAME from Railway verification (do the proxy-flip dance temporarily during setup); newer Railway custom-domain adds also require a TXT `_railway-verify.<subdomain>` record.
- **RLS fix for `webhook_events`** `security` `P1` `shipped` — Migration promoted to KC prod 2026-05-15. Bookkeeping bug in `schema_migrations` (Supabase `apply_migration` MCP auto-stamped apply-time version `20260515142600` instead of the filename version `20260512192536`) caught by operator citing own playbook rule on manual-SQL drift; repaired via aligned UPDATE so future `db push` doesn't choke. Advisors clean on prod. Playbook updated with new anti-pattern + recovery one-liner. PR #71 (`fix/rls-webhook-events` → staging) merged 2026-05-19.
- **Zernio webhook secret restored on KC prod** `incident` `SEV3` `shipped` — `ZERNIO_WEBHOOK_SECRET` added to Railway prod 2026-05-19 after Ricky shared the value. Redeploy verified. Two `post.published` events successfully processed at 11:06–11:07 UTC (verified via `webhook_events` rows + matching Railway `POST /api/webhooks/zernio 200`s). 16-day outage ended. Reconciliation of 38 mis-statused items + post-mortem queued separately in backlog.
- **Pitch dry-run** `ops` `shipped` — Held before Monday's pitch.
- **CEO Pitch (Monday May 18)** `milestone` `shipped` — Deck + playbook + live staging demo presented. _Operator: update with outcome / follow-ups if any._
- **HOW-WE-DO-APP-AUDITS v2** `phase-3` `docs` `shipped` — Audit checklist bumped to v2 with new Railway section (env-var parity, watch-branch, health-check) + Supabase `schema_migrations` consistency checks. Triggered by the 2026-05 incident pair (Zernio 16-day outage + RLS bookkeeping drift). Live on the Sentinel ops site. Both findings would have been caught at the previous quarterly audit if v2 had existed.
- **Sentinel Brief 2 env-var pre-flight** `phase-3` `ops` `shipped` — `KEYCONTENT_SUPABASE_URL` + `KEYCONTENT_SUPABASE_SERVICE_ROLE_KEY` set to KC staging values on both Sentinel production AND staging Railway environments (2026-05-20). Brief 2 dev work now unblocked. When Phase 2 ships to KC prod, flip back to KC prod values via the same path.
- **Sentinel V1 — Brief 2: render bug_reports cards** `phase-3` `shipped` — Read-only 5-column board wired to KC staging's `bug_reports`. Cards group by status; type emoji (🐛/💡/❓) + app tag + time-ago + optional priority pill + narrative-or-excerpt + reporter email. Skeleton loader, error banner with retry, refresh button. PR [#4](https://github.com/CoreshiftHQNZ/sentinel-app/pull/4) merged to dev → staging; PR [#6](https://github.com/CoreshiftHQNZ/sentinel-app/pull/6) merged staging → main 2026-05-20. 20/20 tests pass (7 new). First time Sentinel has real data on screen — foundation for Briefs 3–7.
- **Node 22 nixpacks bump** `phase-3` `infra` `shipped` — Brief 2 surfaced that `@supabase/supabase-js` eagerly constructs a Realtime client at `createClient()` time, which checks `global.WebSocket`. Node 20 has it only behind `--experimental-websocket`; Node 22 has it natively. `nixpacks.toml` updated from `nodejs_20` → `nodejs_22`. PR [#5](https://github.com/CoreshiftHQNZ/sentinel-app/pull/5) merged 2026-05-20. _Lesson: when bumping any app to use `supabase-js` server-side, set Node ≥ 22 (or pass `realtime.transport: ws`)._
- **Sentinel branch-protection bypass dance documented** `phase-3` `ops` — Three times this session (PRs #71, #4, #5, #6) the auto-promote workflow (`deploy-staging.yml` push `dev → staging`) was blocked by branch protection requiring review approval. Worked around with admin-merge + manual `git push --force-with-lease=staging:origin/staging origin origin/dev:staging`. **Update 2026-05-20:** Brief 3's PR #7 merge auto-promoted to staging cleanly, but Brief 4's PR #9 merge did NOT — inconsistent behavior suggests the rule still isn't reliably configured. Status verification still queued in This Week.
- **Sentinel V1 — Brief 3: drag-and-drop status updates** `phase-3` `shipped` — First WRITE operation against KC's `bug_reports` from Sentinel. `PATCH /api/reports/:id` with UUID + status-allowlist validation + distinct `wontfix` rejection. `@dnd-kit` primitives wrap `ReportCard` (draggable) + `ReportColumn` (droppable). `useUpdateReportStatus` mutation with optimistic `setQueryData` + onError revert + onSettled invalidate. Inline toast for error feedback. PR [#7](https://github.com/CoreshiftHQNZ/sentinel-app/pull/7) merged to dev → staging; PR [#8](https://github.com/CoreshiftHQNZ/sentinel-app/pull/8) merged staging → main 2026-05-20. 31/31 tests pass (11 new for PATCH). Verified end-to-end on Sentinel staging — Triage → To Do drag persists in KC's DB.
- **KC prod service-role key rotation** `security` `ops` `shipped` — The KC prod service-role key was logged in a Railway env-var listing on 2026-05-19 during the Sentinel `KEYCONTENT_*` swap session. Rotated same day via Supabase dashboard (Reset). KC Railway prod env + KC Supabase Edge Function secrets updated by operator with the new value. In-memory key on the running KC process picked up the new value via the unrelated Railway outage that forced a container restart — verified by uptime drop on `/health` afterward. **Anti-pattern captured:** `npx railway variables` (no flag) dumps full values to stdout; use `--kv` with regex extraction for any secret-bearing variable inspection.
- **Zernio post-mortem** `incident` `docs` `shipped` — `incidents/2026-05-19-zernio-webhook-secret.md` written + PR [#76](https://github.com/CoreshiftHQNZ/keycontent-app/pull/76) admin-merged to KC staging. Establishes the `incidents/` directory in keycontent-app. Action items spawned as KC issues [#77](https://github.com/CoreshiftHQNZ/keycontent-app/issues/77) (Sentry 5xx webhook alert) and [#78](https://github.com/CoreshiftHQNZ/keycontent-app/issues/78) (Zernio 401-doublet investigation).
- **Sentinel V1 — Brief 4: card detail + triage fields** `phase-3` `shipped` — Click a card → side panel slides in from the right → operator edits priority, blast_radius, narrative inline → Save sends a partial PATCH to KC. Generalized `PATCH /api/reports/:id` to accept any subset of writable fields with strict per-field + envelope validation (unknown top-level keys rejected). `ReportDetailPanel` with backdrop/ESC/X close + unsaved-changes confirm. `useUpdateReportStatus` renamed/generalized to `useUpdateReport`. PR [#9](https://github.com/CoreshiftHQNZ/sentinel-app/pull/9) merged to dev → staging; PR [#10](https://github.com/CoreshiftHQNZ/sentinel-app/pull/10) merged staging → main 2026-05-20. 41/41 tests pass (10 new). Status edits still drag-and-drop-only (clean separation of interaction models).
- **Sentinel V1 — Brief 4.5: Google OAuth + Coreshift HQ domain check** `phase-3` `auth` `shipped` — Email/password + magic-link sign-in replaced with Google OAuth restricted to `@coreshifthq.com`. Server-side domain check (not env-var allowlist). `SENTINEL_ALLOWED_EMAILS` retired from Railway. Sign-in tested: allowlisted Coreshift Google identity → `/dashboard`, non-Coreshift → `/access-denied`. PR merged to dev → staging → main 2026-05-20.
- **Sentinel V1 — Brief 5: auto-file GitHub Issue when leaving Triage** `phase-3` `shipped` — `server/lib/github.ts` with `createIssue` + body/title builders. `POST /api/reports/:id/file-issue` route. Frontend hook + panel button + card badge. PAT pre-flight: fine-grained `GITHUB_TOKEN_KC` scoped to `keycontent-app` (Issues: R+W) on Sentinel Railway prod + staging. PR [#15](https://github.com/CoreshiftHQNZ/sentinel-app/pull/15) merged 2026-05-20. Closes Zernio post-mortem action item.
- **Sentinel V1 — Brief 6: resolution-ack email when → Done** `phase-3` `shipped` — `server/lib/postmark.ts` with HTML-escaped resolution email builder. PATCH `done`-transition logic extends the existing endpoint with a synthetic-row safety check. Panel footer shows "Resolution email sent <time ago>". Reused KC's Postmark server + `POSTMARK_API_KEY`. Promoted with dry-run + synthetic-row safety. PR merged 2026-05-20.
- **Sentinel V1 — Brief 7: Better Stack status pills** `phase-3` `shipped` — `server/lib/betterstack.ts` with `listMonitors` + normalize. `GET /api/status` route with 10s in-memory cache. Frontend `useStatus` hook + colored `StatusPills` component on Dashboard. PR [#21](https://github.com/CoreshiftHQNZ/sentinel-app/pull/21) merged 2026-05-21 — auto-promoted cleanly (CI workflow hardening pay-off). **Closes Phase 3.**
- **CI workflow hardening (Sentinel + KC)** `phase-3` `infra` `shipped` — Sentinel PR #19 + KC PR #84. Node 22, `permissions: contents: write`, force-with-lease in the dev → staging promotion. Eliminated the every-PR manual force-push friction that plagued Briefs 4–6. Brief 7 PR #21 was the proof — auto-promoted on first try.
- **Sentinel V2 — Brief 8: filter strip (app filter slot, conditional render)** `phase-4` `shipped` — Opens Phase 4. Server-side `?app=` filter on `/api/reports`. Client-side `useUrlFilter` (popstate sync + replaceState) drives `useReports` via filter param. `FilterStrip` + `AppFilter` components conditionally render only when ≥ 2 apps are onboarded (no UI noise for KC-only state). PR merged 2026-05-21.
- **Sentinel V2 — Brief 9a: /maintenance V1 foundation** `phase-4` `shipped` — New operational surface for tracking AI-model lifecycle, vendor reviews, and recurring upkeep. `AppShell` + `Sidebar` (Inbox + Wrench icons). Sentinel-owned Supabase tables (`maintained_apps` + `maintenance_entries`) with deny-all RLS + service-role bypass. Onboard / edit / delete entries. Keycontent onboarded + 5 entries seeded.
- **Sentinel V2 — Brief 9b: auto-discovery scanner** `phase-4` `shipped` — `node-cron` v3 nightly job (2am UTC) walks every onboarded app, runs 8 model-name patterns (gpt-3.5, gpt-4, o1-, o3-, claude-, gemini-, mistral-, llama-) against GitHub Code Search, upserts findings into `maintenance_entries`, archives ones that disappeared. `POST /api/maintenance/apps/:id/scan` for manual triggers; "Scan now" button per onboarded app on `/maintenance`. Took **three deploy attempts** to land: (1) node-cron@4 crashed on ESM `__dirname` (real bug, confirmed in dashboard logs); (2) v3 downgrade ALSO crashed for the same reason — turned out node-cron v3 has the same `__dirname` use; (3) **third attempt added an esbuild `banner` polyfill** that injects `__dirname`/`__filename`/`require` derived from `import.meta.url`. The banner fix was sitting **uncommitted in the local working tree** the whole time (PR #40). Once committed → staging + prod deployed cleanly. Migration `20260521030000_add_running_to_scan_status.sql` applied to Sentinel Supabase 2026-05-22. PR [#38](https://github.com/CoreshiftHQNZ/sentinel-app/pull/38) (code) + [#40](https://github.com/CoreshiftHQNZ/sentinel-app/pull/40) (banner fix) merged 2026-05-22.
- **Brief 9a follow-up: service_role GRANT on maintenance tables** `phase-4` `fix` `shipped` — Brief 9a's migration enabled RLS + deny policies but omitted the underlying `GRANT SELECT/INSERT/UPDATE/DELETE` to `service_role`, which RLS bypass needs. Discovered when Brief 9b's promotion attempt surfaced 16hr of `permission denied for table maintained_apps` errors in Sentry. Fix migration applied via Supabase dashboard 2026-05-22. Lesson folded into HOW-WE-DO-APP-AUDITS via the v2.2 audit check.
- **Sentinel V2 — Brief 10: vendor lifecycle reference table** `phase-4` `shipped` — New `public.vendor_lifecycle_reference` table on Sentinel Supabase, keyed by (kind, identifier) and seeded with 22 known dependency rows (Anthropic Claude, OpenAI GPT/embeddings, Google Gemini families). `/maintenance` entry endpoints (GET list, POST, PATCH) enrich each entry with `lifecycle_reference` via in-memory join. UI: banner in `MaintenanceEntryPanel` when the reference is more pessimistic than the entry's own status, with an "Adopt suggestion" button that PATCHes status + sunset_date in one go; small ⚠ badge on entry cards. Shared pessimism helper at `shared/lifecycle-pessimism.ts` with 6 unit tests. PR [#31](https://github.com/CoreshiftHQNZ/sentinel-app/pull/31) merged 2026-05-22.
- **Sentinel V2 — Brief 11: maintenance digest (manual + auto-schedule)** `phase-4` `shipped` — `server/lib/digest.ts` buckets entries into "vendor reminders" (Brief 10 disagreement) + "sunset approaching" (within 60 days). HTML + text email bodies rendered, HTML-escaped. `POST /api/maintenance/digest/send` endpoint with optional `dryRun` + `to` overrides. "Send digest" button in `/maintenance` page header for ad-hoc previews. **Auto-schedule** via `server/lib/digest-scheduler.ts`: weekly Monday at 10am Pacific/Auckland (DST-correct via node-cron's `timezone` option). Empty digests are sent anyway — heartbeat behavior. Kill switch via `DISABLE_DIGEST_SCHEDULER=true`. 21 unit tests total (14 for builder + 7 for scheduler). Manual button PR [#32](https://github.com/CoreshiftHQNZ/sentinel-app/pull/32) + auto-schedule PR [#43](https://github.com/CoreshiftHQNZ/sentinel-app/pull/43) → #44 → #45 merged 2026-05-22.
- **Catch-block Sentry discipline (KC #88)** `phase-3-followup` `observability` `shipped` — Audit found 138 catch-block sites across 23 KC server route files returning 5xx without `Sentry.captureException`. PR [#91](https://github.com/CoreshiftHQNZ/keycontent-app/pull/91) → #92 → #93 promoted dev → staging → main. All 138 sites now capture with route + resource-ID context.
- **KC Sentry env-var drift (incident + fix)** `incident` `SEV3` `shipped` — Sentry vars set on KC staging when Phase 1 W1 shipped, never propagated to prod. KC backend Sentry was a silent no-op for ~9 days. Discovered during Quick Post 502 triage. Vars copied staging → prod on Railway via CLI. Prod backend now observable for the first time (verified via timestamped probe). Same drift pattern as the 2026-05-19 Zernio incident.
- **Sentry env-var drift post-mortem** `incident` `docs` `shipped` — `keycontent-app/incidents/2026-05-22-sentry-env-var-drift.md`. PR [#98](https://github.com/CoreshiftHQNZ/keycontent-app/pull/98) → #99 → #100 promoted dev → staging → main 2026-05-22.
- **HOW-WE-DO-SENTRY-ALERTS.md (new playbook)** `phase-3-followup` `docs` `shipped` — PLAY 7 in the ops playbook. 3-rule recipe (new-issue, regression, high-frequency) for every Coreshift Sentry project. Triage rhythm renumbered to PLAY 8.
- **HOW-WE-DO-APP-AUDITS v2.2 (full env-var diff)** `phase-3-followup` `docs` `shipped` — Refined the Railway env-var parity check from "check whichever var we're investigating" to "diff all keys staging vs prod" with shell + PowerShell one-liners. Triggered by the 2026-05-22 Sentry drift incident — the 2026-05-19 pass missed the Sentry block because it focused on Zernio only.
- **V0 widget promoted to KC prod** `phase-0-prod` `shipped` — User reported "Failed to fetch" on KC prod ticket submit. Diagnosis: widget UI was shipping to prod via Cloudflare but the Supabase backend never followed (Phase 0 backlog item from May). Closed the gap in one session: bug_reports table + bug-report-screenshots Storage bucket migrations applied to KC prod Supabase, report-issue Edge Function deployed via Supabase CLI, secrets set (POSTMARK_SERVER_TOKEN reused from Railway's POSTMARK_API_KEY + REPORT_TO_EMAIL=abe@coreshifthq.com). Operator confirmed live submission works end-to-end.
- **Sentinel prod KEYCONTENT_* flipped to KC prod** `phase-0-prod` `ops` `shipped` — Sentinel was reading bug_reports from KC staging since May (task #7 carry-over). With V0 widget now writing to KC prod, flipped Sentinel prod's `KEYCONTENT_SUPABASE_URL` + `KEYCONTENT_SUPABASE_SERVICE_ROLE_KEY` to KC prod values. Sentinel staging stays on KC staging (sandbox isolation).
- **ONBOARDING.md for Sentinel design sessions** `docs` `shipped` — Self-contained handoff doc for a UI/UX design session: visual system (full color tokens, dark-mode-canonical), page inventory with maturity tags, component vocabulary, six concrete angles for a whole-app design system pass, hard constraints. Lives at github.com/CoreshiftHQNZ/sentinel-app/blob/main/ONBOARDING.md. PR [#35](https://github.com/CoreshiftHQNZ/sentinel-app/pull/35) → #36 → #37.
- **Sentry alert rules applied across 4 projects** `phase-3-followup` `ops` `shipped` — Operator configured one consolidated `Notify Abe` alert in Sentry covering all 4 Coreshift projects (KEYCONTENT-BACKEND/FRONTEND, SENTINEL-BACKEND/FRONTEND), 3 trigger types (new issue, escalation, regression), filtered to error/fatal levels, 5-min throttle. Newer Sentry UI lets one rule span projects + triggers, so the original PLAY 7 design (12 separate rules) was replaced by a single consolidated rule. Verified end-to-end via the regression probe — fresh issue → alert email landed in inbox 2026-05-25. PLAY 7 updated to document the v2 consolidated pattern with the v1 design retained for reference. **Closes the Zernio post-mortem's "Sentry alert on persistent webhook 5xx" item (KC [#77](https://github.com/CoreshiftHQNZ/keycontent-app/issues/77)).**
- **Sentinel V2 — Brief 13: weekly social-token digest** `phase-4` `shipped` — Operator-facing weekly email at **Mondays 8am Pacific/Auckland** listing expired + expiring-within-14d social-account OAuth tokens across all KC clients. Two hours before Brief 11's maintenance digest so they don't land simultaneously. Mirrors the Brief 11 pattern: `server/lib/social-token-digest.ts` (builder), `server/lib/social-token-digest-scheduler.ts` (node-cron, DST-correct), `server/routes/socialTokenDigest.ts` (manual trigger with `dryRun`), "Send digest" button on /social-tokens. Also refactored: extracted `server/lib/social-token-snapshot.ts` so the GET route + the digest call the exact same data pipeline (can't drift). Threshold mismatch with UI's 7-day pill is intentional — email is "schedule the call" at 14d, UI pill is "drop everything" at 7d. Empty digest sends an "all healthy" heartbeat. 19 new tests (11 builder + 6 scheduler + 2 boundary), full suite 262/262. PR [#58](https://github.com/CoreshiftHQNZ/sentinel-app/pull/58) → #59 → #60 promoted dev → staging → main 2026-05-25. First autonomous send: next Monday 8am NZ.
- **Sentinel V2 — Brief 12: /social-tokens page** `phase-4` `shipped` — New `/social-tokens` sub-page on Sentinel. Surfaces every connected social account across every onboarded app with KC + Zernio health side-by-side. Status pills (expired / expiring_soon / unknown / healthy) with platform-specific TTL math (FB/IG/LinkedIn = 60d, TikTok = 365d). Sorted urgency-first so the eye lands on action items. New `server/lib/zernio.ts` (minimal listAccounts wrapper with 5-min cache) + `server/routes/socialTokens.ts` (aggregates KC service-role read + Zernio per-profile call). Per-profile Zernio failures isolate to "unknown" rather than blowing up the response. `ZERNIO_API_KEY` planted on Sentinel Railway prod + staging (copied from KC's legacy `LATE_API_KEY` — same secret). 243/243 tests pass (7 new on `socialTokens.test.ts`). Caught + fixed during the KC #102/#108 incident chain so the next OAuth expiry doesn't require an incident to discover. PR [#49](https://github.com/CoreshiftHQNZ/sentinel-app/pull/49) → #50 → #51 promoted dev → staging → main 2026-05-25.
- **KC pre-flight check for expired OAuth tokens (KC #108)** `kc` `P1` `shipped` — Follow-up to KC #102. After the backfill ran (PR #105), 8 of the 28 newly-backfilled accounts STILL failed at post-time. Root cause via diagnostic: Zernio marks accounts `isActive: false` when OAuth tokens expire, and KC had no signal back. New `verifyAccountActiveOnZernio(profileId, accountId)` helper in `server/services/zernio.ts` with 5-min per-profile cache. Wired into all 4 send-paths (compose, schedule, reschedule, publish-now). On detected-inactive: ALSO `UPDATE social_accounts.is_active = false` defensively so the UI catches up immediately. Falls through to optimistic post on Zernio API failure. New diagnostic scripts: `script/audit-zernio-active.ts` (one-shot + `--sync` flag, already run live to flip 8 accounts) and `script/diagnose-zernio-post.ts` (revealed the root cause). 42/42 KC tests pass. PR [#108](https://github.com/CoreshiftHQNZ/keycontent-app/pull/108) → #109 → #110 promoted dev → staging → main 2026-05-25.
- **KC Zernio backfill (28 stale accounts → 0)** `kc` `data` `shipped` — Plan A from the KC #102 follow-up plan: written + ran the data backfill BEFORE any user had to reconnect. New script at `script/backfill-zernio-account-ids.ts` walks each client's stale social_accounts, calls `zernio.listAccounts(client.lateProfileId)`, matches by Zernio account ID / unique-platform / displayName, and populates `zernio_account_id`. Ran dry-run first against KC prod (clean), then `--apply` — **matched 28/28 (100%) via Strategy A (exact ID match)**. Confirmed via DB: `SELECT COUNT(*) FROM social_accounts WHERE is_active AND zernio_account_id IS NULL` = 0. Zero users needed to reconnect. PR [#105](https://github.com/CoreshiftHQNZ/keycontent-app/pull/105) → #106 → #107 promoted dev → staging → main 2026-05-25.
- **KC Zernio account-ownership fix (KC #102)** `kc` `P1` `shipped` — Discovered via Railway error "Zernio schedulePost failed: One or more accounts do not belong to this user." Root cause: KC was sending `lateAccountId` (old "Late" publisher's IDs) to Zernio in 4 send-paths (compose + 3 calendar paths). The schema comment on `social_accounts` already flagged this as the follow-up read-side switch from PR #4's dual-write that never landed. Scale: 28 of 34 active social_accounts (82%) were silently failing on post — only post-PR-#4 accounts worked (their `late_account_id` column happens to hold a Zernio ID). Fix: switch all reads to `zernioAccountId`, null-guard with structured `ACCOUNTS_NEED_RECONNECT` error so affected users get a clear "reconnect" message instead of the cryptic Zernio one. Also swept in the `publishNow: true → scheduledFor: now` fix from PR #86 for `calendar.ts` publish-now (was still on the old code path). 42/42 KC tests pass. PR [#102](https://github.com/CoreshiftHQNZ/keycontent-app/pull/102) → #103 → #104 promoted dev → staging → main 2026-05-25. Follow-ups: data backfill script for 28 stale accounts; webhook handler matching by `zernioAccountId`; rotate prod creds committed in `script/migrate-v1-to-v2.ts` (security).
- **Brief 9b follow-up: scanner rate-limit resilience** `phase-4` `fix` `shipped` — First autonomous Brief 9b nightly scan (2026-05-22 02:00 UTC) tripped GitHub's secondary rate limit on Code Search because 2am UTC = 2pm NZ — peak operator daytime — and the scanner raced operator GitHub activity against a per-USER shared limit. Two fixes: (1) `scan-scheduler.ts` timezone `UTC` → `Pacific/Auckland` so the cron fires at 2am NZ (quiet window); (2) `github-search.ts` now detects 403 + "rate limit" body and retries once after 90s (absorbs transient secondary-limit hits without operator-visible failure). Non-rate-limit 403s still error immediately. 3 new tests on `github-search.test.ts`; full suite 236/236. PR [#47](https://github.com/CoreshiftHQNZ/sentinel-app/pull/47) → #48 promoted dev → staging → main 2026-05-22.
- **PLAYBOOK-cloning-to-new-app.md (PLAY 9)** `phase-4` `docs` `shipped` — Self-contained runbook for onboarding the next Coreshift app to Sentinel. Calls out the V1 shared-backend decision (all apps write to KC's `bug_reports`), the two pre-onboarding code changes required before app #2 (generalize `fileIssue.ts` target lookup + per-app `GITHUB_TOKEN_*` resolution), a step-by-step checklist mapping every system that gets a touch (Sentinel UI, Sentry, Better Stack, Postmark, Railway env vars, GitHub Actions secrets), a verification checklist, and a V2 strategy (3 tracks: code-edit-elimination, Sentinel-side onboarding wizard, GitHub Actions reusable workflow) so onboarding eventually drops to <10 min without a Claude Code session. Registered as PLAY 9 in the ops playbook (147.8 KB rebuild).
- **esbuild banner: __dirname/__filename polyfill** `phase-4` `infra` `shipped` — The hidden root cause behind Brief 9b's two earlier deploy crashes. node-cron (both v3 and v4) has `const daemonPath = \`${__dirname}/daemon.js\`;` at module top level — ReferenceError in an ESM bundle. The polyfill banner via `createRequire`/`fileURLToPath`/`dirname` was sitting **uncommitted in the local working tree** the whole time. Once committed (PR #40), Brief 9b deployed cleanly on third attempt. Lesson: when a local build passes but CI crashes the same way twice, `git diff HEAD` is the first diagnostic.

## 🟡 In Progress

_None — Briefs 9b, 10, 11 + V0 promotion all shipped to prod today. Phase 4 is one brief away from done (Noozey onboarding via cloning playbook)._

## 🚫 Blocked

_None._

## 🔵 This Week

- **Run first scan against keycontent** `phase-4` `ops` — Now that Brief 9b is live on prod, hit "Scan now" on the keycontent row in `/maintenance` and verify scanner populates entries from the GitHub Code Search hits. Otherwise the nightly cron will fire tonight at 2am UTC automatically.
- **Sentinel UI/UX design pass** `phase-4` `design` — Hand the ONBOARDING.md link to Claude Design (or a human designer). Scope: typography scale, spacing rhythm, color token consolidation, ReportDetailPanel vs MaintenanceEntryPanel consistency audit.

## ⚪ Backlog
- **Status page custom domain** `phase-1-w2` `deferred` — Move from `keycontent.betteruptime.com` to `status.keycontent.ai`. Requires Better Stack Pro (~$25/mo).
- **Supabase Storage backup strategy** `phase-1-w5` `audit-followup` — Daily DB backups exclude Storage objects. Prod `job-assets` has ~890 user objects with no backup. Design a strategy.
- **"My past reports" view** `phase-2` — User-facing transparency. Deferred until Dashboard polish phase.
- **Pre-onboarding code changes (Track A of PLAY 9)** `phase-4` `code` — Two PRs in `sentinel-app` before Noozey can land: (1) generalize `server/routes/fileIssue.ts:45-46` hardcoded `keycontent-app` target → lookup by `report.app_id` from `maintained_apps`; (2) generalize `server/lib/github.ts:41-44` `GITHUB_TOKEN_KC` → per-app `GITHUB_TOKEN_<SLUG>` lookup with KC fallback. Recommend single org-wide PAT instead once the second app's pattern is proven. Without these, filing a GitHub issue from a Noozey bug report would create the issue in `keycontent-app` instead of the correct repo.
- **Onboard Noozey to Sentinel** `phase-4` — Apply [PLAY 9](playbook/PLAYBOOK-cloning-to-new-app.md) (cloning playbook). Each piece auto-flows into Dashboard + /maintenance as it lands. Closes Phase 4 once shipped. Blocked on Track A above.
- **V2 onboarding automation (Tracks B + C of PLAY 9)** `phase-4` `low-priority` — Two future improvements that compress onboarding from ~45 min (Claude Code) to <10 min (operator UI only): **Track B** — `/maintenance/onboard` wizard inside Sentinel using Better Stack + Sentry APIs to auto-create monitors + projects; **Track C** — GitHub Actions reusable `onboard-app.yml` triggered by `workflow_dispatch`. Recommend Track A first (one-time code work, unblocks app #2), then Track C (most leverage per LoC), then Track B if onboarding cadence justifies. See PLAY 9 §"V2 strategy" for full breakdown.
- **Sentry alert "heartbeat" detection** `phase-3-followup` `low-priority` — Sentry has no built-in "you stopped receiving events" alert. Options: liveness ping via daily cron, or Better Stack monitor on a `/__sentry-server-test` endpoint. Deferred — without the 3 base alert rules above, this wouldn't get noticed anyway. Tracked in `incidents/2026-05-22-sentry-env-var-drift.md`.
- **Zernio reconciliation followups (KC #79, #80)** `incident-followup` — Issue #79: live Zernio webhook handler doesn't update `jobs.status`. Issue #80: AeroFlow Filters Facebook posting failures on May 4/11/18 — platform-side pattern worth understanding.
- **Investigate 401-then-200 Zernio doublet (KC #78)** `incident-followup` — Every successful Zernio 200 since the secret was restored is preceded by a 401 ~1s earlier. Probably probe + real event, or HMAC format the handler doesn't accept first try. Not blocking; worth understanding before Zernio scales.
- **Match Zernio webhooks by `zernioAccountId` too** `kc` `low-priority` — `server/routes/zernio-webhooks.ts:486,523` still matches incoming events by `lateAccountId`. Works for new accounts (dual-write); old accounts get no status updates from incoming webhooks. Low impact because old accounts can't even post yet (PR #102 surfaces the reconnect-needed error first); will become more important after backfill lands.
- **Operator: reconnect 8 expired social accounts** `kc` `ops` — After the Zernio backfill (KC #105), 8 accounts still need user reconnection because their OAuth tokens expired ~May 22 (Facebook 60-day limit). Per-client list: **Growth Partners** (FB + LinkedIn), **Unistor** (FB), **Sharewise** (FB), **Impact HR Group** (FB), **LDM Motor Group** (FB), **Greg Riley** (FB), **AeroFlow Filters** (FB). Each: log in to KC → Settings → Social Accounts → disconnect + reconnect. After reconnect the dual-write path populates a fresh `zernio_account_id`. Sentinel `/social-tokens` (Brief 12) now surfaces these in real-time.
- **Brief 12 V2: per-row reconnect-email button** `phase-4` `low-priority` — Inline action on the `/social-tokens` page that drafts a Gmail to the affected customer with "your X account needs reconnection, here's how." Lighter-touch than visiting each Settings page manually. Defer until Brief 12 V1 has been used in practice for a few weeks.
- **Brief 12 V2: daily auto-sync cron** `phase-4` `low-priority` — Background job in Sentinel that runs the existing audit logic on a schedule (e.g. 4am Pacific/Auckland) and flips KC's `is_active = false` for any Zernio-side inactive rows. Currently this only happens on demand (post-attempt pre-flight OR a manual script run). Daily cron catches expirations within 24 hours rather than waiting for a user to discover them. Mirror of the digest scheduler pattern.
- **Brief 13 V2: per-customer reconnect emails** `phase-4` `low-priority` — Today's V1 sends the digest to the operator only. V2 would send personalized reconnect emails directly to each affected client's agency owner ("Hi [client], your Facebook account on KeyContent needs reconnection — here's how"). Requires per-customer email targeting, copy-edit review, and probably an operator-side staging step (V1 confirms format works first). Ship after V1 has run for ~4 weeks and the operator confirms the content reads right.
- **Rotate prod creds committed in `script/migrate-v1-to-v2.ts`** `kc` `security` — Discovered during the Zernio fix investigation: this V1→V2 migration script has hardcoded prod connection strings + a Supabase service-role key in plain text, committed to GitHub. Need to (a) rotate the service-role key, (b) remove the file or move creds to env vars + git-history scrub via BFG, (c) verify the service-role key wasn't used elsewhere.
- **Vendor lifecycle reference auto-population** `phase-4` `low-priority` — Brief 10 ships with 22 manually-seeded rows. Future enhancement: scrape Anthropic/OpenAI/Google deprecation pages on a cadence and upsert into `vendor_lifecycle_reference`. Probably HTML scraping + a kind-specific parser per vendor. Only worth doing once the manual edit cadence proves painful.
- **Monthly trend reviews** `recurring` — Recurring bug patterns analysis.
- **Quarterly free-tier usage check** `recurring` — Sentry / Postmark / Better Stack / Railway against limits.
- **Quarterly stale-issue prune** `recurring` — Close aged P3s.
