# Coreshift Privacy & Security — Kanban

> Cross-cutting program board. Edit this file and run `node tools/build.js privacy-security` from the coreshift-kanbans repo root to refresh.
>
> Card format: `- **Title** \`tag\` \`tag\` — Description.`
>
> The strategy doc (SECURITY-PRIVACY-PLAN.md) and the meeting brief (PRIVACY-SECURITY-MEETING-BRIEF.md) are kept local in `Sentinel/playbook/` since they list live findings. This board is the operational view.
>
> **Above-Industry-Standard Initiative (kicked off 2026-06-16).** Operator directive: *under-promise (the fine print), over-deliver (the practice) — be measurably above the small-SaaS baseline.* **Tier 1** (`tier-1` tag) = in our control, executing now: B5b tenancy sweep → DA→KC parity → tested backups+DR → fleet-wide scanning. **Tier 2** (`tier-2` tag) = operator/external decision, surfaced not executed: pen test, SOC 2 readiness, MFA. Full roadmap in SECURITY-PRIVACY-PLAN.md.

---

## ✅ Done

- **Phase B5b: T3 tenancy sweep — COMPLETE** `phase-B5` `security` `code` `above-standard` `tier-1` `shipped` — **Tier 1 workstream #1 done (2026-06-16). Threat model T3 (cross-agency data leak) closed at the KC route layer.** Swept every KC route file (KC PRs #198 → #215, dev→staging→main). **3 REAL LIVE vulns found + fixed:** `jobs.ts` (a non-admin with no agency saw/modified ALL agencies' jobs — `canScopeByAgency` treated `null` as unscoped), `calendar.ts` (a non-admin with no agency could read any agency's calendar via `?agencyId`), and **`billing.ts` (highest impact — any authenticated user could open another agency's Stripe Customer Portal / cancel its subscription / attach a Stripe customer, by passing `agencyId` in the body)**. **7 files hardened** (post-load 403 oracle → scope-in-query 404: business-context, topics, clients, products, overlay-templates, social + invites tightened). **9 files audited as already-safe** (agencies, agency-linking, notifications, business, zernio-webhooks, auth, review, support, admin + prompt-rules). **44 new unit tests, suite 102/102**, every batch prod-verified. **Post-sweep exploitation scan: no detectable sign of exploitation** — KC prod has 3 agencies, 0 with a Stripe customer, 0 subscription events; the ~18 no-agency accounts are dormant signups (reads aren't logged so read-exploit can't be positively excluded, but practical exposure was minimal). Full detail in `Sentinel/playbook/audits/2026-06-07-b5b-migration-plan.md`. **Follow-up spun out:** `files.ts` object-level storage IDOR (signed-url / `/objects/*` serve any path to any authed user) — separate threat class, see Backlog.
- **Migration-parity guardrail in the Monday digest** `security` `guardrail` `shipped` `multi-app` — Detection guardrail for the recurring prod/staging migration-drift incident class (login_fingerprints 2026-06-14, Sentry-env, Zernio — a migration applied to prod while staging is skipped, surfacing later as a confusing staging-only error). New `server/lib/migration-parity.ts` reads applied migration **names** from each app's staging + prod Supabase projects via the Management API run-query endpoint (reuses `SUPABASE_ACCESS_TOKEN`) and diffs them; the Monday digest flags any app where **staging is behind prod**. Sentinel PRs [#140](https://github.com/CoreshiftHQNZ/sentinel-app/pull/140) → #141 → #142 dev→staging→main (admin-merge). New `supabase_staging_ref` column on `maintained_apps` (applied + stamped on the shared Sentinel project; KC staging ref `hireapkvjkadlzzpcitx` populated; single-project apps like DA null → skipped). **Design notes:** compare by NAME not version (version diff false-positived 16× on KC due to divergent early-bootstrap timestamps); alert ONLY on staging-behind-prod — staging-ahead is normal pending-promotion. **Bonus fix:** the digest renderers early-returned on a zero-maintenance week, silently dropping the entire Privacy & Security section — now it always renders. 16 new tests, suite 301 passing. This is the "guardrail over habit" decision (operator: drift detection that doesn't depend on remembering, on-brand for Sentinel). The deeper source-fix (frictionless db-push so the prod shortcut isn't needed) remains a someday-item.
- **Velocity RLS exposure closed** `security` `P1` `shipped` `multi-app` — Surfaced by a Supabase advisor email Mal forwarded (2026-06-13). Live advisor check found **all 35 public tables on Velocity (`uodsmyystkaxoogitvsw`) had RLS disabled** — the anon key (shipped in Velocity's browser bundle) could read/write everything via PostgREST (`deals`, `users`, `transcripts`, `commission_records`, `tenants`…). **Fix:** enabled RLS on all 35 with no policies — safe because the browser uses Supabase for auth only (no `supabase.from` data queries) and all data is server-side via Drizzle over the pooler `DATABASE_URL` (owner role, bypasses RLS). Applied to live prod via `execute_sql`; recorded in repo as `script/enable-rls.sql` ([velocity#8](https://github.com/CoreshiftHQNZ/velocity/pull/8)) since drizzle-kit `push` doesn't manage RLS. **Verified:** 35/35 RLS on; advisor ERRORs cleared (now benign INFO `rls_enabled_no_policy`); anon-key probe returns 0 rows. Also enabled **HIBP leaked-password protection** via Management API (cleared the remaining WARN). Velocity advisor now 0 ERROR / 0 WARN. **Reusable pattern:** auth-only-client + server-owner-connection apps → enable-RLS-no-policy is the safe one-shot fix.
- **🚨 Org-wide Supabase advisor finding — remaining apps** `security` `P1` `multi-app` `in-progress` — The same advisor email flagged RLS-off across **8 Coreshift projects** (Velocity now fixed). Still open: **go-chiro** (`movfypgjqdosvacrinbq`) — **most urgent: patient health data** (`patient_messages` exposed w/ `patient_id`, `clinical_notes`; plus ~15 tables with `USING(true)` policies = effectively open). go-chiro is NOT the easy fix — it reads via the client with (broken) policies, so it needs real per-table policies written, not enable-RLS-no-policy. **mission-control** (`hwugqukvovfjsviqxqpj`) — ~15 RLS-off tables; likely the same architecture as Velocity (worth confirming for the one-shot fix). **Plus 5 more projects / 26 tables** not yet enumerated. **Meta:** none of these are onboarded to Sentinel, which is why the daily advisor scanner never saw them — onboarding closes the gap. go-chiro gated on ownership/authority confirmation (Abe).
- **Privacy & security plan kickoff** `phase-A` `meta` `shipped` — `Sentinel/playbook/SECURITY-PRIVACY-PLAN.md` published 2026-05-29 with phased plan (A: quick wins; B: foundation; C: Sentinel briefs 14-18; D: trigger-gated compliance). Operator forks listed inside the doc.
- **Phase A1: KC `bug_reports_set_updated_at` search_path hardened** `phase-A` `security` `shipped` — `ALTER FUNCTION public.bug_reports_set_updated_at() SET search_path = ''`. Closes [Supabase advisor Lint 0011](https://supabase.com/docs/guides/database/database-linter?lint=0011_function_search_path_mutable). Migration through dev→staging→main + applied to prod via `execute_sql` + `schema_migrations` stamped.
- **Phase A2: Sentinel `maintenance_set_updated_at` search_path hardened** `phase-A` `security` `shipped` — Same fix on Sentinel prod. Closes Lint 0011 there.
- **Phase A3: Sentinel `rls_auto_enable()` PUBLIC EXECUTE revoked** `phase-A` `security` `shipped` — Pre-flight verified: SECURITY DEFINER event-trigger function exposed to anon + authenticated via PostgREST `/rest/v1/rpc/`. `REVOKE EXECUTE ... FROM PUBLIC` — safe because event triggers fire via engine, bypassing routine_privileges. Closes Lints [0028](https://supabase.com/docs/guides/database/database-linter?lint=0028_anon_security_definer_function_executable) + [0029](https://supabase.com/docs/guides/database/database-linter?lint=0029_authenticated_security_definer_function_executable).
- **Phase A4: clean advisor baseline established** `phase-A` `audit` `shipped` — `get_advisors(security)` returns `lints: []` on both KC prod (`keycontent-v2`) and Sentinel prod. HOW-WE-DO-SECURITY.md "Current findings snapshot" refreshed to the new zero-findings baseline.
- **Phase A5: bug_reports end-to-end data flow documented** `phase-A` `privacy` `docs` `shipped` — `Sentinel/playbook/data-flows/bug_reports.md` published. ASCII diagram + table of every field, sub-processor (region + retention), RLS posture, 8 identified privacy gaps. Template for the next ones (social_accounts, clients, jobs).
- **Phase A6: KC privacy notice + cookies audited** `phase-A` `privacy` `audit` `shipped` — `Sentinel/playbook/audits/2026-05-29-kc-privacy-notice.md`. Headline: KC app has NO published privacy notice, ToS, consent prompt, or cookie banner — but also no analytics/tracking SDKs. Cookies/storage are operational only.
- **Coreshift T&Cs reviewed for Privacy Act 2020 coverage** `phase-A` `privacy` `audit` `shipped` — Verified the Type 3 Platform Subscription Agreement template covers Privacy Act 2020 (clause 7), notifiable breach under Part 6 (clause 7.4), data exit (clause 12, 60-day Export Window), Sentinel named (clause 8), customer data ownership (clause 5.4), sub-processor framework (Annexure 2 placeholder). **Insight:** the privacy gap is NOT contract-layer — it's end-user-facing notice on the app.
- **Public privacy/ToS hosting probed** `phase-A` `privacy` `audit` `shipped` — Confirmed: nothing published at `coreshifthq.com` or `keycontent.ai` (both SPAs catch-all to 1.7-1.9KB shells). B8b is "publish new," not "link existing."
- **Phase B1: data inventory** `phase-B` `privacy` `foundation` `shipped` — `Sentinel/playbook/data-flows/DATA-INVENTORY.md`. Classification scheme + all 30 KC tables + 2 Storage buckets (job-assets 935 objects, bug-report-screenshots 1) + 3 Sentinel tables + 7 sub-processor stub apps + 12 identified gaps.
- **Phase B7: sub-processor inventory** `phase-B` `privacy` `foundation` `shipped` — `Sentinel/playbook/data-flows/SUB-PROCESSORS.md`. 18 third parties classified with jurisdiction, data class, retention, DPA status, per-app applicability matrix. ~~Originally flagged Kling as a P1 IPP 12 concern based on its `is_active=true` flag in `ai_providers`~~ — closer audit (2026-05-30) found the adapter was a stub and `ai_call_logs` showed 0 Kling invocations across 1,269 historical provider calls. False positive resolved via Path A (see Done).
- **Kling scaffolding removed** `phase-B` `privacy` `shipped` `path-a` — Operator chose Path A in response to the audit-method correction: delete the dead code + DB rows. **KC PRs #140 → #141 → #142** merged dev → staging → main. Code changes: deleted `server/services/ai/kling.ts` (stub), removed `runKlingPrompt` dispatch branch in `services/ai/index.ts`, narrowed `AIProviderName` union, removed kling seed entries. DB cleanup applied to KC prod via `execute_sql` + stamped (migration `20260530010000_drop_kling_provider`): `kling_rows_remaining: 0`. **Audit-method lesson:** the next sub-processor inventory should join `ai_providers` (configuration) with `ai_call_logs` (actual invocations) — registered ≠ in use. Captured in SUB-PROCESSORS.md operator-action #5.
- **Brief 18 V2: heartbeat dashboard shipped** `phase-C` `briefs` `observability` `shipped` — Sentinel `/heartbeats` page surfacing the existing `sentry_heartbeat_pings` table. Per-app rollup pills (healthy / unverified / missing / ping_failed), 14-day window stats, recent pings table. Empty state documents Brief 18 activation steps. PRs #85/#86/#87 merged dev → staging → main on Sentinel. 280/280 tests pass.
- **Phase B+ #3: audit logging wired in** `phase-B+` `security` `shipped` — KC PRs #143/#144/#145 merged dev → staging → main. New `server/middleware/audit.ts` with `auditAdminMutation` middleware (appended to adminGuard) records every mutating admin call to `audit_logs`. `auditImpersonation()` fires inline from `isAuthenticated` when the impersonation header is present on a mutating request. New `GET /api/admin/audit-logs` admin route lists the most recent 200 entries. Privacy Act 2020 IPP 6 traceability is now real, not aspirational. Body content is NEVER logged (path + method + metadata only).
- **Phase B3-code partial: export-user-data.ts shipped** `phase-B` `privacy` `code` `shipped` — KC PRs #146/#147/#148 merged dev → staging → main. Read-only IPP 6 access script in `keycontent-app/script/`. `npx tsx script/export-user-data.ts (--uid <auth-uid> | --email <email>) [--output <path>]`. Walks 16 tables to assemble a JSON dump (parameter-bound SQL for the non-Drizzle `bug_reports` table). The matching `delete-user-data.ts` write-path script remains deferred pending operator cascade review.
- **Brief 14: Sentinel /security advisor surface shipped** `phase-C` `briefs` `security` `shipped` — **Closes the highest-leverage piece of the Sentinel-UI-gap.** Sentinel PRs #88/#89/#90 merged dev → staging → main. New `/security` page with per-app rollup cards + active findings list + recently-resolved tail. Daily 5am NZ cron walks every `maintained_apps` row that has a `supabase_project_ref` set, calls Supabase Management API `/v1/projects/{ref}/advisors`, upserts into a new `security_findings` table (RLS deny + service_role grant + active/resolved/first-seen/last-seen semantics). Migration applied to Sentinel prod via `execute_sql` + stamped (`20260603040000`). KC's `supabase_project_ref = ayipiwqupgtaiirmmjjr` pre-populated. **Operator activation Tuesday:** set `SUPABASE_ACCESS_TOKEN` in Sentinel Railway prod + staging.
- **Phase B+ #7: account-takeover detection (basic) shipped** `phase-B+` `security` `shipped` — KC PRs #149/#150/#151 merged dev → staging → main. New `login_fingerprints` table (RLS deny + service_role grant). `server/lib/login-fingerprint.ts` SHA-256-hashes the User-Agent, atomic upsert returning isNew via Postgres `xmax` pseudo-column, sends a Postmark "new device sign-in" email + writes an `auth:new_device_login` audit row on first-seen fingerprints. Hook point: `POST /api/auth/sync` (called right after Supabase sign-in succeeds). Best-effort — failures never block sign-in. Migration applied to KC prod via `execute_sql` + stamped (`20260603050000`).
- **Phase B5: req.scoped() helpers (T3 closure foundation) shipped** `phase-B5` `security` `shipped` — KC PRs #152/#153/#154 merged dev → staging → main. New `server/middleware/scoped.ts`: `requireAgencyContext` middleware (401 if no user, 403 if no agency) + `scopedAgencyId(req)` helper (returns agency_id or throws `MissingAgencyScopeError`). 7 new unit tests; full suite 49/49. HOW-WE-DO-SECURITY.md gains a "Multi-tenant scope enforcement" section documenting the migration pattern + the `req.user?.agencyId` → `scopedAgencyId(req)` swap rationale (throw beats silent undefined). **Phase B5b — actual route-by-route migration — is the follow-up sweep on Backlog.**
- **Sentinel /audit-events page shipped** `phase-C` `briefs` `observability` `shipped` — Sentinel PRs #91/#92/#93 merged dev → staging → main. Surfaces KC's `audit_logs` + `login_fingerprints` via service-role. 4 count cards (admin mutations / impersonations / new-device sign-ins / distinct fingerprints in 14d window), recent audit events table, login fingerprints inventory. Closes the DB-only gap from B+ #3 + B+ #7. Navigation: Sentinel → Audit events.
- **Sentinel /retention page shipped** `phase-C` `briefs` `observability` `shipped` — Sentinel PRs #94/#95/#96 merged dev → staging → main. Informational view of retention policy + live counts of rows currently eligible for the next sweep (best-effort live query via KC service-role). Copy-to-clipboard buttons for the railway run sweep commands. Surfaces the B3 retention sweep scripts in the Sentinel UI.
- **KC /privacy page (draft v1) shipped** `phase-B` `privacy` `shipped` — KC PRs #155/#156/#157 merged dev → staging → main. Publicly accessible at /privacy (mirrors the /review and /invite token-route pattern — checked early in the router before the auth state machine). Content matches the SUB-PROCESSORS-aware draft 1:1 — 12 sections, NZ Privacy Act 2020 framing only, post-Kling sub-processor list (10 vendors), IPP 6/7/9 rights, OPC contact. Flagged DRAFT in the page eyebrow until the 6 vendor verifications close.
- **Monday digest — privacy + security extension shipped** `phase-C` `briefs` `observability` `shipped` — Sentinel PRs #97/#98/#99 merged dev → staging → main. Existing 10am NZ Monday maintenance digest now includes a "Privacy & Security" section: active security_findings counts + newly-resolved this week, heartbeat health across all apps, KC audit-trail counts (admin mutations / impersonations / new-device sign-ins). Subject line promotes ERROR-level findings ("Sentinel digest — 2 security ERRORs + 5 maintenance"). Best-effort — section drops silently if any underlying fetch fails. **The operator now reads ONE email a week to see Brief 14 + Brief 18 + B+ #3 + B+ #7 signals roll up.**
- **Phase B2: retention policy** `phase-B` `privacy` `docs` `shipped` — `Sentinel/playbook/HOW-WE-DO-RETENTION.md`. Per-class retention rules. Privacy Act 2020 IPP 9 aligned.
- **Phase B3: deletion path doc** `phase-B` `privacy` `docs` `shipped` — `Sentinel/playbook/HOW-WE-DO-DATA-DELETION.md`. Three scenarios (IPP 7 request 30d SLA · agency Type-3 closure 60d Export Window · retention expiry). Cascade map for every KC table. Four scripts scoped.
- **Phase B4: breach response plan** `phase-B` `security` `docs` `shipped` — `Sentinel/playbook/HOW-WE-DO-BREACH-RESPONSE.md`. Privacy Act 2020 Part 6 aligned: detect → contain → assess → notify. OPC + affected-individual notification templates. Three rehearsed scenarios from real KC incidents.
- **Phase B5: threat models (KC + Sentinel)** `phase-B` `security` `docs` `shipped` — `Sentinel/playbook/threat-models/keycontent.md` + `sentinel.md`. Top 8 threats each, ranked, mitigations + residual risk. KC top: **T3 cross-agency data leak via missing scope filter**. Sentinel top: **T1 Sentinel-as-cross-app trust-anchor**.
- **Phase B8b: KC privacy notice (draft v1)** `phase-B` `privacy` `docs` `shipped` — `Sentinel/playbook/drafts/PRIVACY-NOTICE-KC-DRAFT.md`. 12 sections, NZ Privacy Act 2020 framing only, all 18 sub-processors disclosed. Blocked on Kling decision + 7 vendor verifications + `/privacy` route + footer link + signup consent.
- **Phase A4 follow-up: schema_migrations drift cleanup** `infra` `audit-followup` `shipped` — Backfilled 2 missing rows on Sentinel prod `supabase_migrations.schema_migrations`. Future `supabase db push` won't trip.
- **Brief 18 — Sentry heartbeat shipped to prod** `phase-C` `briefs` `observability` `shipped` — Endpoints on both apps + Sentinel scheduler + `sentry_heartbeat_pings` table on Sentinel prod. Safe-by-default until SENTRY_HEARTBEAT_TOKEN is set in Railway. Closes the 2026-05-22 Sentry env-var drift incident class. **Operator activation Tuesday.**
- **Phase B3 retention sweep scripts shipped to prod** `phase-B` `privacy` `code` `shipped` — `script/retention-sweep-{daily,monthly}.ts` in keycontent-app. Dry-run by default; `--confirm` to delete. TTL: review_tokens, team_invitations, social_connect_tokens. Anonymise: bug_reports past 24m. Delete: webhook_events + audit_logs past 6m. **Operator activation: dry-run on staging, then confirm-run, then schedule.**
- **Phase B9 — OWASP ZAP DAST CI shipped to prod** `phase-B` `security` `pen-testing` `shipped` — `.github/workflows/zap-baseline.yml` on KC + Sentinel. Weekly Sunday 11pm UTC + `workflow_dispatch`. V1 informational only; findings post to rolling GitHub Issue. **First real pen-testing motion for Coreshift.** First scheduled run next Sunday.
- **Meeting brief** `phase-B` `ops` `docs` `shipped` — `Sentinel/playbook/PRIVACY-SECURITY-MEETING-BRIEF.md` — operator-ready single doc for team review. TL;DR + before/after + 9-item risk register + state-table + decisions needed + stock answers to common questions.
- **Phase B+ #6: gitleaks secret scanning CI** `phase-B+` `security` `code` `shipped` — Workflows live on both repos via dev → staging → main (KC PRs #126/#127/#128, Sentinel #74/#75/#76). Scans PRs (diff only) + pushes (new commits only) + manual workflow_dispatch for full-history. Uses the upstream binary directly so we avoid the gitleaks-action v2 org licensing requirement. Closes the 2026-05-19 service-role-leak incident class.
- **Phase B+ #5: Dependabot + npm audit** `phase-B+` `security` `code` `shipped` — `.github/dependabot.yml` weekly grouped updates (npm + GitHub Actions) Monday 9am NZ on both repos. `npm audit` workflow gates on high+ for Sentinel (passes); ships informational-only for KC (continue-on-error) until Dependabot files the cleanup PRs for pre-existing high-severity advisories. Closes threat T4 supply-chain blind spot.
- **Phase B+ #4: per-route rate limiting on KC** `phase-B+` `security` `code` `shipped` — KC PRs #132/#133/#134 promoted dev → staging → main. New `server/middleware/rate-limit.ts` adds Layer 2 (per-user, RFC-9239 headers) on top of existing per-IP global limiter in services/security.ts: `strictAuthLimiter` (5/min IP), `heavyAiLimiter` (20/min user — applied to POST /api/jobs + :id/generate + :id/regenerate), `bugWidgetLimiter` (5/hr user — ready for widget rewrite), `standardApiLimiter` (60/min user). Memory store (KC is single-instance Railway today). 42/42 tests pass.
- **Phase B+ #2: HOW-WE-DO-DR.md** `phase-B+` `security` `docs` `shipped` — `Sentinel/playbook/HOW-WE-DO-DR.md`. Seven scenario runbooks (Supabase, Railway, Cloudflare, DNS, Postmark, operator-account-compromise, catastrophic data loss). RTO/RPO per service tier. Honest about gaps: no practiced playbook, no automated failover, no multi-region anything.
- **Phase B+ #1a: HOW-WE-DO-BACKUP-RESTORE.md (tabletop)** `phase-B+` `security` `docs` `shipped` — `Sentinel/playbook/HOW-WE-DO-BACKUP-RESTORE.md`. 60-minute tabletop walkthrough + supervised staging-drill procedure + paste-into-incident-channel checklist. Surfaces that we have backups but have never restored, and that Storage has no backup until Brief 19 ships.

## 🟡 In Progress

- **DA → KC security parity** `above-standard` `tier-1` `security` `multi-app` — **Tier 1 workstream #2 — in progress.** Survey done 2026-06-16: DA lacked security headers, DAST/dependency/secret-scanning CI, audit logging, ATO/new-device detection, and a threat-model doc. (Auth is a thoughtful dual-realm design — GP Google-SSO + client JWT-cookie; **RLS-deny-all IS live on DA prod tables** as a DB backstop KC lacks, though it's not in migrations — drift to reconcile.) **✅ Shipped: baseline security headers** (HSTS · X-Frame-Options SAMEORIGIN · nosniff · Referrer-Policy · Permissions-Policy · COOP, via `next.config` `headers()`) to DA dev+staging ([#60](https://github.com/CoreshiftHQNZ/digital-architect/pull/60)→[#61](https://github.com/CoreshiftHQNZ/digital-architect/pull/61)); enforcing **CSP deferred** (needs nonce+allowlist, report-only validation against the live Next.js UI first). **Remaining:** ZAP DAST + Dependabot + gitleaks + npm-audit CI (overlaps Tier-1 #4 — DA is the first fleet app); audit logging (B+ #3 pattern, adapted to Next.js route handlers); ATO/new-device detection (note: client realm shares one password in v1 — different shape); DA threat-model doc; enforcing CSP. **✅ Survey flag CLEARED (2026-06-16):** `apps/web/app/api/integrations/callback` is **not** a vuln — the OAuth `state` is a signed JWT (HS256 over `CLIENT_SESSION_SECRET`, 15-min expiry) with `projectId` set server-side; `verifyIntegrationsState` checks the signature, so the `projectId` can't be forged. GP realm is all-access internal staff (no client-to-client boundary). Confirmed by reading `lib/google/oauth.ts`. **Release note:** DA `main` is ~100 commits behind dev/staging — prod promotion is the DA team's batched release, so security work lands in dev+staging and reaches prod on their cadence.

## 🚫 Blocked

- **Phase B8b: end-user privacy notice (publish)** `phase-B` `privacy` `blocked-by:verifications` — Draft v1 ready (see Done). ~~Kling block resolved 2026-05-30 (Path A).~~ Remaining blockers: (1) 6 vendor verifications (Postmark TTL, Sentry region, OpenAI no-train opt-out, Gemini tier, Zernio DPA, repo-private call); (2) `/privacy` route + footer link + signup consent checkbox in KC app.
- **Annexure 2 fill on Type 3 agreement** `phase-B` `privacy` `blocked-by:verifications` — SUB-PROCESSORS.md is ready to copy into Annexure 2 once DPA confirmations close. Kling line removed from the source doc 2026-05-30; Annexure 2 will be simpler as a result.

## 🔵 This Week

- **Tighten KC npm audit threshold to `high`** `phase-B+` `security` `ops` `low-priority` — After Dependabot files the cleanup PRs for the pre-existing high-severity advisories (next Monday 9am NZ cycle), flip `keycontent-app/.github/workflows/npm-audit.yml`: remove `continue-on-error: true` so the workflow gates rather than informs. ~2 min change.
- **Phase B+ #1a: run the backup-restore tabletop** `phase-B+` `security` `ops` `above-standard` `tier-1` — **Tier 1 workstream #3 (part 2 of 3).** `Sentinel/playbook/HOW-WE-DO-BACKUP-RESTORE.md` is ready. Operator + Claude run the 60-min tabletop together (no destructive ops). Output: friction points captured + plan for the supervised staging drill.
- **Brief 18 activation — operator adds env vars** `phase-C` `ops` — Set `SENTRY_HEARTBEAT_TOKEN` (any opaque string, same in both apps) in Railway prod + staging for KC and Sentinel. Optional: `SENTRY_AUTH_TOKEN` + `SENTRY_ORG` in Sentinel only to enable Sentry-API verification of pings. ~5 min total.
- **Brief 14 activation — operator adds Supabase access token** `phase-C` `ops` — Set `SUPABASE_ACCESS_TOKEN` (personal access token from supabase.com/dashboard/account/tokens) in Sentinel Railway prod + staging. Then trigger the daily 5am NZ cron OR call `runDailyScan()` from a one-shot script. KC's `supabase_project_ref` already pre-populated. ~2 min.
- **B3 retention sweeps — first dry-run on staging** `phase-B` `ops` — `railway run --service keycontent-app -e staging -- npx tsx script/retention-sweep-daily.ts` (and `monthly`). Verify counts. Then `--confirm` on staging. Then first prod run. ~15 min.
- **B9 ZAP — manual first run** `phase-B` `ops` — Trigger `zap-baseline.yml` once via `workflow_dispatch` on KC and Sentinel to get the first baseline report before next Sunday's auto-run. ~2 min.
- **7 vendor verifications** `phase-B` `ops` — Postmark plan TTL · Sentry org region · OpenAI no-train opt-out · Gemini paid-tier · Zernio DPA + jurisdiction + retention · `keycontent-app` repo-private decision · `hello@coreshifthq.com` address confirm. From SUB-PROCESSORS.md action items.

## ⚪ Backlog

- **Object-level storage authz on `files.ts` (IDOR)** `security` `code` `follow-up:b5b` — Surfaced during the B5b sweep (2026-06-16). `GET /api/files/signed-url?path=…` and `GET /objects/*` are auth-gated but serve **any** Supabase Storage path to **any** authenticated user — there's no check that the path belongs to the caller's agency. Documented as accepted-risk "audit C1" (relies on id-keyed unguessable paths), but it's a real object-level IDOR, distinct from B5b's agency query-scope filters. Fix: map each storage path prefix (`jobs/`, `clients/`, `products/<agency>/`, `uploads/<user>/`, `compose-media/<agency>/`…) to its owning agency and authorize before signing/streaming. Moderate effort (path→owner resolution). Not a leak of agency *rows*, but of stored *assets* if a path is known.
- **Fleet-wide DAST / dependency / secret scanning** `above-standard` `tier-1` `security` `multi-app` `code` — **Tier 1 workstream #4.** ZAP (B9), Dependabot + npm audit (B+ #5), and gitleaks (B+ #6) run on KC + Sentinel only today. Extend the same three CI motions to every maintained app — DA first, then the rest of the fleet as each onboards. One standard, applied everywhere. Reuses the existing workflow YAML as a template.
- **MFA on operator + admin accounts** `above-standard` `tier-2` `security` `ops` — **Tier 2 (operator action).** Enforce MFA on GitHub org, Supabase, Railway, Cloudflare, and Google Workspace operator/admin accounts. Cheap, high-leverage — closes the "operator-account-compromise" DR scenario (HOW-WE-DO-DR.md) at the source. Per-service enrolment by the operator.
- **Phase B3-code: deletion + access scripts** `phase-B` `privacy` `code` `~4d` — Implementation of B3 doc. Four scripts in `keycontent-app/script/`: `export-user-data.ts` (IPP 6 access), `delete-user-data.ts` (IPP 7 cascade + anonymise), `close-account.ts` (Type 3 clause 11/12), monthly aggregation step for `ai_call_logs`. **Needs operator review of cascade semantics before code lands.** Unblocks the 30-day deletion SLA promise.
- **Phase B+ #7 V2: surface login fingerprints in Sentinel** `phase-C` `briefs` `security` `low-priority` — Today login fingerprints are visible only via DB query / Postmark inbox. Add a `/auth-events` page in Sentinel reading from KC's `login_fingerprints` (via service-role) plus the `auth:new_device_login` rows from `audit_logs`. Glanceable "is anyone signing in from unexpected places" surface.
- **Phase B+ #8: privacy-contact channel SLA-backed** `phase-B+` `privacy` `ops` `~30 min operator` — Draft privacy notice promises `hello@coreshifthq.com` + 5-business-day acknowledgement. Operator confirms address, sets Gmail filters routing to priority + auto-acknowledgement. Templates ready when Brief request comes.
- **Phase B+ #1b: backup-restore drill on staging** `phase-B+` `security` `ops` `supervised` `above-standard` `tier-1` — **Tier 1 workstream #3 (part 3 of 3 — proves recoverability).** After the tabletop closes (item in This Week), schedule the operator-supervised destructive drill on staging Supabase. Drop a low-risk table, restore from backup, time each step. Worst-case findings update HOW-WE-DO-BACKUP-RESTORE.md.
- **Brief 15 — Data inventory surface in Sentinel** `phase-C` `briefs` `privacy` — `/data-inventory` sub-page reading B1 from a Sentinel-owned table. Drift detection (new public table → alert).
- **Brief 16 — Pen-test cadence tracking** `phase-C` `briefs` `security` — Stores last pen test / next due / scope / report link per app in `maintenance_entries`. Surfaces in digest when overdue. Same shape as Brief 10 lifecycle reference.
- **Brief 17 — Breach response runbook surfacing** `phase-C` `briefs` `security` — `/breach-response` sub-page; clickable B4 card with "Start incident" button that scaffolds incident folder + notification draft templates.
- **Brief 19 — KC `job-assets` daily R2 backup** `phase-C` `briefs` `infra` `above-standard` `tier-1` — **Tier 1 workstream #3 (part 1 of 3 — Storage backup).** Sentinel-owned node-cron: list Supabase Storage `job-assets`, diff against R2 bucket, copy new/changed objects, prune versions per retention policy. Companion CLI `script/restore-from-r2.ts`. R2 export decision locked.
- **Sentry alert "heartbeat" detection follow-up** `phase-C` `low-priority` — V2 of Brief 18: if Sentinel-side verify pass reports `missing`, fold into Monday digest as a P1 alert. Today operator inspects pings manually.
- **Phase D triggers** `phase-D` `future` — Compliance escalations. Each is trigger-gated, not calendar: NZ Privacy Act formal review · GDPR posture for EU customers · SOC 2 Type I / II · annual third-party pen test · automated DAST in CI · cyber-insurance · bug-bounty. **Note:** the Above-Industry-Standard Initiative (2026-06-16) recommends pulling **annual pen test** + **SOC 2 Type I readiness** forward as `tier-2` items (see SECURITY-PRIVACY-PLAN.md) rather than waiting for a hard trigger — "above standard" is the goal, not "compliant once forced." Still operator-gated on budget/horizon.
