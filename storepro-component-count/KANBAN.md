# Storepro Component Count — Kanban

> Visual rollout state. Edit this file and run `node tools/build.js storepro-component-count` from the coreshift-kanbans repo root to refresh.
>
> Card format: `- **Title** \`tag\` \`tag\` — Description.`
>
> The column headings below are what the renderer looks for. Keep the leading emoji — it's how the engine assigns colours.
>
> **🎯 Current focus (M7): reconcile the Cottonsoft BOM against Shivneel's confirmed count.** Engine work DONE 2026-07-31. **Every frame line now matches Storepro exactly** (80 total; 52 × 4877x900, 12 × 7013x900, 16 × 4267x900), along with bays 63, beams 352/20 and mesh decks 398. Two lines remain open as written questions the drawing genuinely can't settle: the 2700x80 TW-vs-MW spec, and mesh backs (36) which aren't drawn in the file at all. **M7 closes when Ricky sends the drafted reply.**

---

## 👉 On Ricky

- **Send the Cottonsoft reconciliation reply to Shivneel** `M7` `client-facing` `DRAFT READY (rev 2)` — **Ready to send: `Storepro_Reply_to_Shiv_Cottonsoft_Reconciliation.md`** in the project working dir. Rev 2 is shorter because Ricky answered two of the questions himself. It reports that EVERY frame line now matches (80 total, 52/12/16) plus bays, beams and mesh decks, and asks only what's genuinely unanswerable: (1) which drawing his screenshots are from — the blocks he selected aren't placed in the Cottonsoft DXF and the coordinates don't match it; (2) mesh backs 36, which aren't drawn at all — with the hypothesis that they ARE the 36 × 2700x80 beams; (3) 2700x80 TW-vs-MW; plus 7010-vs-7013 and whether the frame block's height list is the official stock list. **This send is the last thing standing between M7 and done.**

- **Rotate the Anthropic API key — AND the Azure AD client secret** `security` `ops` `MORE URGENT NOW` — The Anthropic key was pasted in chat in May and is still live in Railway staging. **It was re-exposed on 2026-07-31**: a `railway variables` call meant to list only variable NAMES printed full values into the session transcript, so `ANTHROPIC_API_KEY` *and* `AZURE_AD_CLIENT_SECRET` are both now in a second transcript. Rotate both. The Anthropic key needs a Console login; the Azure secret needs the app registration (and Zoltan may need to know). Hand Claude the replacements and it will set them via the Railway CLI and confirm the old ones are dead. Lesson recorded: never dump `railway variables` unredacted — query one name at a time.
- ~~**Reconnect the Railway GitHub source**~~ `infra` `DONE 2026-07-31` — No longer needed. `railway status` now shows `repo: CoreshiftHQNZ/storepro-component-count` on the API service, and the M7 push to `staging` triggered an auto-deploy without any `railway up`. Auto-deploy is working again; nothing on Ricky here.
- **Ping Zoltan with the confirmed SSO setup** `ops` — Loop back to Marksmen IT now that all three Azure AD env vars are wired and verified against Microsoft. Confirms his side is complete and unlocks production redirect-URI registration when M8 starts.
- **Chase the promised new drawing set** `M7` `client-facing` — Shiv: "I will do another follow-up with a new drawing set soon." A third real job is what turns the engine from two-drawings-validated into something generalisable.

## ✅ Done

- **DWG parse prototype against Sigma** `phase-0` `shipped` — Validated that Storepro DWGs are structured enough to count from. Named blocks + StorePro_ layer convention produce clean counts. Direction confirmed → build the app.
- **component_library.yaml + rules.yaml** `phase-0` `shipped` — Storepro-editable config: block-name aliases, layer-substring filters, deterministic counting rules. Iterated through 3 sample drawings (Sigma, Arlec, Suntory) — added S-type frame markers, BBSL beam variants, Chinese-named blocks (offshore designer), architect-XREF ignore patterns.
- **Cross-job comparison study** `phase-0` `analysis` `shipped` — Parsed all 3 sample drawings end-to-end, generated matrix outputs, diffed against the golden Sigma Phase 1 Stage 2 spreadsheet. Found the bay-count → frame-count rule (frames = bays + runs, NOT 2 × bays) and the per-level beam profile variation. Locked these with Gino + Gareth on 2026-05-28.
- **Storepro rules confirmed (Gino + Gareth meeting)** `phase-1` `shipped` — Locked: frames = bays + runs; splice kit threshold = 11,400 mm; back ties = 2 per frame default; frame-end protectors = 1 LH + 1 RH per run; beam profile varies per level (no fixed pattern); copy-paste anomaly when DWG > 1.5× rule expectation flags clarification; post-protector spec asked per job.
- **FastAPI backend with libredwg + vision pipeline** `phase-1` `shipped` — Streaming 500 MB uploads, DWG → JSON via libredwg, vision pass via Claude (component_count_vision reads per-level beam profile + run_count), recipe extractor, AI reviewer, job storage. Dockerfile builds libredwg from source; runs on Railway.
- **Reconciler + matrix builder** `phase-1` `shipped` — Combines DWG-block parse + vision recipe into a unified bay-type × component matrix. Applies the meeting rules. Emits clarifications (post-protector spec, copy-paste anomaly). Confidence flag per cell.
- **High-fidelity UI ported** `phase-1` `shipped` — Designer's 6-screen prototype (Storepro.zip) ported to Next.js: Dashboard → Upload → Processing → Review → Clarify → Export. IBM Plex Sans/Mono + industrial-yellow + paper-white palette. CAD-scanner Processing animation. Live job-list filter strip + tabs.
- **Real upload → matrix → export flow on staging** `phase-1` `shipped` — Suntory smoke test: 32 MB DWG + 2 MB PDF uploaded, pipeline completed in 144 s, 350 bays detected (matches capacity table exactly), 367 frames vs golden 384 (95.6% accuracy — was previously 2× over with the old rule).
- **Microsoft Azure AD SSO** `phase-1` `auth` `shipped` — Sign-in with Microsoft button, OAuth code flow via `/api/auth/microsoft/exchange`, opaque session token in localStorage, server-side revoke on logout. HTTP Basic kept behind toggle for CLI. Tenant + client + secret all verified against Microsoft directly.
- **Excel + PDF export** `phase-1` `shipped` — `.xlsx` is a 3-sheet workbook (Matrix, Assumptions & Questions, Rules & Clarifications) via openpyxl. `.pdf` is A4 landscape print-ready via reportlab (pure Python, no native deps). Both download from real endpoints on the Review screen.
- **Job metadata + notes persistence** `phase-1` `shipped` — Client / Site / Reference / Racking-system captured on upload, stored with the job, shown on Dashboard rows + Review header. Notes posted via `POST /api/jobs/{id}/notes` with auth-derived author + timestamp; surfaced in Review right-rail activity feed.
- **Dashboard search filter** `phase-1` `shipped` — Top-bar search filters jobs by ID / client / site in real time.
- **Sample drawings comparison artifacts** `phase-0` `analysis` `shipped` — Parsed JSON + matrix outputs + golden spreadsheets retained under `samples/` (gitignored — multi-GB) for future regression checks once Storepro standardised drawings land.
- **Exit prototype mode — real engine wired to the clarifications screen** `phase-2` `merged-to-dev` `PR #1 + #2` — Reconciler now runs in the job pipeline and its clarifications reach the Review screen (was: dead compliance flags saying "not implemented in this prototype"); silent-zero splice/mesh/protector counts replaced with manual-confirm clarifications; stale prototype wording stripped; empty-export bug fixed (bay_counts was never persisted); SP_ standardised library mapped (95.6% block coverage, longest-match matcher). Merged to dev 2026-07-02 — staging promotion still pending at the time.
- **Phase B — validated against a Storepro-confirmed count (OPPAK-12515)** `phase-2` `shipped` `ground-truth` `tests/test_oppak_oracle.py` — Storepro supplied OPPAK-12515 (Glowbal NZ) with their confirmed component count — locked as a regression oracle (fixture + identity tests). Every future engine change is now diffed against a known-good job automatically. Also exposed two drawing quirks the system must flag: capacity-table Total 86 ≠ 132, and FSL6629 code on a 6477mm frame.
- **Dynamic-block topology engine — full BOM parity, line-for-line** `phase-2` `shipped` `parser/topology_spike.py` `feature/plan-topology` — Decoded Storepro's real drawing format (dynamic blocks: *U anonymous inserts → source blocks via eed; bays = arrayed strips; letters nested per bay-unit; spec via visibility states) and built a deterministic engine that reproduced OPPAK-12515's confirmed count exactly: bays 83/46/3, frames 93/54 (shared-frame max-height rule), beams 498/18/368 from elevation reading, back ties 112 (aligned-pairs rule), anchors 588, protectors 11 LH + 11 RH (by rotation), load signs 3. Component-space palette auto-excluded by bay-pitch signature. No vision needed. 4 hypotheses surfaced as clarifications, end-to-end test env-gated on the job minJSON.
- **CI green again — vision deps were never installed** `infra` `shipped` — Every CI run since 2026-05-20 failed at import cv2: the parser job never installed parser/vision/requirements.txt (Docker did). One workflow fix later, all three jobs pass — first fully green build in 6 weeks — and the newer test files (SP library mapping, reconciler clarifications, OPPAK oracle) now run in CI.
- **bom() graduated into the upload pipeline** `phase-2` `merged-to-dev` `PR #3` — count_standardised() runs at job finalise: job.bom + topology_summary on JobResult, TOP-clarifications merged into the Review-screen wiring, export.xlsx gains a "BOM (part-coded)" first sheet. Legacy drawings → applicable=false, existing engines stand alone. Merged to dev 2026-07-03, CI green.
- **Staging promoted + deployed — full stack live** `infra` `shipped` — staging fast-forwarded to dev (all 3 PRs: exit-prototype-mode, SP_ library, topology engine + BOM pipeline). CI green on the branch. Railway auto-deploy found dead (repo disconnected — see the On Ricky item), so both services deployed via railway up from the staging checkout: API healthy (/api/health OK, SSO enabled), web serving.
- **Clarification actions + cutout automation + export fix (PR #4)** `phase-2` `on-staging` `PR #4` — Post-protector cutout rule AUTOMATED (protector height vs lowest beam level from elevations). AI review now sees the topology result (no more "4541 unclassified *U" scare) and proposes actionable include/exclude/adjust options per comment. Clarification answers + free-text now PERSIST (POST /clarifications → feedback corpus for refining rules). Exports front the part-coded BOM (PDF was showing only the legacy vision-matrix → looked wrong though the count was right). Merged + on staging 2026-07-04.
- **Storepro's rule answers encoded (PR #5)** `phase-2` `merged-to-dev` `PR #5` — Shivneel replied 2026-07-05. Encoded as corrected clarification wording (OPPAK count unchanged): splice kits de-automated (1/post, load-driven, 2 variants — manual flag); load signs → per-aisle estimate not per-bay-type; weld reads from block name (OPPAK unmarked → MW confirmed); Bay B custom-height cut-to-size documented (FSL6629→6477); back-tie Gino-vs-count contradiction flagged. 30 tests pass. Merged to dev 2026-07-13.
- **Engine generalised off OPPAK-specific hardcodes** `phase-2` `on-staging` `parser/topology_spike.py` — The BOM is the deliverable (Storepro never provides one) — so the engine must work on unseen drawings. Removed 6 OPPAK-baked assumptions, all now DERIVED: bay pitch (any consistent arrayed strip), bay letters (regex discovery, not A/B), taller-at-shared-frame (from elevation heights not hardcoded 'B'), half-bays (pattern-matched + width→type), BOM groups (by frame height). OPPAK still reproduces line-for-line; new synthetic test proves it on C/D/E bays at non-OPPAK pitches.
- **Storepro questions email sent** `ops` `done` — Sent 2026-07-04: OPPAK line-for-line proof + part-code table request + 7 rule questions (draft in Storepro_Questions_Email_Draft.md). Answered by Shivneel 2026-07-05 and encoded in PR #5.
- **Second real job (Cottonsoft) — library over-count diagnosed** `phase-2` `analysis` `shipped` — Joanna's first drawing on the new library (types A/A1/A2/A3/B/B2/C, table=48 bays). Engine PARSED the new format cleanly (new letters/heights/beam) but counted ~144 vs 48: it was counting the embedded STOREPRO LIBRARY plus duplicated plan/elevation regions (the pitch-signature filter fails — this library has real arrayed strips), and didn't split A2/A3/B2. Diagnosis drove the count-box + clean-slate rebuild below.
- **One engine — capacity table authoritative, DWG as gospel** `phase-2` `shipped` — Killed the two-engines ambiguity: the capacity table is the authoritative bay count and geometry is gated on agreeing with it. Mesh decks counted from placed blocks rather than inferred; weld and cutout reported honestly (weld lives in the visibility state, not the block name — the named beam blocks are library templates). 2026-07-23.
- **Count box — isolate the job from the embedded library** `phase-2` `shipped` — Storepro's standardised drawings ship the component library inside the same model space as the job, near AND far. The labelled count box scopes geometry to the actual job before anything is counted, which is what the pitch-signature filter could never do on a library full of genuine arrayed strips.
- **Clean-slate dxf_count engine — count what's drawn** `phase-2` `shipped` `feature/dxf-count` — Rebuilt the counter on ezdxf around one principle: count what is drawn, drop the manual-count rules inherited from the spreadsheet era. View reconciliation (capacity-table spine + direct plan counts), beams from a per-bay-type elevation recipe, frame/baseplate resolution, and genuine-only structured clarifications (three real options, no filler). Merged to dev 2026-07-29, CI green.
- **New engine wired end-to-end + clean client-facing exports** `phase-2` `on-staging` — DXF uploads run the clean engine through the app; the web Review screen renders the new bom output instead of the legacy counts; real uploads never fall back to demo rows; xlsx + PDF exports rebuilt as a clean client-facing document. On staging 2026-07-30, CI green on all three pushes.
- **Frame heights fully reconciled — 52/12/16 exact** `phase-2` `shipped` `ground-truth` — Ricky's rule, confirmed 2026-07-31: a run-end frame takes the height of the bay it sits against (at a run end there's only ONE adjacent bay, so no taller-of-two to resolve). Type comes from the nearest visible plan bay label, whose elevation gives the height. All three frame lines now equal Storepro's count exactly — the split is DERIVED from the drawing, not asked about. The unlock was a one-line bug: a bay label is a dynamic block holding FOUR texts (B, B1, B2, B3) with one visible, and the walker collected all four because it never checked the text's own invisible flag. Reading only the visible one turns 252 ambiguous labels into **63 exact ones matching the capacity table row-for-row** — which also buys a genuinely new cross-check: bays are no longer taken on trust from the table. Also CORRECTED a wrong claim from earlier the same day: the 36 placed mesh blocks are DECKS, not backs — the backing-mesh blocks are unused library definitions, so the 36-vs-36 was coincidence. A regression test now guards that exact trap. 18/18 oracle against the real DXF, CI green on staging.

- **Layer-awareness — three of four Cottonsoft deltas closed in one fix** `phase-2` `shipped` `PR #6` `ground-truth` — All three fixable deltas had ONE root cause: `parse_spec()` read a block's NAME and threw away its LAYER, and Storepro's layers are as self-describing as their block names. (1) **Frames 63 → 80 and baseplates 143 → 126.** 17 blocks named `Plan_View_Baseplate_100_Dynamic` sit on layer `0`, not an `SP_` baseplate layer like the other 126; they're the run-END frames (a run of N bays needs N+1). One misread caused two wrong lines, and the remaining 126 are then exactly 2 per frame. (2) **Frame heights emitted for the first time** — they were always in the drawing as a dimension text on `SP_Bay_References` inside each elevation: 4267 / 4877 / 7013, Storepro's exact three, with `Frame 7013x900` landing on their 12. (3) **Mesh decks 36 → 398** — only 36 blocks are placed, but a decked bay carries one deck per pallet position per beam level; `Σ(bays × levels × width)` = 398 against their 398. Shipped at MED with the rule in the `basis` and a clarification against the drawn figure — a derivation, never a silent coefficient. Merged to dev + staging 2026-07-31, CI green.

- **Cottonsoft oracle locked before the engine moved** `phase-2` `shipped` `tests/test_cottonsoft_oracle.py` — Second ground-truth oracle, committed BEFORE any fix so the three already-matching lines couldn't drift. Three layers: fixture identities; a reconciliation lock that runs in CI off a committed extraction snapshot (the DXF is 37 MB and lives outside the repo); and a DXF-gated extraction lock. The two genuinely-open gaps are asserted AS gaps, so closing one fails the test on purpose rather than passing silently. 15/15 against the real DXF. Also wired `test_dxf_count.py` + `test_topology_generalization.py` into CI — the clean-slate engine's own guards had never run there.

- **M7 engine verified end-to-end on deployed staging** `phase-2` `verified` `job 24a05878` — Not just green tests: the real 37 MB `OPPAK Cottonsoft.dxf` was re-uploaded to the deployed staging API after the M7 merge and returned the new BOM live — Bays 63, **Frame 80**, Frame 7013x900 **12**, Frame 4877x900 48, Frame 4267x900 12, **Baseplate 126**, **Mesh deck 398**, Mesh (as drawn) 36, Beam 2700 MW 352, Beam 2700 TW 36, Beam 1350 MW 20, plus the four genuine clarifications (`bay-total`, `run-end-frames`, `frame-height-split`, `mesh-decks`). In at 03:21:00, complete 03:22:14 — **74 seconds**, no error. Also proved Railway auto-deploy is alive again: the staging push built and released with no `railway up`.

- **Cottonsoft run end-to-end through the staging UI** `phase-2` `verified` — Job 31ee6719 on staging: OPPAK Cottonsoft.dxf in at 02:42, complete at 02:43 (2026-07-30). BOM returned 63 bays (from the PALLET CAPACITY table A:10 A1:2 A2:10 A3:2 B:24 B2:6 C:9), 63 frames, 143 baseplates, 36 mesh, 352 + 36 + 20 beams, two genuine clarifications. Verified live against the staging API on 2026-07-31.

## 🟡 In Progress

- **M7 — reconcile Cottonsoft against Shivneel's confirmed count** `M7` `phase-2` `ground-truth` `PR #6 on staging` — **Engine work complete; waiting only on Ricky sending the reply.** Six BOM lines now equal Storepro's confirmed count (bays 63, Beam 2700 MW 352, Beam 1350 MW 20, frames 80, Frame 7013x900 12, mesh decks 398) and baseplates dropped to 126 — exactly 2 per frame. Two lines stay open as written questions because the DXF can't settle them, which is what the doneWhen allows. Oracle locked BEFORE any engine change; 15/15 against the real DXF, full CI 45 passed / 8 skipped, OPPAK engine untouched.

## 🚫 Blocked

- **NetSuite part-code catalogue + stock-size list** `phase-2` `blocked-on-storepro` — The 7 rule questions were ANSWERED (Shivneel, 2026-07-05) and encoded (PR #5). Remaining blocker is the one input geometry can't produce: Storepro's NetSuite codes (geometry → FSL/BBSL) + the STANDARD STOCK FRAME SIZES (custom heights order the nearest stock size + a cut-to-size line — that's why Bay B is FSL6629 cut to 6477). Storepro confirmed they'll send it; not on drawings yet. Two smaller open items in the reply: back-tie count reconciliation (Gino says 2/pair → 114 vs confirmed 112) and whether beams are always weld-marked in the block name.

## 🔵 This Week

- **Close the two remaining Cottonsoft lines once Shiv answers** `M7` `blocked-on-storepro` — The frame split is now CLOSED (see the Done card). What's left: (1) **Beam 2700x80 weld** — drawn as `Front_View_Beam_2700x80 TW Rebate`, his count says MW; quantity agrees at 36, so it's spec only. We report as drawn rather than overriding. (2) **Mesh backs (36)** — no backing mesh is drawn in the file, so it can't be counted. Strong hypothesis worth testing: the backs ARE the 36 × 2700x80 beams (component_library notes a 2700x80-with-integrated-mesh that counts as both a beam and a back panel), which would also explain the TW/MW disagreement. If so, both open lines close together.

- **Frame-height assumption to re-check on the third drawing** `phase-2` `assumption` — Each Cottonsoft elevation carries TWO dimensions (e.g. 4267 and 4606) and `frame_heights()` takes the **smaller** as the frame height, on the reasoning that the larger is overall/top-of-load. True on all five Cottonsoft elevations and it reproduces Storepro's three heights exactly — but it is a one-drawing-deep assumption, flagged in the code. First thing to test when Shiv's next drawing set lands.

- **Mesh connectors still not counted** `phase-2` — Not in the BOM at all, and Storepro's Cottonsoft count didn't include a connector line either so nothing flagged it. The backlog rule is 6 connectors when the beam is > 2743 mm else 4, and beam length is now read per level — so this is close to shippable.

- **PDF export BOM section + Review-screen BOM table** `phase-2` `polish` — The part-coded BOM ships in export.xlsx (first sheet) and its clarifications reach the Review screen; remaining polish is a BOM section in the PDF export and a proper BOM table in the Review UI (job.bom is already on JobResult + ApiJob).
- **Hide demo job rows once usage builds** `polish` `low-priority` — Dashboard falls back to sample rows when the API has few real jobs. Real uploads no longer fall back (fixed 2026-07-30); removing the demo fallback entirely is trivial once Storepro has ~10 real jobs running through.

## ⚪ Backlog

- **Geometric design-review checks (Phase C epic)** `phase-2` `epic` `design-review` — The senior-reviewer judgment layer. Was four checks; the topology engine (2026-07-03) already solved row spacers / back ties (aligned back-to-back pairs) and proved the extraction path for the rest. Remaining three, easiest-value-first: (1) Mesh connector 6-vs-4 — 6 connectors when beam > 2743 mm, else 4 (beam length now read per level from elevations); (2) Post-protector cutout variant — protector height vs lowest beam level (level heights now extracted; needs protector height from catalogue); (3) Double-deep backstop quantity — needs drive-through/double-deep detection in plan topology. Each independently shippable.
- **Production Railway environment** `phase-3` — Provision the production service for both api + web. Will need a separate Microsoft redirect URI registered with Zoltan (`https://storepro-component-count.up.railway.app/auth/callback`).
- **Retire HTTP Basic** `phase-3` `security` — Once SSO has been used in earnest for a few sessions and Storepro confirms it's stable, hide the "Use admin password instead" toggle on /login.
- **PDF letterhead + Storepro branding** `phase-3` `polish` — Right now the .pdf header uses a generic three-bar racking icon. Once Storepro shares an official logo (SVG or transparent PNG), swap it in to both the PDF header and the web TopBar.
- **Per-row confidence from the real reconciler** `phase-2` `low-priority` — Web adapter currently uses a heuristic (`confidenceFor()`) for the per-row confidence bar. The reconciler already produces real per-cell confidence — expose it via JobResult so the bar reflects actual cross-source agreement.
- **Email + share-link delivery on Export** `phase-3` `low-priority` — Buttons currently inert (download covers the workflow). Email path needs SMTP + recipient field; share-link needs a signed-URL endpoint with expiry. Adds when the operator team asks for it.
- **Pricing column** `phase-3` `blocked-on-storepro` — Hidden by default (showPricing=false). Needs Storepro's per-SKU price list before the column has anything real to show.
- **Auto-render DWG → PDF in the pipeline** `phase-2` `low-priority` — Today users supply a plan-view PDF (or export from Autodesk's free online viewer). The DWG already gives us the geometry — could render our own plan view via libredwg → SVG → PNG and feed vision directly. dwg2svg works but rsvg-convert / cairosvg currently fight back; needs revisiting on Linux (the Railway environment).
- **Storage backend for jobs** `phase-3` `infra` — JOBS dict is in-memory per API container. Survives until a deploy. Move to Supabase or Postgres once we want jobs to outlive redeploys.
- **Module 4 — BOM rollup (salesy quote)** `phase-4` `program` — Next module in the wider Storepro App program. Roll the per-bay matrix up into Storepro's verbose quote (Sigma ALL IN.xls shape). Specced in the build proposal — pick up when Phase 3 has bedded in.
- **Two more sample drawings** `phase-2` `low-priority` — Lets us test rule generalisation beyond the first two standardised files. Shiv has promised a new drawing set.
- **Quarterly review rhythm** `recurring` — Once the app is in real use, schedule a quarterly check-in with Gareth + Gino to revisit rules + sample new edge cases.

## 🅿️ Parking Lot

- **Retire the legacy vision + reconciler engines** `decision` `tech-debt` — The clean-slate dxf_count engine now handles DXF end-to-end, but the libredwg block parse, the Claude-vision recipe extractor and the reconciler all still run on every job (they're what produced the legacy counts block on the Cottonsoft staging job). Keeping them costs an Anthropic call and a pile of confusing output per job; deleting them drops the DWG path until dxf_count reads DWG too. Needs a call on whether Storepro will ever send a non-standardised drawing again.
- ~~**Local clone is 28 commits behind origin/dev**~~ `hygiene` `RESOLVED 2026-07-31` — Was already current at session start (`dev` == `origin/dev`). The real issue was different and is now fixed: local `dev` and `staging` had **no upstream tracking**, so a bare `git pull` failed and a `git checkout dev` silently produced a stale working tree. Both branches now track their origin counterparts. `dev` and `staging` were also reconciled (staging's 4 export/web commits merged into dev) so the two no longer diverge.

- **Retire the `_visible_leaves` name-only path** `tech-debt` `low-priority` — M7 added `_visible_placements()` (name + layer + world position) and left `_visible_leaves()` as a thin wrapper for the older callers. Once `elevation_recipes()` is moved across, the wrapper can go. Harmless as-is; two functions where one would do.
