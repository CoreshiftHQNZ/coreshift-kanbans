# Engine Optimization — Kanban

> Visual rollout state. Edit this file and run `node tools/build.js engine-optimization` from the coreshift-kanbans repo root to refresh the published board.
>
> **Card format:** `- **Title** \`tag\` \`tag\` — Description.`
>
> The column headings below are what the renderer looks for. Keep the leading emoji — it's how the engine assigns colors.

---

## 🟡 In Progress

- **M12: Handover to the team — a specialist who is not Ricky runs a full monthly cycle unaided** `M12` `phase-4` `milestone` — Brought forward **2026-08-19** ahead of M11, on Ricky's stated default, because M11 cannot open before 5 November and M12 is not locked to it. All three things that used to block this became controls in M10 the same day: a verdict can be recorded in the app and attributed, a prompt set can be signed off in the app, and all three frozen writes have a correction path. ⚠️ **It cannot start today either, and the handover that recommended it did not say so.** A full monthly cycle needs a month to run over — Storepro's cycles are Apr–Jul with 2026-07 `published`, there is no 2026-08 cycle, and August's Search Console figures are not final until **~5 September** on this system's own five-day convention (the same one that makes 2026-10 answerable on 2026-11-05). Re-running the published July cycle would be a rehearsal. **So the test runs the week of 2026-09-07** — two and a half weeks out instead of eleven, which is still the argument for bringing it forward, just not the argument as it was written. **The specialist is Mal** (`mal@growthpartners.co.nz`), who has already signed in and approved a plan, so *can a second human use this at all* is answered and *can they run a month* is not. **What the test is:** ingest August, audit, propose and **sign off a prompt set in the app** (the last M10 control never exercised on real data), plan, predict, record the shipping position, publish the report — every step by Mal, in the app. ⚠️ **The session running it must not help.** That is the one way a handover test gets laundered into a pass: Claude observes and records, every point where Mal had to ask anybody anything is a finding, and the doneWhen is not met if there were any. If a cycle turns out not to be runnable unaided, **that is the finding and not a failure** — it becomes cards and the test re-runs. ✅ **It feeds M11 rather than competing with it:** whatever a real specialist trips over in September is fixed before November's verdict rather than discovered on the day the answer is due. Nothing in it may write a verdict for `a3e5a8a7`, which stays refused by name until 2026-11-05.

- **The bounded prep list — four cards, and not a fifth** `M12` `method` — What the gap to 7 September is for. Four cards already written in **This Week** that a specialist running a cycle will actually touch: the **UTC shipping date** that reads a day behind in New Zealand (label it, do not shift it), the **plan built with no declared capacity** saying so on the plan itself, the **subdomain split** that shows one competitor twice in a client-facing number, and the **acceptance-criterion decidability check**. Fixing a *known, written-up* finding before a usability test is not hiding it — it stops the test spending its budget rediscovering what is already on the board. ⚠️ **Nothing else gets pre-fixed.** If it was not already a card on 2026-08-19, it is a finding for Mal to hit, and pre-empting it is how this becomes a polish milestone that answers nothing.

## 🚫 Blocked

- **M11: The readback — the verdict itself, and it cannot happen before 5 November** `M11` `phase-3` `milestone` `blocked-by-calendar` — 🔒 **Blocked on a calendar and on nothing else**, which is why it is here rather than in progress: every input it reads was built and exercised in M8 and M9, and no work on it can move. Prediction `a3e5a8a7` reads back over **October 2026**, Search Console figures for October are treated as final from **2026-11-05**, and the machinery refuses by name until then — not `too_early`, which would be honest and would still empty the `outcome is null` work queue. Three things are open when the date arrives. **(1) October has to be ingested first** — the arithmetic refuses a month with no complete snapshot rather than reading it as zero, so `npm run ingest -- --client storepro --month 2026-10` is a prerequisite and not a detail. **(2) The verdict is run for real**, once, frozen afterwards. **(3) The report has to carry the answer**, including when the answer is that we were wrong — a new publication check, its twin in the newest migration's required-check array, and a `REPORT_METHOD_VERSION` bump. 🔴 **The verdict may well be `confounded` rather than yes or no**, and that is a real outcome: the work is below the capacity line, and if it ships after 3 September October contains days from before the change. The system will say so out loud either way, which is the point.

## 👉 On Ricky

- **Book Mal for the week of 7 September — it is the only thing M12 is waiting on** `M12` `blocked-on-ricky` — Decided 2026-08-19: M12 comes forward ahead of M11, which was your stated default. Everything it needs is built and the date is set by Storepro's August Search Console figures becoming final around **5 September**, not by us. What is left is a real specialist's calendar time. **Mal** (`mal@growthpartners.co.nz`) is the candidate — he has already signed in and approved a plan, so he is the shortest path to the one test never run. Budget most of a day; it is a full cycle, not a walkthrough. ⚠️ **He must not be helped during it** — that is the whole measurement — so brief him before and debrief him after, and expect the debrief to produce cards. **Default if you say nothing:** the week of 7 September passes, October's cycle becomes the next opportunity, and M12 lands after M11 instead of before it, which is the outcome this decision existed to avoid.

- **Mal's cycle publishes a real report on a real client — and that is the point** `M12` `decision` — Flagging it rather than assuming it. M12 cannot run on the `app-check` fixture: a fixture cycle would prove the buttons work, which M10 already proved, and not that a month can be run. So Mal's cycle produces **real frozen writes against Storepro** — an approved plan, recorded predictions, a published report. ✅ **Lower stakes than it sounds:** nothing leaves the app. There is no PDF, no email and no export — that is still on the Backlog as an open positioning decision — so a report Mal publishes is published *in the app* and Storepro receives nothing automatically. What genuinely is real and frozen is the plan approval and any prediction recorded. **Your call if you want it otherwise**, and the only alternative is a weaker test.

- **Tell the team about 3 September — nothing else will** `M8` `M11` `decision` — Reframed 2026-08-19. The question is no longer *do we give the FAQ item the hours* — that is the team's call now, and the capacity line is coming off the plan. What does **not** become the team's call is the date, because it is not a scheduling preference: **3 September is the last day "Question content is marked up as such" can be recorded as shipped and still have the October readback window mean anything.** Ship on or before it and prediction `a3e5a8a7` gets a clean read; ship on the 4th and October contains days from before the change, so November's first verdict comes back `confounded` — no answer rather than a wrong one, at the cost of a full cycle. ⚠️ **Nothing chases it.** There is no scheduler in this system; the date is printed wherever the shipping record is read and that is the whole of the mechanism. It is 12 of the item's 147 target pages that the prediction actually covers, so a partial slice on those twelve is a legitimate way to make the window readable without the full item. **Default if you say nothing:** the work slips, and November's first verified prediction is inconclusive. 🆕 **2026-08-19 — the date now sits inside a live milestone.** M12's prep window runs from today to the week of 7 September, so 3 September falls inside it rather than in a column nobody is looking at. That is not a scheduler and it does not chase anything; it is simply the first time this date has been inside the current milestone instead of two milestones away.

- **Sign off Storepro's next derived prompt set in the app, not the terminal** `M10` `M12` — New, and small. Storepro's fifteen active prompts were all signed off in M6, so there is nothing there to press today — the gate was proved on the fixture instead. The next time `--from-gsc` proposes a set for a real client, sign it off in the AI-visibility panel rather than from a terminal: that is the path a specialist who is not you will take, and it is the last thing M12 needs exercised on real data rather than on a fixture. **Default if you say nothing:** the CLI still works and nothing breaks; M12 just discovers it instead.

- **A second client, when you're ready** `M12` — Not blocking anything, and parked here rather than dropped because it is the only thing that tests M6's four matching thresholds, all of which have exactly one property behind them. You said you'd add one later; this is the note that says why it matters more than it looks. **Default:** Storepro-only until you name one.

## ✅ Done

- **M10: The controls a specialist presses — a verdict, a prompt sign-off and a correction, and not one of them needs a terminal** `M10` `phase-3` `milestone` — Landed **2026-08-19**, the day it was inserted. Three gaps, all found by building M8 and M9, all closed against the deployed environment by Ricky pressing them himself. **(1) A verdict can now be recorded in the app.** It could not be before, and the reason was not squeamishness: `predictions` had no `verified_by`, so a verdict pressed in a panel could not have been attributed to anybody — and a permanent, client-facing, frozen claim with no name on it is worse than no control at all. The panel said exactly that for two milestones. One column (`0014`, required the moment an outcome exists and frozen with the rest of the verdict, so an anonymous verdict can never be given a name afterwards), one endpoint, one control. **The control writes what the judge decided, not what the specialist typed** — no outcome dropdown, no decision dropdown. The verdict on screen came from the matched control, the noise floor read off that control's own drift, the shipping record and the ledger; a form that let a person adjust it before saving would have made every verdict in the database an opinion with a method version on it. What the specialist records is that they have read it and stand behind it, which is precisely what `verified_by` means. **(2) A prompt set can be signed off in the app.** `npm run probes -- --review <email>` had demanded a real identity since M6, so it was correct — it was simply not a surface, and a specialist who cannot sign a prompt set off in the app cannot run a cycle unaided, which is M12's whole question. Both routes now go through one writer, so they cannot drift into disagreeing about what a sign-off is: the whole active set at once (signing off nine of fifteen would produce a measurement over a set nobody agreed to), active prompts only (a *retired* prompt marked reviewed becomes permanently un-retireable — a real M6 bug, preserved rather than re-derived), and never a re-stamp of somebody else's decision. **(3) A published report, a recorded shipment and a written verdict all have a correction path, and it is one design.** A correction **supersedes**; it never edits. The original row is untouched, all three freeze triggers are exactly as they were, and what moves is what a *reader* sees — the report composer, the shipping panel and the verification panel all walk the chain and take the latest, with the original still on screen beside it. That distinction is the whole point: an UPDATE with an audit log makes the corrected value primary and the history a second-class artefact somebody has to go and find; a superseding row makes the two the same kind of object, sitting next to each other, with the reader told there are two. **Verified by:** Ricky's own four presses on the `app-check` fixture against staging — 5 of 5 prompts signed off (`reviewed_by` = his row, 03:32:51), `dca82ef8` answered `held · promote` over a July with nothing announced over it, `18c4cde3` answered `held · keep_testing` over a June that two Google updates overlap **with both named on the verdict** (the confounder cap, seen rather than believed), and correction `f2d8c709` superseding shipment `c266bd31` while `work_items.evidence_url` still reads `https://google.com`, which is what was written first; his Supabase session refreshed at **03:32**, the minute of the first press, which local no-auth mode can never produce because it never signs in at all; 14 database refusals fired by name against the live database inside a transaction that was rolled back with nothing kept — including a well-formed correction accepted and then refused an edit, a delete and a forked chain; 253 tests across 9 suites; deploy `95386240` SUCCESS on commit `5135d6d` with no `queuedReason`, `/health` 200, the served bundle matching the local build hash and grepping clean for every new string, and all five new endpoints 401 unauthenticated. **Storepro was not touched by any of it:** `a3e5a8a7` is still refused by name with `outcome` null, 0 shipped, 0 corrections, and report `079c69d1` still frozen at method `2026-08-a`. (Three traps. **The correction form had a dead button and said nothing** — submit was gated on two conditions and the footnote named only one, so a correction with a good reason and no stated value sat behind a disabled control with no sentence explaining it. That is the *"control that looks passable and is not"* failure this app names by hand at every other gate, arriving at the one gate that exists to undo a mistake, and it cost two presses that were genuinely made and did not land. Every unmet condition is now named off one list, so the sentence and the `disabled` attribute cannot disagree. **The fixture was re-seeded**, which is how a shipment was lost last time — it was at 0 shipped so nothing of Ricky's went, but M9's fixture verdict did, deliberately: it predates `verified_by` and backfilling a name onto a press nobody made is the one thing this system exists to refuse. **A `CHECK` constraint cannot carry a subquery**, so "every key in this correction is one this kind may state" is written as `correction - 'a' - 'b' = '{}'` — strip every permitted key and nothing may be left.)

- **A correction supersedes, it never edits — and a corrected verdict is held to every rule the first one was** `M10` `decision` — The shape was settled in M8 and M10 built it, but three things inside it were decided here rather than inherited. **A correction states a corrected reading or withdraws the write, never both and never neither** — both would be two claims in one row that a reader could take as either; neither would be a note, and a note about a frozen row belongs wherever notes belong rather than in the record every reader takes the latest of. **What a correction may touch is bounded per kind**, in the writer and again in `0014`, and three omissions are decisions rather than gaps: a shipment correction cannot touch `owner_id` (re-attributing a delivery to somebody else is not a correction, it is a different and much worse act), a verdict correction cannot reach a prediction's frozen terms, and a report correction states prose and never a re-composed body. **A corrected verdict is a verdict** — both halves with its working, `too_early` still never written, still nothing promoted over a stated confounder, still no numbers on a `confounded` window — and for a *stronger* reason than the first time: the first verdict was produced by the judge, which had read the ledger and the arithmetic; the second was typed by a person. **A corrected shipping date is still dated against the plan's approval and still refused before it**, because a correction is a better-lit route for that edit than an UPDATE, not an exemption from the check. **Verified by:** 23 offline tests, each named after a way a correction could be a hole in a freeze and still look like a correction, plus a test that fails if `0014` and the writer ever disagree about which fields a kind may state.

- **A correction never leaves the app — the client is not told twice** `M10` `decision` — Ricky's call on 2026-08-19, the day the correction path became M10's: *"in-app only for the correction, don't send anything to the client."* So the correction path supersedes **internally** and stops there. The **original published report stays the document of record** — it is what the client received, and a corrected version arriving unasked would be a second document claiming to be the account of the same month, which is the exact failure the one-report-per-cycle index exists to prevent. What a specialist sees in the app is the correction, who made it and why, with the original still readable underneath. **What this rules out, stated so it is a decision rather than a gap:** nothing notifies the client, nothing re-exports, and a client acting on a figure that has since been corrected will not learn it from this system. That is acceptable because **nothing in this system emails or exports a report today** — adding a send path is a bigger change than the correction, and it would smuggle in the client-facing format decision that is deliberately still open in the Backlog. If a correction is material enough that the client must know, a person tells them; the app's job is to make sure the agency is never the one who has forgotten. ⚠️ **It also settles the shipment and the verdict by not applying to them** — nobody outside the agency ever sees either, so neither ever had this question.

- **M9: The verdict machinery — a prediction can be answered against a control, and `a3e5a8a7` is refused by name until November** `M9` `phase-3` `milestone` — The half of the loop nothing on the market has: not *what did traffic do*, but *what did the pages we changed do against twelve pages we deliberately did not*. **The arithmetic reports the difference and never the raw number** — and prints the raw one beside it, labelled, so what the control bought is visible. On Storepro's real June the treated pages rose 2.7 points and the untouched control rose 8.8, so the honest answer is **−6.1 against the control** where a retainer report would have printed *+2.7, up*. **The bar a movement has to clear is read off the control set rather than declared**: the pairs drifted 8.5 points apart before anyone touched anything, so an effect smaller than that cannot be told from the matching — which makes the bar harder, not easier, and harder is the only direction a self-set bar should move. **A page Search Console did not return is dropped and named, never counted as zero** (the couldn't-measure-read-as-measured-zero trap, caught for the fifth time; and dropping is not free either, so the count travels into the notes because keeping the pages that stayed visible biases what is left towards good news). 🔴 **Nothing was written about Storepro.** `a3e5a8a7` is refused by name in the CLI and in the app — *"read back over 2026-10 … not final until 2026-11-05 … there is no answer to give, which is not the same as the answer being no"* — and its `outcome` is still null in the database. **Verified by:** the June rehearsal above on the prediction's own twelve frozen pairs, with the May core update and the June spam update named from the ledger; `a3e5a8a7` read straight out of Postgres afterwards with `outcome`, `decision`, `verified_at`, `actual_value`, `control_value` and `confounders` all null; **223 tests across 8 suites** (35 new), `tsc --noEmit` exit 0; staging deploy `ee298f23` SUCCESS, `/health` 200, the served bundle downloaded and grepped for every new UI string, both new endpoints 401 unauthenticated.

- **Two decisions that make a verdict worth something, both taken deliberately and both enforced by the database** `M9` `decision` — **(1) A verdict is frozen the moment it is written.** A published report is frozen and a recorded shipment is frozen; a verdict is the strongest of the three, because it is the one that says whether the work worked, and one that can be revised after the client has read it is an opening position rather than a finding. The cost is stated rather than discovered: an algorithm update found *after* a verdict cannot be added to its confounders, which is exactly why the ledger is filled first. It also means the missing correction path is now **one Parking Lot card for three call sites** instead of two. **(2) `too_early` is never written, and `0013` refuses it outright.** The index `predictions (client_id, expected_by) where outcome is null` **is** the work queue. Writing `too_early` would be perfectly honest and would take the row straight out of it, so a prediction nobody could answer would be quietly settled and never looked at again. Every "not yet" is a refusal instead, `outcome` stays null, and the row stays where somebody trips over it. **Verified by:** ten refusals fired by name against the live database inside a transaction that was aborted so nothing was kept — including `too_early` refused, `promote` over a stated confounder refused, an outcome with no decision refused, a written verdict refused a second value, and `0010`'s terms freeze still refusing a moved window underneath all of it.

- **The algorithm-update ledger was empty since day one, and it is filled before the verdict rather than after it** `M9` `method` — `algorithm_updates` has existed since `0001` and nothing had ever written a row to it — the same shape of gap M8 found in `shipped_at`, and the ordering matters more than the table does. Deciding in November whether a core update overlapped October, **with October's numbers already on screen**, lets the answer pick its own confounder: a disappointing month finds an update to blame and a good one does not go looking. So it is filled now. **Google-owned sources only** — an SEO publication reporting a *confirmed update* is reporting somebody's inference from ranking volatility, which is worth reading and is not a fact to qualify a client's verdict with. **`verified` false is a research lead, not evidence**: it never flags a confounder, and it is never silently ignored either — a verdict over a window an unconfirmed entry overlaps is **refused** until somebody checks it or removes it, because otherwise *nobody checked* quietly becomes *no confounders*. (An open rollout with no published end date claims **one day**, not forever — a confounder that applies to every month after it means nothing.) **A confounder does not overturn the delta**, which surprises people: a matched control is precisely the defence against a change that moved the whole property, so what a confounder does is cap the decision — nothing is promoted over one. **Verified by:** six 2026 updates recorded, every one read off its own Search Status Dashboard incident page rather than from the summary list (February Discover core, March spam, March core, May core, June spam, August spam). **None of them overlaps 2026-10** as things stand — a fact with a date on it, and the reason the ledger is a standing job rather than a one-off.

- **Machinery that has only ever met a fixture is machinery nobody has checked — so the arithmetic was rehearsed on a month that already closed** `M9` `method` — Every prediction in this system is read back over a month that has not happened yet, so on the day the writer was built there was no way to run it on real data at all. `npm run verify -- --client storepro --over 2026-06` runs **the same judge** over a closed month, using the prediction's own frozen pairs against the month before it: real pages, real Search Console figures, the whole verdict. **What it is not is an answer**, and it is refused a write structurally rather than by convention — every rehearsal reading is marked unwritable, the writer throws on one, and the CLI refuses `--over` with `--write`. (The tempting alternative was to move a window back to a month with data in it, which is the single thing `windows.ts` exists to make impossible. A rehearsal also carries **no drift**: the shipping deadline belongs to the recorded window, and applying it to a month chosen for having data would be reading one window's deadline against another window's numbers.) The writer's *accept* path was then pressed for real on the `app-check` fixture only — a prediction over a closed month wrote `held · promote`, 7,800 against 6,120, and a second attempt was refused as `already_verified`.

- **A prediction on a superseded plan's item would have looked like one that shipped on time** `M9` `finding` `method` — Found reviewing the new code rather than by anything failing. The shipping position was read through `loadShippingContext`, which only ever loads a client's **most recent approved plan** — so a prediction attached to an item on an earlier plan came back with no shipping date and no change type, the drift check produced nothing, and **a missing drift check reads exactly like a clean one**. Storepro has two approved plans (`62ff1c2c` from M4 and `70337c95`), so this was live rather than theoretical. Fixed by reading the work item by id. Same family as the `not_assessed`-versus-`failed` distinction the readback is built on: the expensive bugs in this project are all *absence rendered as a passing answer*.

- **There is no verdict button in the app, and the reason is a missing column rather than caution** `M9` `finding` `decision` — The panel shows the verdict and leads with the refusal and its date; it does not offer to write one. `predictions` has **no `verified_by` column**, so a verdict recorded from the app could not be attributed to anybody — while every other frozen write in this system carries a name (the plan approval, the report publication, the shipment). Offering a control that records an unattributable permanent claim about a client's work would be worse than offering none, so the panel says where the verdict is written from instead. Carded for M10, where the specialist actually has to run it.

- **The capacity line comes off the plan — how much of the month's work gets done is the team's call, not the ranker's** `M11` `decision` — Ricky's call on 2026-08-19: *"I don't think it's our call to place hours on the work — since we might use AI tools to make it more efficient — so the work list is enough and the team will decide how much work to do and what to push to another month."* He is right about the schedule and the mechanics split in two. **(1) The capacity line goes, and it costs nothing** — `--capacity` is already optional on `npm run plan`, and a plan built without it prints *"No capacity declared, so nothing is marked below the line"*. Storepro's `70337c95` was built with `--capacity 12` passed once, in August. **(2) The hours stay, because they are not a schedule — they are the ranking's denominator.** `rank.ts:430` scores `value / effortHours`; strip the estimates and the plan ranks by raw value, which floats the largest and most expensive items to the top. So the estimates keep doing the job they are good at (relative cost) and stop doing the one they were never calibrated for (fitting a month). ⚠️ **The AI-efficiency argument bites the ratio as well as the total**, and unevenly: AI collapses bulk mechanical work far harder than it collapses *"not cited for what is a mezzanine floor"*, so the denominators shrink at different rates and the order drifts. That is an argument for calibrating the constants against real `shipped_at` data — which M8 made possible for the first time — not for deleting them. 🔴 **Not applied to Storepro's live plan, deliberately.** Prediction `a3e5a8a7` is bound to work item #3 *in plan `70337c95`*; a rebuilt plan creates a fresh row for the same FAQ work and shipping the new one would not answer the prediction, which still points at the old one. **Takes effect at the next plan build, after November's readback.**

- **M9 was split from the verdict, for the third time and for the same reason** `M9` `M11` `decision` — Ricky's call on 2026-08-19, immediately after M8 landed. The readback's doneWhen cannot be met before **5 November**: prediction `a3e5a8a7` reads back over October 2026, those figures settle then, and back-dating is refused by a trigger on purpose. Eleven weeks of an open milestone with nothing able to move is the quiet expansion a doneWhen exists to prevent. So the milestone was cut where the calendar cuts it: **M9 is everything the verdict needs and can close this week**, **M10 is the verdict and waits for the month**. The standing rule from the two previous cuts is unchanged and now applies a third time — *work that must be recorded before the verdict date is its own milestone ahead of the verdict* — and the ledger is the clearest case of it yet, because a confounder decided while the answer is visible is not a confounder. **The two options not taken, and why:** M10-then (now M11) the handover is real work with a closable doneWhen, but it is blocked on in-app prompt sign-off and it would leave the November verdict's inputs unbuilt with the window already open; the three small fixes (UTC labelling, the undecidable acceptance criterion, the flaky staging deploy) are all cards in This Week and none of them is a milestone. **Arc grew 10 → 11.** M11 is the handover; M4's comment naming the handover milestone was renumbered with it.

- **Proving the gate by walking through it would have been the failure the gate exists to prevent** `M8` `decision` `method` — The most important call of the session and it was Ricky's. M8's doneWhen originally required a specialist to mark one of **Storepro's** approved items shipped, which assumed August's work would be done. It is not being done — the exercise was a test of the app, not a commitment to the site — and the obvious shortcut was to press the button on Storepro anyway to see whether it worked. **That shortcut costs a permanent false statement about a real client's work.** `shipped_at` is frozen on write with no correction path, and the composer reads it to tell a client an item was *delivered*. The record would have sat in their document forever, and the motive being *testing* does not change what the record says. So the human act stayed and its subject moved: **`npm run fixture -- --seed`** creates `app-check`, unmistakably not a client — status `prospect`, domain `app-check.invalid` (RFC 2606, can never resolve, so no crawl can reach a real site), and a name that says so wherever a client name appears. One approved plan, three approved ranked items across three change types so the *ship by* column is visibly derived rather than constant, and one recorded prediction — that last one added deliberately, because a client with no predictions satisfies *"every prediction changes accordingly"* vacuously rather than demonstrating it. **New doneWhen:** *A specialist records a shipment in the app on a deployed environment, with evidence, and the shipping record and every prediction change accordingly.* The old wording and the reasoning are kept in `kanban.config.js` rather than overwritten. ⚠️ The fixture writes to the same Supabase project staging reads — that is the point, and it is why the teardown is a command (`npm run fixture -- --drop`) documented in the README rather than a note in somebody's head.

- **M8: The shipping record — the system can be told the work is done, and it checks whether that was in time** `M8` `phase-3` `milestone` `shipped` — **Ricky recorded the first shipment this system has ever held**, in the app on staging, on 2026-08-18 at 21:47:56Z. Before that, `work_items.shipped_at`, `owner_id` and `evidence_url` had existed since `0001` with nothing anywhere writing them — which is why the published July report says *13 committed, 0 delivered*. That number was true, and it was true because there was no way to say anything else. **What a shipment is now:** a date, a link somebody else can open, and a named person, recorded together or not at all, and frozen the moment it is written. **What it refuses:** no evidence; evidence that is not a link; a session whose identity resolves to nobody; an unapproved item; an unranked observation; a date before the plan committed to the work; a date in the future; a second shipment. Then no un-shipping, no re-dating, no swapping the evidence, no reassigning the owner. **The policing half introduced no new judgement, exactly as scoped.** `server/ship/drift.ts` re-runs the existing pure `readbackWindow` against the date the work shipped and compares the month it earns to the month the prediction recorded. **The recorded window never moves** — re-deriving it from `shipped_at` and storing the new one would make every prediction retroactively right about its own timing, which is the story-with-the-flattering-dates the whole design refuses. What changed is that the report now says out loud when the work did not arrive in time. It produces **3 September** for Storepro — found by walking the boundary with the function itself, not by subtracting the latency, and asserted for every change type across four months. **The client's report gained a tenth publication check**, `shipping_stated`, which fails when a prediction says nothing about whether its work shipped — because an unstated assumption reads as a met one. `REPORT_METHOD_VERSION` moved to `2026-08-b`. **Verified by:** the shipment read straight out of Postgres — item `c1def139`, `shipped_at` 2026-08-18 21:47:56Z, `owner_id` resolving to `ricky@coreshifthq.com`, evidence stored, status `shipped`. Then **every freeze exercised against that real row** and refused by name inside a rolled-back block: un-ship, re-date later, re-date earlier, swap the evidence, reassign the owner, revert the status — while a move to `verified` stayed allowed, so M9 is not blocked. A second shipment refused through the real writer. All eight database guarantees also exercised against Storepro's real FAQ item and refused by name. **The drift check then produced `on_time` out of the database rather than out of a test double**: shipped 2026-08-18, sixteen days before the 2026-09-03 deadline, recorded window `2026-10` unchanged and answerable from 2026-11-05. **188 tests across seven suites** (13 detector, 38 plan, 24 action, 23 prompt, 35 prediction, 33 report, **22 shipping**), typecheck clean. Commits `3d14835`, `a633c64`, `953a5b0`, `9a8ded1`. Method in `docs/shipping.md`. ⚠️ **Storepro has nothing marked shipped and its report still says 0 of 13, which is correct** — the August work is not being done, and that is the whole reason the fixture exists.

- **Staging deploys now fail roughly every other time, always with the same signature, and always succeed on retry** `M8` `M9` `infra` `risk` — Caught three times on 2026-08-19 by the standing rule rather than by anything visibly breaking: `820a9f07` FAILED, retry SUCCESS; `314b9488` FAILED, retry SUCCESS. Each time the site kept answering on the previous build, so `/health` stayed 200 and a green push looked like a deploy. **The build is not the problem.** Every failed build log runs to completion — `tsc --noEmit && vite build` clean, 73 modules, image exported and pushed — so this is the deploy step, not nixpacks, and it is **not** another instance of the `NODE_ENV` cache break, which always left a named error in the build log. ⚠️ **`railway logs -d` returns nothing whatsoever** for the failed deployments — not even a startup line — and Railway exposes no commit SHA at runtime, so there is no way from outside to confirm which commit is being served. **Recorded as Railway-side rather than diagnosed**, because that is what the evidence supports and no more: identical commit, identical image, clean on retry, three times. 🤔 **Where to look next, in order:** the healthcheck — `railway.json` allows `/health` 60 seconds with `restartPolicyMaxRetries` 3, and a cold `tsx` start (this runs TypeScript through `tsx` at boot rather than compiled JS) on a small instance is the plausible edge. Raising the timeout is one line. Compiling to JS for the start command is the real fix and is a bigger change. 👉 **Until it is understood, every deploy needs `railway deployment list` and a retry.** That is a manual step in an otherwise automatic pipeline and it will bite whoever forgets. ⚠️ **A second, different signature on 2026-08-19 (M9).** Both M9 deploys sat at `QUEUED` for roughly twelve minutes with nothing in `railway deployment list` to say why, then built and deployed cleanly first time. `railway deployment list --json` carries the answer the plain list hides — `meta.queuedReason: "Deployment queued due to upstream GitHub issues"` — while githubstatus.com reported all systems operational, so that diagnosis is Railway's own and is not corroborated. **`--json` is also the only place the commit hash appears**, which is the standing *which-commit-is-staging-running* gap in a form that can at least be read from outside. Two different stalls now, one silent and one self-explaining: check `--json` first before assuming the intermittent one.

- **The doneWhen assumed work that is not going to happen, so it was replaced rather than reinterpreted** `M8` `decision` — Ricky's call on 2026-08-17. M8 read *"A specialist marks an approved item shipped, with its evidence…"*, which assumed Storepro's August work would be done. It is not being done: the whole exercise was a test of the app, not a commitment to the site. That made the clause unmeetable honestly — and the tempting move, pressing the button on Storepro anyway to prove the app works, is precisely the failure the milestone was built to prevent. `shipped_at` is frozen on write, has no correction path, and is read by the composer to tell a client an item was **delivered**. **Walking through a gate to prove it is shut is not a test of the gate.** So the human act stayed and its subject moved: `npm run fixture -- --seed` creates **`app-check`**, unmistakably not a client — `status` `prospect`, domain `app-check.invalid` (RFC 2606, can never resolve, so no crawl can reach a real site), and the name saying so in the client list. One approved plan, three approved ranked items, no metrics and no audit runs, because composition and readback are already proven and the only thing a fixture adds is the click. **New doneWhen:** *A specialist records a shipment in the app on a deployed environment, with evidence, and the shipping record and every prediction change accordingly.* Storepro keeps 0 of 13. ⚠️ The fixture writes to the same Supabase project staging reads — that is the point, and it is why the teardown is `npm run fixture -- --drop` in the README rather than a note in somebody's head.

- **The cheapest item's own instruction offers a fix its acceptance criterion cannot see** `M8` `finding` `method` — Found by re-running the audit to make the one-click reachable, which is the sort of thing only running it turns up. Item 1's authored action is *"make the 3 pages either return real content **or** return 404/410, and remove them from sitemap.xml either way"*, and its acceptance criterion is *"the next audit run emits no `obs.empty_responses` finding"*. **Those two branches are not equivalent to the readback.** `obs.empty_responses` has a per-page predicate, so `server/predict/readback.ts` refuses to call the criterion passed unless the run actually *assessed* the named pages. Remove them from the sitemap and they are never discovered, never crawled, never assessed — and the verdict is **`not_assessed`**, with the entirely correct message *"a finding cannot be raised about pages nobody crawled, so its absence says nothing here"*. Only the *give them content* branch can ever produce `passed`. ⚠️ **This is not a bug in the readback** — it is the project's most persistent rule working exactly as designed, one layer further out than it has appeared before: the honest verdict on a page that no longer exists is *unknown*, not *fixed*. What is wrong is that `server/plan/actions.ts` authored an action whose cheaper branch makes its own acceptance criterion unverifiable, and nothing notices. **The consequence today is small and specific:** the one-click cannot appear for item 1 if the 404 route is taken, and the shipment has to go through *Mark shipped anyway*. **The consequence at scale is the thing to fix:** an action with a branch its criterion cannot observe will keep producing items that read as unfinished forever. The check that would catch it — does every branch of an authored action leave the acceptance criterion decidable — does not exist and belongs next to the existing action-coverage tests in `npm run test:actions`.

- **Re-running the audit made the readback a readback rather than a baseline** `M8` — Run `d853a778` (147 of 202 pages, method `2026-08-a`, SEO 91.50 / GEO 93.44 / AEO 45.45, 12 findings) post-dates the plan's approval, so `server/predict/readback.ts` will now mark things finished when they are. Before this the only run on file was `54b3758e` — the run the plan was *built from* — which is labelled `baseline` and may never mark anything finished, correctly. **It did not make the one-click appear, and could not have:** the one-click is gated on the audit agreeing, and the audit can only agree once the work is done. 0 finished, 12 failing, 1 needs a person — unchanged, and right. The shipping record is untouched: 0 of 13 shipped, binding date still 2026-09-03.

- **Bumping the composer's method version made the report panel tell a small lie** `M8` `finding` — Adding the shipping statement to pending predictions moved `REPORT_METHOD_VERSION` to `2026-08-b`, which is correct and required. What was not correct was the panel: it prints `Method <version>` from the **composed** body, directly under a green *published* pill — so the frozen July report appeared to carry a method it was not published under. The version stamp exists precisely so a reader can tell which document they are holding, and printing the wrong one under a publication badge defeats the whole point of stamping it. Both the panel and the CLI now say when the two differ and which is which. Found only because the bump was the first time in this project's life that a published report and a composed one had different method versions — everything before this had exactly one version to print. ⚠️ The published report `079c69d1` also now shows *the records have moved since* permanently, which is honest and unavoidable: any change to the composer, or the first shipment recorded, makes today's composition differ from what was sent.

- **The one-click had to be built as a click plus a link, not as a click** `M8` `method` — The readback's *"the audit agrees, mark it shipped"* was wanted since M6, and the obvious build — one button, no fields — collides head-on with M8's own first guarantee: **no shipment without evidence**. A button that ships with no evidence is a tick in a box wearing an audit verdict. So the button opens a row with the evidence field **pre-filled with the audit run that agreed**, and *Record it as shipped* stays disabled until there is a link. What the click removes is leaving the panel to construct a CLI call; it does not remove the judgement, because *"this named check passes on these pages"* and *"this work is done"* are different claims and only the first is one a crawl can make. ⚠️ Marking an item shipped while the audit **disagrees** is still allowed and says so on screen in as many words — the criterion may be unmeasurable, or the evidence may be somewhere the crawl cannot see, and it is also how work that was never done reaches a client's report as delivered. **Verified by:** the panel offering no button at all under the default local identity (`local@localhost` has no `auth.users` row, so the gate would refuse it — M7's rule, applied without being asked), and offering the disabled-until-evidence form under a session that resolves.

- **Staging stopped deploying and every check we had said it was fine** `M7` `finding` `infra` — The most useful thing found this session, and it was found only because landing the milestone required proving staging served the commit rather than assuming it. **Railway keeps serving the last good build when a build fails.** So `/health` returned 200, the site loaded, the push was green, `origin/staging` was at the right SHA — and the newest code had never been live. `railway deployment list` was the only thing that said otherwise: **two FAILED deployments** sitting above the last SUCCESS. **The cause:** `NODE_ENV=production` is set on the Railway environment, so the default `npm ci` omits devDependencies — and `npm run build` is `tsc --noEmit && vite build`, both of which are devDependencies. `tsc: not found`, exit 127. **It had been latent for about a day**: Docker was reusing a `node_modules` layer built before that variable existed, so builds kept passing on cache and broke the first time the cache expired — which is the worst shape a break can have, because nothing in the commit that exposed it was wrong. (Who set `NODE_ENV` and when is not recoverable from here, and is recorded as unknown rather than guessed at.) **The first fix traded one silent failure for another:** moving `npm ci --include=dev` into `railway.json`'s `buildCommand` died with `EBUSY: rmdir '/app/node_modules/.cache'`, exit 240 — the build phase mounts a cache *inside* `node_modules`, and `npm ci` wipes `node_modules` before installing, so it cannot remove its own mount point. `npm ci` can only run in the **install** phase, whose cache lives at `/root/.npm`, outside the tree it deletes. Pinned in **`nixpacks.toml`** rather than as a Railway dashboard variable, on the grounds that a build which only works because of invisible dashboard state is the bug and not the cure — and the file reads as a bug report, deliberately, so the next session inherits the reasoning. **Verified by:** deployment `6edd08f1` SUCCESS after two FAILED; staging serving `index-BfTTALVZ.js`; and the live bundle downloaded and grepped — all three new strings present (`has no user record, so a publication could not be attributed`, `connection problem, not a refusal`, `Publish this report`), which is stronger than a matching hash. Commits `45d235e` and `06d6b20`. ⚠️ **The shape of this failure outlives the fix:** never take a green push as a deploy.

- **M7: The monthly report — published, frozen, and it states what we expected before we did it** `M7` `phase-3` `milestone` `shipped` — **Storepro's 2026-07 report is published.** Ricky published it himself on **2026-08-14 21:44:15Z**, which is the whole point of the milestone: `--publish` takes an email that must resolve to a real `auth.users` row, and in the app the publisher comes from the verified session and cannot be typed into a field. Claude built it, proved every refusal, and left the last act to a person — publishing it as Ricky would have forged a human gate, which is the failure this product exists to make impossible. The report a client reads now says, on the record and unable to be edited: **13 items** each bound to the finding, audit run, rule and severity that produced it, each carrying its own reasoning word for word; **one prediction, PENDING**, with its readback window `2026-10` and the date it can be answered — **5 November** — on the face of it; **twelve refusals in full**; and **eight stated limits**. **Verified by:** the published row read straight out of Postgres — `079c69d1`, `published_at` 2026-08-14 21:44:15Z, `published_by` `ricky@coreshifthq.com`, cycle `2026-07` moved to `published`, 9 of 9 `review_checks` passing. Then **every freeze exercised against that real row**: editing the body refused (and `body_tampered` still false afterwards), revoking the publication refused, flipping a check to failing refused, deleting the report refused, a second published report for the cycle refused by the unique index — while `review_notes` stayed writable, which is correct, because an annotation is not the document. **162 tests across six suites** (13 detector, 38 plan, 24 action, 23 prompt, 35 prediction, 29 report) and typecheck clean. Commits `b758643` and `15a0f23`. Method in `docs/reports.md`.

- **The panel offered a publish button that could never work — and the error made it look like a rejection** `M7` `finding` — Found the way these things should be found: Ricky pressed publish and got **"Failed to fetch"**. Two faults, and the second is the real one. The proximate cause was a stopped dev server — Claude had opened a local preview panel to verify the report rendering and then shut the server down at the end of the turn, leaving a live publish button on screen pointing at nothing. Nothing was attempted and nothing was published, but `fetch` rejecting rendered its raw message straight into the error line directly beneath the button, which reads as *the report was rejected*. **A transport failure and a refusal are materially different** and the panel now says which one it was, and that the draft is untouched. **The worse fault underneath:** that button should never have been offered at all. Local mode signs in as `local@localhost`, which deliberately has no `auth.users` row, so the gate would have refused it by design. **A control that looks passable and is not is the unenforced checklist inverted** — arriving through the exact surface built to prevent it. The server now reports whether *this session* can be attributed, which is a different question from whether the report is publishable, and the panel distinguishes the two reasons there may be no button. **Verified by:** the endpoint returning `{email: "local@localhost", attributable: false}` with `publishable: true`, and the panel rendering the reason with no publish button, seen against a local server on the production bundle. Railway's logs confirmed no publish request ever reached staging, which is how the diagnosis was settled rather than guessed. Commit `15a0f23`.

- **The report refuses to call approved work delivered — and Storepro's first one therefore opens by saying nothing shipped** `M7` `method` — An item is delivered when `work_items.shipped_at` is set, which is a specialist saying so. It is **not** delivered because the acceptance readback says the audit rule now passes: the readback reports, a person decides, and reading a verdict as a delivery would have the report claim work nobody has done. On the facts as they stand, all 13 items are `approved` with `shipped_at` **null** on every one — so the report's second sentence reads *"Nothing is marked delivered yet: 13 items were approved and the work runs from there."* ⚠️ **That is the honesty the milestone was warned about, arriving in the first paragraph rather than the footnotes.** It is true, and on the morning after approval it is the only true thing to say — but whether it reads as rigour or as a thin month is a writing-and-positioning question, and it is Ricky's. Fifth appearance of this project's most persistent failure — *"couldn't measure" read as "measured zero"* — in its first client-facing form.

- **An uncalibrated effort estimate was hiding inside the rationale we promised to carry verbatim** `M7` `finding` — Two rules collided and only one could win as written. The milestone says *carry each item's rationale verbatim*; the standing rule says *never quote effort hours to a client, they have never been calibrated and one release moved several by more than 25%*. `work_items.rationale` contains the clause **`Estimated 18h.`** — so reproducing it verbatim would have put an uncalibrated guess in front of a client as a commitment, and stripping it silently would have made "verbatim" untrue. **Resolved by segmenting the rationale into the labelled clauses the ranker actually builds** (`observation` / `Why now` / `Estimated Nh` / `First principle` / `If this is wrong`), carrying every clause word for word, and **withholding exactly one — with the omission stated in the report's own limits section rather than left as a gap.** The segmenter never paraphrases: `exact` is true only when the clauses rejoin into the stored string **byte for byte**, and a rationale that does not reconstruct **fails the publication gate by name** instead of being quietly tidied. Guarded by a test that builds its input through `buildRationale` itself, so a change to the clause format fails the suite rather than dropping a clause out of every future report.

- **The publication gate is nine checks derived from the report, not nine questions asked of a person** `M7` `method` — `review_checks` has carried a comment since `0001` saying that a gate with no storage is a convention rather than a control. It is now a control. Nine checks are **derived from the composed body** — work bound to its origin, rationale verbatim, nothing claimed as delivered, predictions pending with their windows, refusals carried, no unattributed movement, coverage stated, no magnitude, no effort hours — stored as rows with **what the inspection found next to the boolean**, and re-read by the database at publication. A failing check does not grey out a button with a tooltip: the publish path is **gone**, and Postgres refuses independently. ⚠️ **The required-key list is duplicated into the migration on purpose**, so the enforcement survives an edit to the composer — and a test reads the SQL and fails if the two lists ever disagree, which is what keeps the duplication a safeguard rather than a bug waiting to happen. **Verified by:** all nine database guarantees exercised against the live database inside a block that was **rolled back so nothing landed on a real cycle** — publishing with no checks refused, publishing with one failing check refused by name, `published_at` with no publisher refused by CHECK, the published body refused to change, the checks refused to change, the row refused to delete, and both unique indexes fired. `review_notes` stayed writable, which is correct: an annotation is not the document.

- **The one prediction is attached to work the month does not have the hours for** `M7` `finding` `risk` — Found while composing the report, and it changes what November will mean. The FAQ-schema item that carries Storepro's only prediction is ranked **third at 18 hours against a declared capacity of 12**, behind two items that consume 5.8 of those hours — so the plan itself marks it **below the capacity line**. The item is approved and its reasoning stands, but the month cannot pay for it, which makes it **the most likely thing on the plan to slip**. And the readback window is anchored to the plan's *approval* rather than to when the work ships, deliberately, because waiting for the ship date would mean choosing the comparison window after the change. So if it slips out of August, October stops being a clean month and the honest outcome is **`confounded`** rather than negative. The report states this in its limits section rather than leaving it to be discovered on 5 November. 🤔 **Worth a decision before the work starts:** either the FAQ item gets the hours, or the first verified prediction is likely to come back inconclusive.

- **The prompt sign-off gate could not record who signed** `M6` `finding` — Found by trying to use it. Ricky signed the 15 active prompts off on 2026-08-14, and recording that exposed that `npm run probes --review` took a **free-text name**, set `reviewed_at` and left `reviewed_by` **null** — with a comment explaining that `auth.users` had no rows to point at. True when it was written, and it stopped being true the moment two real users signed in. A timestamp with no attributable identity is the unenforced checklist this product exists to replace, arriving through yet another door: it records that *somebody* approved the prompt set and makes it impossible to say who. The reviewer is now an email that must resolve to a real `auth.users` row, and an unknown one is **refused** rather than recorded — the same refusal, for the same reason, as the plan approval gate. (Second bug in the same seven lines: it marked *every* unreviewed prompt on the client, including the 14 retired ones. `--from-gsc` refuses to retire a reviewed prompt, so a retired prompt marked reviewed would have become permanently un-retireable, and "prompts an analyst signed off" would have stopped describing the set actually being probed.) **Verified by:** `nobody@example.com` refused with the reason in words; then **15 active prompts reviewed and attributed to `ricky@coreshifthq.com`, 14 retired prompts untouched, one distinct reviewer** read back out of Postgres; a fresh plan run no longer carries the unreviewed caveat, while approved plan `70337c95` keeps it because its inputs record what was true when it was built. Commit `c33c617`.

- **Four decisions, taken 2026-08-14** `M6` `decision` — All four open questions closed in one pass, and each changes what the next session does. **(1) M7 is split.** The verdict half needs data that is final on 5 November, so it became M8 and the report became buildable today; handover moved to M9. **(2) The 15 prompts are signed off** — see the card above, which is what trying to record it turned up. **(3) No holdouts, not yet.** A true holdout — deliberately leaving comparable pages broken for a month — would support a stronger causal claim and would cost the client real traffic, and it is the kind of thing that has to be said to them out loud. Revisit once one prediction has actually verified and we know the signal is even visible. **(4) Storepro only, for now**, with a second client to come later. ⚠️ That last one is worth carrying as a known limit rather than as a closed question: every threshold M6 declared has exactly one property behind it.

- **M6: The prediction machinery — we now write down what we expect, against a cohort we deliberately left alone, before the change** `M6` `phase-3` `milestone` `shipped` — The reason this product exists, and the thing none of the seven surveyed repos does. Every tool recommends work; none of them records what it expected the work to achieve, against a control, over a window fixed in advance, and then goes back and checks. **Storepro's first prediction is recorded and frozen:** the FAQ-schema item's target pages will show **more Search Console impressions than a matched set of untouched pages over October 2026**, direction only with no magnitude claimed, confidence **low**, baseline 16,184 impressions in July, readback window `[2026-10-01, 2026-11-01)` and final from **5 November**. Against **twelve untouched pages matched one-to-one** on the shape of their four-month history, tracking the treated pages within **8.5 percentage points** across the whole pre-period. **Twelve of the thirteen items were refused, each with a reason a client could read** — and that ratio is the product, not a shortfall. **Verified by:** the prediction read straight out of Postgres (`a3e5a8a7`, control set `d7de6e64`, 12 members, `expected_magnitude` null, `method_version 2026-08-a`); **133 tests across five suites** (13 detector, 38 plan, 24 action, 23 prompt, **35 new prediction**); typecheck and build clean; staging serving the new bundle `index-Ye_0f7Ex.js` with `/health` 200 and both new endpoints 401 unauthenticated; and all five database guarantees exercised against the live database and refused by name. Commit `994f604`. Method in `docs/predictions.md`.

- **"Every page we didn't touch" is not a control — and it took real data to prove it** `M6` `finding` — The single most important thing found this milestone, and it would have shipped as a bug in any tool that reasoned about it instead of measuring. The obvious control set is *the pages no item on the plan targets*. On Storepro those are the product category pages; the pages the plan targets are the homepage, the blog and the about/contact set. Over the four months before the change the untouched group grew **25%** while the targeted group sat **flat** — they are different kinds of page with different trajectories. A difference-in-differences against that "control" **would have required the treated pages to grow 25% just to score as no effect**, and the November report would have described a successful month as a failure with a completely straight face. So membership is *matched*, not merely complementary: one-to-one on the shape of the pre-period series (each page indexed to its own mean), baselines within 5×, no page used twice, and both sides needing a complete history across every month. **Then it checks itself and refuses**: under five pairs, one page carrying over 40% of a group, or more than ten points of pre-period divergence, and there is no prediction. Every surveyed repo demands a control. Building one turns out to be much harder than demanding one.

- **Twelve of thirteen items were refused, and the refusals are the argument for the one** `M6` `method` — What a tool refuses to claim is the only reason to believe what it does claim. **5 items** had too few target pages with a matched control (`aeo.answer_proximity` could match 2, `obs.stale_content` 1). **2 items** had a treated group where the homepage alone carried 57% of the baseline — one page's result wearing a cohort's clothes. **2 items** had a control that was already diverging before the change. **2 items** have an authored reason they cannot be predicted on at all: `obs.images_missing_alt`, because alt text is an accessibility and image-search signal and neither surfaces in the web-search impressions we collect; and `obs.empty_responses`, whose two correct outcomes pull in opposite directions. **1 item** is the probe, refused because the citation sampling design still does not exist. ⚠️ **Two metrics are refused permanently or indefinitely.** The three audit scores, because fixing a check raises its own pillar score by construction — predicting it is arithmetic wearing a prediction's clothes, and it would make the loop appear to work every single month. And `ai_citation_rate`, until the sampling design lands: it is the number a client would most want to believe, and it currently rests on a sample nobody has bounded. That was M6's first decision and it was made deliberately.

- **"Finished" is now a fact — and the answer that mattered was neither pass nor fail** `M6` `method` — M5 authored the acceptance criteria, stored them structurally and indexed them; nothing read them. `npm run readback` now re-runs **the audit's own predicate** over a later run's page snapshots and decides each item. The verdict M5 had no place for is the important one: **a target page the crawl did not reach has not passed.** Storepro's runs are `partial` at 147 of 202 pages — the normal outcome above the crawl budget — so an item naming 147 pages can have 90 assessed, and scoring the other 57 as passing is *"couldn't measure" read as "measured zero"*, which this project has now hit five times. There are four verdicts: `passed` (all assessed, rate met), `failed` (a definite no), `incomplete` (met on what was assessed, and here is what was not), `not_assessed`. Two further guards: a run that pre-dates the plan's approval is labelled `baseline` and can never mark anything finished, and a changed audit method blocks a finished verdict because the named check is no longer the same check. **It reports and marks nothing** — a specialist still decides that work is done. (Bug found and fixed on the way: criteria were being dispatched on `scope` rather than `mode`, so all six observational rules — authored `{mode: "finding_absent", scope: "site"}` — were looked up in the scored-check table and reported as *"no result"*, which reads as *we do not know* when the run had said something perfectly clear.)

- **The readback window is derived from the approval timestamp, and the database will not let it move** `M6` `method` — What makes "we fixed the window before the change" checkable rather than claimed. `Date.now()` does not appear in `windows.ts`: the window is a pure function of `work_plans.approved_at`, so re-deriving it in November produces the same dates. **The readback month is October, not September**, and that is not conservatism — the metric is a calendar month, a month that is half pre-change and half post-change measures a blend, and a blend that comes in flat cannot be told apart from no effect. Approved 14 August + 28 days of latency = 11 September, so October is the first month measured entirely with the change in place. **Five guarantees now live in Postgres rather than in whichever code writes the row, and all five were exercised against the live database:** the terms of a recorded prediction cannot be edited (17 columns frozen by trigger — only the verification fields are writable); a prediction cannot exist without a control set (NOT NULL, and the FK moved to `on delete restrict`); a readback window cannot begin inside a month already ingested for that client (rejected by name, with the reason in the error); the baseline must end before the readback begins; and no outcome may be recorded without a `control_value` beside its `actual_value`. A writer-enforced version of any of these would be one careless `UPDATE` away from a prediction that always turns out to have been right, with nothing in the row to show it.

- **No prediction carries a number, on purpose** `M6` `method` — `expected_magnitude` is null on every prediction and will stay that way until something here has been verified against an outcome. *"Impressions on these pages should rise relative to a matched control, and we do not know by how much"* is a falsifiable claim. *"+15%"* is a fabricated one that looks more professional, and the first time it is wrong it costs more than the whole feature is worth. Coverage is recorded separately from confidence for the same reason: the FAQ prediction covers **12 of the item's 147 target pages**, because Search Console returns only its top 50 pages and only 12 targets have both a complete four-month history and an admissible partner. That makes it a *narrower* prediction, not a less confident one, and blending the two would hide both. Magnitude becomes answerable after a few cycles of verified predictions — the same shape as the effort estimates waiting on `shipped_at`.

- **Storepro's competitor cohort is declared — and declaring it did nothing until a bug was fixed** `M6` `finding` — Ricky declared five competitors on 2026-08-14 01:08:55Z (`dexion.co.nz`, `palletrackingsolutions.co.nz`, `aucklandracking.co.nz`, `multiracknz.co.nz`, `shelvingdepot.co.nz`), sourced from the client's own answer rather than from our observation, and recorded as version 1 with that note. **Then the check that it had worked found it had not.** `competitors_cited` is derived at judge time against whatever cohort existed *at that moment* — all 93 answered runs were judged with none, so all 93 carried `null`, which is correct: no cohort declared is *not measured*, never zero competitors. Declaring one makes those runs answerable and `rejudge` is what answers them. But its change-detection compared dropped sources, the cited verdict and the detector version — **and not competitor presence** — so it printed **"0 of 93 runs would change"** when the truth was that 43 of the 82 re-judgeable runs name a real competitor. A confident zero standing in for "we never looked" is the failure this project has now hit **five** times, and it is why the comparison is now a named, exported, tested function instead of an inline `&&`. **Verified by:** the dry run going from 0 to 82 of 93 after the fix, then applied — `competitors_cited` now reads **43 runs naming a rival, 39 measured-and-none-cited, 11 still not measured** (those 11 predate `engine_sources` and must be re-probed rather than re-judged, which `rejudge` correctly refuses to do). Share of the cohort across answered runs: `palletrackingsolutions.co.nz` 26, `dexion.co.nz` 17, `shelvingdepot.co.nz` 12, `multiracknz.co.nz` 8, `aucklandracking.co.nz` 7. 98 tests passing. Commits `6823af8` and `2c2203e`. **Then Ricky widened the cohort to eight** (v2, 01:20:04Z), adding `automate-x.nz`, `shelvingshopgroup.co.nz` and `stackit.co.nz` — the three most-cited non-Storepro domains in AI answers, none of which the client had named, and one of which (`automate-x.nz`, 64 source appearances) outranks every domain they did. The client's original five are all retained; v1 is preserved and the note records why. Re-judged again: **44 more runs updated**, and the cohort's share of answered runs is now `palletrackingsolutions.co.nz` 26, `automate-x.nz` 24, `shelvingshopgroup.co.nz` 19, `dexion.co.nz` 17, `stackit.co.nz` 13, `shelvingdepot.co.nz` 12, `multiracknz.co.nz` 8, `aucklandracking.co.nz` 7. **That widening exposed a second bug in the same family:** the API's re-judge hint fired only when *no previous version existed*, which is backwards for the case that most needs it — adding three competitors leaves 93 runs judged against a cohort that no longer exists, and the response said nothing because a v1 was there. It now follows the declared set rather than the version number, reusing the same tested comparison. (The null-versus-empty distinction is the whole bug: `null → []` is a real change — *not measured* becoming *measured, none cited* — and folding them together would leave a run looking unmeasured forever. There is now a test named after exactly that.)

- **M5: Actionable work — the plan stopped describing problems and started giving instructions** `M5` `phase-3` `milestone` `shipped` — Ricky's question, and it reordered the arc: *"we will need a list of work to be done with actual actionable items."* M4's plan ranked correctly and read uselessly — its top item was **"Question content is marked up as such · 14.5h"**, which is the audit rule's title and an hours figure. Nobody on the team could pick that up and start. It now reads **"Add FAQPage, QAPage or HowTo JSON-LD to the 147 pages listed on this item, mirroring question-and-answer content that is already visible on each page"** — with all 147 URLs on the item, two steps telling the specialist to check each page actually has visible Q&A first, and a finish line: *the next audit run reports `aeo.faq_schema` passing on all 147*. **Every one of the 13 items in the approved plan carries an action, its complete target list and its acceptance criteria; 12 of the 13 are decidable by a machine with no human judgement, across 769 named target URLs.** The instructions are **authored in code, one per rule**, for the same reason the effort estimates are — a model-written action reads differently every month, so an item nobody had finished would look like new work, which destroys the one thing a stable `source_key` exists to answer: *we proposed this in July and it is still not done*. **Verified by:** plan `70337c95` read straight out of Postgres — `status=approved`, approved 2026-08-14 01:01:46Z by `ricky@coreshifthq.com`, **13 of 13 items approved and attributed, 13 with an action, 13 with acceptance criteria, 13 with targets, 0 unauthored, 0 blank rationales, 0 without a source**; 97 tests passing across four suites (12 detector, 38 plan, 23 prompt, **24 new action**); typecheck and build clean; working tree clean at `754e70d` on both `dev` and `staging`; staging live on the new bundle `index-CY2XdO6r.js`, `/health` 200 and the plan endpoint 401 unauthenticated. Method in `docs/planning.md`, *"From a ranked finding to a job"*. (Trap found on the way, and it is the card below this one: turning findings into instructions is what exposed that the plan had been quoting 147 pages of work as 25.)

- **Ricky signed in and passed a human gate for the first time — 72 seconds, start to finish** `M5` `finding` `infra` — Worth recording because two documents claimed it had already happened and `auth.users` disagreed. The M4 handover said the staging login "works for you"; the Postmark card said the magic-link sign-in was Ricky's. Both were wrong — the table held exactly one row and it was Mal's. Checking it before writing the M5 handover caught it, and the sequence that followed is on the record: row created **01:00:34Z**, signed in **01:00:50Z**, plan approved **01:01:46Z**. Postmark delivered, the `coreshifthq.com` allowlist worked against the deployed `/auth-config` rather than against the source, and every human gate in the system — `work_items.approved_by`, `reports.reviewed_by`, `ai_probes.reviewed_by` — now has a second real identity to point at. (The near miss: the fastest way to land M5 was to write the approval as Mal locally. That would have put a named colleague's signature on a plan he had never seen, in an immutable table, in the same record the M4 board card celebrates as a real human passing a real gate. The two-minute path was the honest one and it was also the better one — Ricky now has a row he did not have before.)

- **The plan was quoting 147 pages of work as 25 — and nothing in it could have shown you** `M5` `finding` — The single most dangerous thing found this milestone, and it was invisible by construction. `findings.affected_urls` is capped at 25 URLs, which is correct for an evidence blob and wrong the moment that list becomes an instruction. Effort is `base + perPage × affected pages`, so **every page-level finding in Storepro's plan was being estimated and ranked as though it affected 25 pages.** The real numbers are 35, 39, 41, 55, 62, 70, 91, 94, 107 and **147**. `aeo.faq_schema` moved from 14.5h to 18h on that correction alone and the plan's total went from 234.1h to 268.5h **with no new work found**. An item that said "fix these pages" and listed 25 of 147 would have been short by 122 in the one way the person picking it up cannot detect. **The fix is not a bigger cap.** `page_snapshots` already retains every field of the crawl — the named columns hold most of them and a `signals` blob holds the rest, stored in M2 precisely so a question asked later could be answered without a re-crawl. So the crawl is reconstructable, and the full failing set is recovered by running **the audit's own predicate over it** — the same function object the scoring pass uses, not a second copy that could drift. **Verified by:** the recovery independently reproducing the audit's own `pagesFailing` count on all nine recoverable rules, and a self-check that discards any recovery not containing every URL the audit already recorded — a recovery that disagrees with the run it claims to expand loses to the run. ⚠️ **This bumped the ranking method to `2026-08-b`**: positions from M4's plan are not comparable with these, and the panel says so.

- **Where the full list cannot be recovered, the item says so instead of guessing** `M5` `method` — The counterpart to the recovery, and the reason it is trustworthy. `obs.stale_content` is derived from sitemap `lastmod`, which is run-level data and is not retained per page snapshot — so there is no honest way to expand its list. Its item reads **"Review all 126 affected pages (25 listed on this item)"**, names the reason in the same breath, and the plan's own totals carry the count of items in that state. Same rule the rest of this system runs on: *couldn't measure* is never *measured zero*, and a caveat on the item beats a confident wrong number. One of thirteen items is in this state today.

- **"Done" is now something a machine decides, not an opinion** `M5` `method` — Wherever an item fixes a rule the audit already runs, its acceptance criterion is **that rule passing on the target pages in the next audit run** — stored structurally as `{ruleKey, mode, scope, passRate}` rather than as prose, and indexed. Twelve of Storepro's thirteen items are checkable this way with no human judgement at all. This settles arguments, but that is not why it was built: **M6 needs it.** A prediction is "we expected X because we did Y", and Y has to be a *finished* action — an acceptance criterion a machine can evaluate is what makes "finished" a fact rather than a claim. Where nothing can check it the criterion says `manual` and means it; a manual check labelled as a machine one would be the unenforced checklist this whole product exists to replace, arriving through a different door.

- **The actions are authored in code, for the same reason the effort estimates are** `M5` `method` — 29 instructions written by hand — one per scored check and one per observational rule — rather than generated per run. A model-written action would read differently every month, so **an item nobody had finished would look like new work**, which destroys the one thing a stable `source_key` exists to make answerable: *we proposed this in July and it is still not done*. A rule with no authored action gets a default that says so on the item and in the plan totals, exactly as an uncalibrated effort estimate is labelled `default estimate` — a silent generic sentence would let a gap pass for an instruction. `npm run test:actions` fails if any rule the audit can emit has no action. **Three refusals worth knowing:** an under-sampled probe is told to *measure*, never to write content, because a confident content instruction on one sweep is a confident action on a coin flip; a rankable probe item **names no page**, because the probe records that the client was absent from an answer and not which page should have been there, so "name the page first" is step one; and the traffic anomaly is an **investigation with a written deliverable** rather than a fix, because M3 measured Storepro being cited at source 1–2 in four of those AI Overviews and still getting no clicks — the obvious answer is known to be wrong there at least some of the time.

- **All five AI engines are live, and Storepro is cited on nearly all of them** `M4` `finding` — ChatGPT, Perplexity, Gemini, Claude and Google AI Overviews all answering. A full 15-prompt sweep across every engine: **Perplexity 14/15, Claude 14/15, Gemini 7/8, ChatGPT 13/15, Google AI Overviews 7/10.** Eleven of the fifteen prompts are cited by *every* engine that answered. **This generalises the M3 finding well beyond AI Overviews** — Storepro is cited nearly everywhere a buyer might ask, and still gets no clicks. 🤔 **Google AI Overviews is the outlier**: it misses Storepro most often *and* it is the surface that keeps the searcher on the SERP. The assistants cite them more readily and send traffic. (Trap found on the way: Gemini's pinned model was retired with a `404 no longer available to new users`, while `ListModels` still advertised it — "the API says it exists" is not evidence a key can call it, which is why `verify:engines` makes a real call.)

- **The prompt set was measuring one product line six times** `M5` `method` — The generator deduped on the *templated prompt* rather than the query, so `amr in warehouse`, `warehouse amr` and `amr warehouse` became three prompts; `automated racking system / systems / solutions` became three more. **6 of 15 were near-duplicates.** Rebuilt to canonicalise the query first and pool impressions per concept: **26 raw queries now collapse to 15 distinct concepts**, and the freed slots surfaced product lines that had never been tested at all — cantilever racking, warehouse racking, pallet racking, adjustable racking. The `amr` concept alone pooled four variants carrying **3,952 impressions**, which had been invisible while split three ways. Also fixed: the set now covers **awareness / consideration / commercial** rather than asking "What should I know about X" fifteen times, and a query that is already a question is no longer re-templated into *"What is what is a mezzanine floor"*. **Verified by:** 23 offline tests, each named after a real failure in the live set, plus a dry run against Storepro's own Search Console data. 14 superseded prompts were **deactivated, never deleted** — all 240 probe runs stay valid, including the six behind the mezzanine finding.

- **Postmark wired — the tool is no longer single-user** `M8` `infra` — The Supabase project had no SMTP and a send limit of **2 emails per hour to org members only**, which was the hard blocker on handing the tool to the team. Postmark is now configured (`smtp.postmarkapp.com:587`, sender `hello@growthpartners.co.nz`) and the limit raised to **100/hour**. **Verified by:** an SMTP handshake that authenticated and had the sender accepted (`250 2.1.0 Ok`) without sending anything, then a real magic-link sign-in — on `mal@growthpartners.co.nz` at 2026-08-13 23:49:48Z. ⚠️ **Corrected 2026-08-14:** this card previously said the sign-in was Ricky's. `auth.users` holds exactly one row and it is Mal's; Ricky has never created one. The delivery claim stands — a magic link was sent and completed — but not the identity, and the M4 card two rows down was right all along that Ricky's address has never signed in. Configured through the Management API touching only the SMTP fields, so nothing else in the auth config moved.

- **M4: Work plan — six lists became one ranked month, and a specialist signed it off** `M4` `phase-2` `milestone` `shipped` — The piece none of the seven surveyed repos has. The audit, the AI-citation panel and the Search Console data spine each produced their own list sorted by their own idea of severity; a specialist holding all three had to merge them by hand every month, so the merge was never written down and never the same twice. Now one ordered list comes out, each item carrying the sentence that will appear in the client's report and the arithmetic that put it where it is. **Storepro's first plan: 15 items, 234.1 hours, 3 of them inside a 12-hour retainer and 12 below the line and still listed** — because a plan cut off at the capacity line reads as though it were everything that mattered. Ranking is value per hour: how much of the audit score is at stake, multiplied by how much of *this client's own traffic* the fix touches, discounted by how much the observation can be trusted, divided by declared effort. Prerequisites are a hard ordering rather than a weight, so structured data can never be scheduled before the content it describes exists in the HTML — a big enough score cannot override it. **Verified by:** plan `62ff1c2c` read straight out of Postgres — `status=approved`, approved 2026-08-13 03:55:08Z by `mal@growthpartners.co.nz` (session opened 03:54:31Z), 15 of 15 items approved and attributed, **0 with a blank rationale, 0 without a source**; the only code that can write `approved` is the gated HTTP endpoint at `api.ts:548/567`, so no CLI could have produced this; 33 ranker tests and 12 detector tests passing; audit determinism unchanged (91.50 / 93.44 / 45.45, identical across 23 checks); staging serving `index-DGbLikr7.js` and returning 401 to both new endpoints unauthenticated. Commit `6854acd`. Method and every constant in `docs/planning.md`.

- **A specialist who is not Ricky signed in and passed a human gate — the first time that has happened** `M4` `finding` `infra` — Not a planned outcome of M4, and it moves M7. The **only** row in `auth.users` is `mal@growthpartners.co.nz`, created 03:53:40, signed in 03:54:31, approved at 03:55:08. Ricky's own address never created a row, which also corrects the M3 handover's claim that "the staging login works for you" — it had never actually been exercised by anyone. ⚠️ **This is one observed delivery, not a solved problem.** The documented ceiling on the built-in Supabase sender (2 emails/hour, org members only) has not been shown to have changed, and the Auth email config is not readable through the Supabase MCP, so whether a provider was wired is unknown from here. It does mean M7's "a specialist other than Ricky" is closer than the M3 handover assumed.

- **Four guards put in the database rather than in whichever code path writes the row** `M4` `method` — Each one is a specific way the plan could have started lying, and each was proved by *attempting the illegal write* and watching Postgres refuse it (nine attempts, all rolled back, nothing left behind). **(1)** `unique (plan_id, source_key)` — the audit deliberately reports a missing-content problem twice, once as the scored check that counts it and once as the observational finding that names the cause. Both are right as findings; as *work* they are one job, and billing a month for both is how a retainer gets double-spent on a single fix. A merge bug is now a write failure instead of a duplicate nobody notices. **(2)** A probe-derived item must carry the sample size it rests on, and **fewer than two answered sweeps cannot be ranked** — because `rationale` is `not null` and reaches the client verbatim, so an item built on one sweep would put an authored, confident "why" on top of a coin flip. It is still *recorded and shown*, because a citation gap is worth a specialist's attention before it is actionable. **(3)** An approved plan must name its approver and its time. **(4)** One draft per client, and re-ranking supersedes rather than edits — the plan a specialist approved has to stay recoverable for the report that cites it.

- **Three bugs that only a live client could have surfaced** `M4` `finding` — All three were invisible to reasoning and to the unit tests, and all three now have regression tests named after them. **(1) Partial audit runs were excluded**, so no audit finding could *ever* have entered a plan for a site larger than the crawl budget. Storepro is 147 of 202 pages; partial is the *normal* outcome and the scoring method was built for it. The first run of the ranker returned "no usable audit run" against a client with two good audits. **(2) A probe about a single search query inherited the entire property's traffic** — demand was keyed off "does this item name any URLs", and a probe names none, so three two-sweep coin flips outranked every audit finding. Scope is now declared by the source (site / pages / query) instead of inferred. **(3) `writePlan` inserted before superseding**, the opposite of what its own comment claimed, and hit the unique index on the second run — which then exposed that 0006's two constraints were mutually unsatisfiable in either order (see the Parking Lot).

- **"Absent from the latest month" is not "fixed"** `M4` `method` — Storepro's page-one-no-clicks pattern is in **June**, not July, and two of those queries are missing from July's rows *entirely* rather than present-and-improved. Search Console returns top-N query rows, so a query can leave the list by improving or by being crowded out, and treating the second as a fix is schema rule 4 broken — the exact flaw that made every ranking loss structurally invisible in the surveyed rank tracker. The plan now reports those queries as unmeasured and names them. (This is also why the metrics source contributed **0** items to an otherwise 15-item plan: it ran, found nothing in the latest complete month, and said so. Flagged to Ricky as a judgement call on "merged from every finding source" before he approved.)

- **M3: AI visibility panel — Storepro is cited in Google's AI Overviews, and we can prove it** `M3` `phase-1` `milestone` `shipped` — The thing no surveyed competitor does: we now ask a real AI engine a real buying question and record whether the client was cited, with the engine's full answer stored. **Storepro is cited in 6 of the 9 AI Overviews that rendered — usually as source 1 or 2.** **Verified by:** 75 probe rows queried straight out of Postgres (10 answered with raw answers 1,991–3,595 characters, 5 `engine_error`, 60 `not_configured`), cited URLs pointing at real Storepro product pages, 12 detector unit tests, `tsc` clean, the panel rendering all five cell states, and staging redeployed and gated. Commit `f170cfc`.

- **The Storepro question is answered — and the answer is not the one we expected** `M3` `finding` — M1 hypothesised that AI Overviews were absorbing Storepro's clicks. Half right, and the half that's wrong changes the recommendation completely. Cross-referencing July Search Console against live AI Overviews: of the **7 page-one queries carrying 3,005 impressions and exactly 0 clicks**, six have an AI Overview — **and Storepro is cited in four of them, at source 1 or 2.** So Storepro is not being left out of the AI Overview. **It is inside the AI Overview and getting no clicks anyway**, because the overview answers the question and the searcher never leaves the SERP. "Get cited in AI Overviews" is not the fix; Storepro is already there. ⚠️ **Two honest caveats:** `heavy duty shelving` sits at position 9.0 with 473 impressions, 0 clicks and **no AI Overview at all**, so this does not explain every case; and the AI Overview snapshot is August while the traffic data is July, which is a correlation across two different moments, not a controlled test.

- **DataForSEO is live — and Backlinks is entitled** `M3` `infra` `shipped` — Open since M2 and blocking two features. Root cause was the stored password being the website sign-in password rather than the separately-generated API password. Diagnosed by differential testing — four requests side by side showed our credentials returning byte-identical errors to a deliberately fake account, which ruled out everything except rejection. Storepro: **91 referring domains, 280 backlinks, rank 172**. Also shipped `npm run fix:dataforseo`, which takes a pair, tests it against the real call, and writes it only if it authenticates.

- **Near miss: the first re-judge would have deleted a correct citation** `M3` `finding` — The design claim was that storing the engine's full answer means a better detector improves the whole history rather than only the future. Testing it revealed the claim was false as built: an engine's **structured source list is separate data from its answer text**, and DataForSEO returned `storepro.co.nz` in the AI Overview's `references` array without it appearing anywhere in the visible markdown. Re-judging from the answer text alone therefore dropped it and flipped a true citation to false. Fixed by storing the engine's own source list untouched, and by having the re-judge **refuse** any run predating that column rather than silently downgrading it. *(It was caught only because the command writes nothing without `--write` — the dry-run default paid for itself the first time it ran.)*

- **Found: our own data vendor was the most-cited domain** `M3` `finding` — First share-of-voice table put `api.dataforseo.com` at the top with 90%, ahead of the client. Those are CDN image URLs DataForSEO rewrites into the AI Overview markdown when it captures the SERP — embedded thumbnails and video stills being read as citations. Embedded assets are not sources. *(The general shape, for the third time this project: the tool produced a confident, precise, wrong number rather than a missing one.)*

- **The tool is finally on the internet — behind a login** `M3` `infra` `shipped` — Flagged at M1, carried through M2, and now done. **https://engine-optimization-staging.up.railway.app** auto-deploys on every push to the `staging` branch. The gate landed *before* the deploy rather than after it, because the tool holds four months of a real client's Search Console and analytics data and a public URL changes what "unauthenticated" costs. Sign-in is a Supabase magic link restricted to `growthpartners.co.nz` and `coreshifthq.com` — **no password is set, stored or handled by the app at all**. The service-account key moved to a Railway secret at the same time. **Verified by:** `/health` 200, `/api/clients` 401 unauthenticated, 401 on a forged token, the sign-in screen rendering on the live URL, and a push to `staging` triggering a build that deployed clean. *(Identity is a real user row, not a shared password, because `reports.reviewed_by`, `work_items.approved_by` and `ai_probes.reviewed_by` are all foreign keys to it — a shared secret would make every human gate in the system anonymous.)*

- **Found: the probe table could not record a failed probe without lying** `M3` `finding` — Caught before a single probe ran. `ai_probe_runs` shipped in M1 with `raw_answer` and `cited` both mandatory, sitting next to an optional `error` field. Those cannot all be true at once — an engine that times out, rate-limits or has no key has no answer and no verdict, so the *only* way to store it was `cited = false`. That is the one rule this whole schema is built around, broken by accident: **"couldn't measure" becoming "measured zero"**. An unreachable engine would have been indistinguishable from an engine that answered and ignored the client, and every citation rate built on top would have counted vendor outages as client losses. Now three explicit states, enforced by a database constraint rather than by whichever code path happens to write the row. *(Same failure this project has now hit three times — it was broken twice in M2's first run too. It is the most easily broken rule we have.)*

- **The AI visibility pipeline — five engines, and a citation detector that can be re-run on history** `M3` `phase-1` `shipped` — Adapters for ChatGPT, Perplexity, Gemini, Claude and Google AI Overviews, each queried **with web search on** and each pinned to a specific model — because "ChatGPT" in September is not the same system as "ChatGPT" in October, and without recording which model answered, a citation drop may be entirely a vendor swap. **The detector separates two things every competitor conflates:** an engine writing "Storepro supply racking" has *mentioned* the client; an engine offering storepro.co.nz as a source has *cited* it. Only the second sends traffic and only the second is verifiable from the answer — a brand name in prose may have come from the model's weights. The detector is versioned like the audit's method version, so improving it doesn't look like a client losing citations, and because the engine's full answer is stored, a better detector **improves the whole history rather than only the future**. **Verified by:** 10 offline unit tests covering lookalike domains, prose-only links, brand word boundaries and the no-cohort-declared case; `tsc` clean; the panel live on staging.

- **Storepro's prompt set — 15 questions, taken from what buyers actually searched** `M3` `shipped` — Derived deterministically from Storepro's own July Search Console queries, brand terms excluded (an engine asked "Storepro" will find Storepro — that measures nothing). Search queries are rewritten into the shape a buyer would actually type into an assistant, using fixed templates rather than a model, because a set that reshuffles monthly makes a *lost* citation structurally invisible — the exact flaw in the rank tracker we surveyed. **Stored as unreviewed**, with the review gate as a real column the report reads, not a note. *(Worth knowing: the derived set is visibly mediocre — 15 near-identical "what should I know about X" prompts. That is the review gate earning its place on day one, not a bug. Ricky's list of real buyer questions replaces it.)*

- **Caught in our own code: a threshold that manufactured a signal** `M3` `finding` — The filter picking which queries to probe first originally flagged anything at position 5–20 holding impressions with no clicks. That surfaced a query averaging position 17.7 as "anomalous" — but zero clicks at position 17 is arithmetic, not a mystery, and treating it as one invents a finding out of ordinary page-two behaviour. Tightened to page one only. **The tightening is what surfaced the real result**: with the loose threshold the standout was position 8.5; with the tight one it is position **1.5 and 2.3 with zero clicks**, which is a far harder thing to explain away.

- **M2: Audit engine — a real client site, crawled, scored and explained** `M2` `phase-1` `milestone` `shipped` — Storepro's site is now crawled, scored on three pillars, and the result is on screen with every page and every finding behind it. The audit reads pages the way an AI crawler does — fetching the raw HTML without running any JavaScript — because the gap between what a browser renders and what arrives in the response *is* the problem we're selling the fix to. **Real numbers:** SEO **91.50**, GEO **93.44**, AEO **45.45**, from 147 pages crawled of 202 discovered, producing 150 page snapshots and 12 findings. **Verified by:** two audit runs queried straight out of Postgres, both under method `2026-08-a`, both scoring identically to the hundredth; `GET /api/audits/…` returning 200 with 12 findings and 150 pages; screenshots of all three screens (findings, checks, pages); `tsc` clean; Supabase security advisors clean; commit `68a5737` on `origin/dev`. *(Same trap as M1 and still open: "in the app" means the local build. There is no deployed environment, so nobody outside this laptop can see any of it.)*

- **Scores that don't drift — and a command that proves it rather than claiming it** `M2` `method` `shipped` — The risk carried into this milestone was that two of the tools we surveyed produce audit scores that wander ±10 points on a site nobody touched, which would make every month-over-month claim we build on top fiction. So the scoring was designed backwards from that threat: enumerate every way an unchanged site could score differently twice, then close each one. The ones that mattered — the page set is decided from the sitemap and sorted the same way every time rather than by whichever page answers first; **no rule is allowed to look at today's date**, because a "content updated in the last 90 days" check makes an untouched site score lower every month and the report would call that a regression; a page we couldn't read is dropped from the calculation instead of counted as a failure; and the ruleset is stamped with a version, so if we ever change the weights the app refuses to draw a comparison across the change rather than letting a methodology tweak look like a client result. **Verified by:** `npm run audit -- --client storepro --verify` crawling the site twice and reporting identical results across all 23 checks, plus two separately stored runs both recording 91.50 / 93.44 / 45.45. Reasoning for every rule is written up in `docs/scoring.md`.

- **Found: the audit was confidently wrong twice on its first real run** `M2` `finding` — Both caught and fixed, and both worth knowing because they produced *wrong* answers rather than missing ones, which is the more dangerous failure. **First:** three Storepro URLs return HTTP 200 with a completely empty body. The engine treated them as real pages failing every single check — inventing a shortfall out of an absence, and dragging the scores down with it. A page now only counts as content if an actual HTML document came back and parsed. **Second:** Storepro's team page has an empty `<main>` container with the real content rendered outside it, so the audit read the page as having zero words and reported a perfectly normal page as invisible to AI crawlers. The audit now checks that the section it's reading actually contains text before trusting it. *(The general lesson, and the reason both are recorded: a tool that says nothing is recoverable; a tool that says something false with a number attached is not.)*

- **Storepro's audit baseline — strong technically, weak as an answer source** `M2` `finding` — SEO 91.5 and GEO 93.4 are genuinely good: the sitemap is clean, structured data is present, an `llms.txt` exists, and **no AI crawler is blocked** — all 15 tracked agents including Google-Extended can read the site. AEO at 45.5 is the gap, and it's specific: **not one page** carries FAQ or How-To markup, only half the pages have a heading phrased as a question, and only a third have a list or table an answer engine could lift. **Why that matters here:** it is exactly the profile of a site that gets *read* by AI engines but not *quoted* by them — which is consistent with the M1 finding that Storepro's informational queries hold impressions at position 7–9 while returning zero clicks. Still a hypothesis, and M3's probe panel is what settles it, but the audit and the traffic data now point the same way. **Verified by:** 12 findings stored against run `54b3758e`, each with its evidence and its falsifiability contract.

- **Found: nothing had been loading the local env file since M1** `M2` `infra` `finding` — Every command in the repo used a loader that reads `.env`, a file that doesn't exist here — the secrets live in `.env.local`. M1's commands only ever worked because they were being launched with an explicit flag pointing at the right file, which meant the documented `npm run ingest` failed for anyone following the README from a clean shell. Fixed centrally so every command loads the same way, with real environment variables still winning over files for when this eventually runs on Railway. *(Also found and killed a server left running from a previous session — it was still holding the port and serving old code, which is how this surfaced.)*

- **Near miss: the DataForSEO check would have reported a false all-clear** `M2` `infra` `finding` — The first version of the credential verifier pinged a cheap liveness endpoint before trying anything real. DataForSEO validates the shape of a request *before* it checks who you are, so that ping returned a success response on credentials that were completely invalid — and the verifier would have printed "credentials accepted" while nothing worked. It now makes exactly one probe: the real backlinks call we actually depend on. It also separates "your login is wrong" from "your login is fine but you haven't bought the Backlinks API", because those are different problems with different fixes and DataForSEO sells each API separately. *(Same shape as the M1 trap where Search Console properties list fine and then 403 on every data call. Worth assuming this pattern exists in any vendor API until disproven.)*

- **M1: Data spine — Storepro's data is in the app** `M1` `phase-0` `milestone` `shipped` — The whole spine, end to end: a Supabase project (`xslwvntwrlvqccdupmni`), a 20-table schema, Google access, ingestion, and a client view. **Verified by:** four calendar months of Storepro data (Apr–Jul 2026) queried straight out of Postgres — 8 snapshot rows, 2 sources × 4 months; `GET /api/clients/storepro` returning 200 with 4 months and correct deltas; a screenshot of the client view; `tsc` clean; commit `bd7cbe5` on `origin/dev`. **Real numbers:** July 815 clicks / 77,496 impressions / 1,264 organic sessions. *(Trap worth knowing: the app runs locally only — there is no Railway environment yet, so "in the app" means the local build, not a deployed URL.)*
- **Google access solved permanently — 65 properties, zero per-client setup** `M1` `infra` `shipped` — The blocker that had been open in the enablement log since the GHS Law build in July. Ricky created a service account (`seo-data-reader@gp-seo-data.iam.gserviceaccount.com`) and authorised **domain-wide delegation** so it impersonates `access@growthpartners.co.nz` — the account that already holds access to every client asset. **The consequence is the important bit: onboarding a client now needs nothing at all on the Google side.** Ten clients and a hundred clients are the same amount of setup work: none. **Verified by:** `scripts/verify-google-access.mjs` minting a token and enumerating **65 Search Console and 98 GA4 properties**, plus live Search Analytics queries returning real data. Deliberately *not* user OAuth — LeanSEO's refresh token died twice with `invalid_grant` and took its own re-mint script with it.
- **Found: 11 properties look like access and deliver nothing** `M1` `infra` `finding` — Of the 65 Search Console properties, 11 are `siteUnverifiedUser` — `access@` is listed on them but verification was never completed. They appear in the property list exactly like working ones, so a naive "is it in the list?" check passes and then every data call 403s. **Tested rather than assumed:** `siteOwner` and `siteFullUser` both returned 200 with real data; `siteUnverifiedUser` returned 403. Onboarding now checks the permission *level* and records it, and a 403 during ingestion is written as **revoked access** rather than dissolving into a generic failure note nobody reads.
- **Found: GA4 property IDs cannot be auto-matched** `M1` `infra` `finding` — 98 GA4 properties against roughly 10 SEO clients, including 22 matched Test/Filtered pairs. A name-matching experiment paired `hotspring.com.au` with the *Hawkes Bay* property and `unistor.com.au` with a lead-page property — both wrong, and picking a Test stream yields near-zero sessions that read as a traffic collapse. Decision: **the GA4 property ID is supplied explicitly at onboarding and never inferred**, and the onboarding script warns if the chosen property is named "Test".
- **Storepro's baseline — and a hypothesis worth testing** `M1` `finding` — First client onboarded and measured. Non-brand share **31.8%** (the warning threshold is 30%), non-brand CTR **0.13%**, and **114 queries sitting at position 5–20** with 50+ impressions. The odd part: several informational queries rank at position 7–9 with 500+ impressions and **zero clicks** — *"automated racking solutions"* at 7.0 with 521 impressions, *"what is a mezzanine floor"* at 9.8 with 509. Position 7 should return 10–15 clicks. **Impressions holding while clicks go to zero is the diagnostic fingerprint for AI Overview absorption**, and GA4 corroborates that AI engines read this site — AI-assistant sessions went 0 → 0 → 31 → 43 across the four months. ⚠️ **Explicitly unverified as a cause** — average position is an average and a SERP feature could do the same thing. It's the first thing M3's probe panel should test.
- **Schema applied — 20 tables, and the loop nobody else has** `M1` `phase-0` `shipped` — The reviewed data model from `docs/schema.md` is live: clients, properties, access state, versioned client context, immutable audit runs, per-page snapshots, findings carrying a falsifiability contract, work items with a mandatory rationale, **predictions and control sets**, cycles, metric snapshots, tracked keywords, AI probes storing raw answers, reports with a real review-check table, and the algorithm-update ledger. RLS enabled on every table with staff-only policies; Supabase security advisors return **clean** (one warning fixed on the way — the `updated_at` trigger function was callable over the REST API and had its execute grant revoked).
- **Ingestion built on rules that came from other people's failures** `M1` `phase-0` `shipped` — Every design choice traces to a specific observed failure in a surveyed tool, not to taste. **Calendar months, not rolling windows** — a rolling 28-day window anchored to capture time overlaps its own baseline and dilutes every delta. **Totals from a dimensionless Search Console call** — Google anonymises low-volume queries, so summing query rows under-reports; for Storepro's July, 536 of 815 clicks came from queries too small to list, and the app says so on screen. **Both property forms tried** — `sc-domain:` and URL-prefix are different objects with different data, and Storepro has only the URL-prefix form. **"Couldn't measure" never becomes "measured zero"** — a failed source writes no snapshot and records why. **A month can't be ingested until it settles** — Search Console lags ~2 days, so the tool refuses to ingest a month before the 3rd of the next one rather than recording an incomplete month as final.
- **Project registered + seven milestones** `phase-0` `milestone` — Board, config and handover created; `.coreshift-project` written so every future session in the directory auto-orients. M1 Data spine → M2 Audit engine → M3 AI visibility panel → M4 Work plan → M5 The loop → M6 Monthly report → M7 Handover to the team. Each `doneWhen` is a single observable event.
- **Reference research: seven codebases surveyed** `phase-0` `research` — Read in full: `coreyhaines31/marketingskills`, `AgriciDaniel/claude-seo`, `AgriciDaniel/claude-blog`, `zubair-trabzada/geo-seo-claude`, `aaron-he-zhu/aaron-marketing-skills` (contract layer + 16 SEO/GEO skills), and Coreshift's own `leanseo`. Verdict: they collectively supply ~60% of an agency SOP and 0% of the system. Best material is Apache-2.0 — fork, adapt, use commercially, strip the trademarks.
- **Key finding: nobody closes the assumption loop** `phase-0` `research` — Grepping every surveyed codebase for prediction/assumption logic returns essentially nothing. LeanSEO's only hit is the string `"expected impact"` inside a prompt, written to an untyped blob and never read back. Recording what we expect and checking it is entirely net-new, and it's the product.
- **Key finding: no tool measures AI visibility** `phase-0` `research` — Every repo measures GEO/AEO *readiness* and calls it AI visibility. Not one queries ChatGPT, Perplexity or AI Overviews to check whether a client is actually cited. The single keyless probe available measures Tavily, not the engines clients care about.
- **Correction: CORE-EEAT 80 / CITE 40 are item counts, not pass marks** `phase-0` `research` — LeanSEO's quality-gate doc treats them as thresholds. They're dimension counts (8×10 and 4×10). Real pass mark is 75, with one verified veto capping the score at 59 and two vetoes blocking outright with no score emitted.
- **Architecture decided** `phase-0` `milestone` — Web app on Railway + Supabase, since specialists won't use a terminal. Agent work runs server-side via the Claude Agent SDK. The method lives in versioned, portable skill files the server loads at runtime and we can also run in Claude Code while building. Schema first, before any screen.
- **Automation posture decided** `phase-0` `decision` — Everything unattended: crawls, GSC/GA4 pulls, AI probes, snapshots, delta analysis, plan and report drafting. Human gate before any site change and before anything reaches a client.
- **Data sources chosen** `phase-0` `decision` — DataForSEO for backlinks (cheapest per-call, right at 10 clients; revisit at 100). Build our own multi-engine AI-citation panel rather than buying a monitor — it's the differentiated asset and it feeds the history corpus that becomes the moat.

## 🔵 This Week

- **🔴 Our own concurrent work is not a confounder anywhere in this system** `M11` `M12` `risk` `method` — Found on 2026-08-19 while deciding to bring M12 forward, and it is created *by* that decision. `server/verify/verify.ts` assembles `predictions.confounders` from exactly two sources: the prediction's own shipping drift, and **verified Google algorithm updates** overlapping the window. Nothing anywhere considers *other work we shipped ourselves* inside the same window on the same pages. It has never mattered because Storepro is at **0 shipped**. It starts mattering the moment Mal's September cycle ships anything touching `a3e5a8a7`'s **twelve control-matched pages** — October then measures two changes and the verdict comes back clean-looking and wrong, which is worse than `confounded`. ⚠️ **The control set does not save us**: the control is twelve *untouched* pages, so work landing on the treated set inflates the delta and work landing on the control set deflates it, and either way the number is not about the FAQ item. Two cheap outs, both stated rather than built: have the September plan avoid those twelve URLs (they are on the prediction, they can be read), or make the verdict name any `work_items` row with a `shipped_at` inside the window whose targets intersect the prediction's scope. **Decide before Mal's plan is approved, not after it ships.**

- **Build the next plan with no declared capacity, and say why on the plan itself** `M11` `method` — The implementation half of the decision above, and it is one omitted flag plus some wording. Today a plan built without `--capacity` simply stops marking items, which reads as *nobody told it the number* rather than *this is deliberate*. The plan should say in as many words that the ranking is a **relative-cost order** and the month's scope is the team's to draw — otherwise the next person to read it adds the flag back. **Do not apply it to `70337c95`** (see the decision card). ⚠️ `compose.ts:633` writes a limits paragraph about predictions sitting below the capacity line, gated on `capacityDeclared` — check what the report says when nothing is declared, because *no limit stated* and *no limit* are different sentences and this product exists to keep them apart.

- **A shipping date is recorded and displayed in UTC, which reads a day behind in New Zealand** `M11` `method` — Surfaced the moment a real person pressed the button: Ricky recorded a shipment on the morning of **19 August** NZ time and the record reads **2026-08-18**, because everything downstream of it is UTC. That is not a bug to convert away. `server/predict/windows.ts` is UTC and clock-free by design, and the drift check compares a UTC shipping date against a UTC deadline — showing the NZ date in the panel while comparing the UTC one would put two dates from one row on the same screen, which is worse. **The fix is to label it, not to shift it.** ⚠️ **There is a real bounded edge underneath the display issue.** A shipment made at 1am NZ on 4 September is 13:00 UTC on 3 September, so it lands inside the deadline by UTC and outside it by local reckoning — up to twelve hours of slack in the wrong direction, at the boundary only. It cannot mis-order anything else, and it only ever matters for a shipment landing within half a day of a deadline. 🤔 **And the deeper question it opens, which is not this card's:** the readback month is a Search Console calendar month, and Search Console reports in the property's own timezone rather than UTC. That has been true since M1 and nothing has ever depended on it to the day. It starts mattering the first time a window boundary is argued about. Worth costing before M11 verifies anything.

- **Nothing checks that an authored action's branches leave its acceptance criterion decidable** `M11` `method` — The general form of the item-1 finding above. `npm run test:actions` already proves every rule the audit can emit has an authored action and an acceptance criterion; it does not prove the two are *compatible*. Item 1's action permits removing pages from the sitemap, which makes its own criterion return `not_assessed` forever. The check is cheap where it belongs — alongside the existing action-coverage tests — and the rule it would encode is one sentence: **an action must not offer a fix that makes its own acceptance criterion unobservable.** Worth doing before a second client multiplies the authored actions, not after.

- **Subdomains split one competitor into two rows** `M6` `method` — Surfaced by the v2 cohort: `automate-x.nz` is cited in 24 answered runs and `offer.automate-x.nz` in one more, and they are reported as two separate competitors. The *matching* is right — a subdomain of a declared competitor is that competitor — but the detector reports the host it found rather than the domain that was declared, so share-of-voice would show one rival twice. Small, and it distorts a client-facing number, so it should be fixed before the report milestone rather than after.

- **One sweep is not a measurement — needs a sampling design** `M5` `method` `risk` — The biggest thing M3 found, and it is a limitation. Two full sweeps of the same 15 prompts **minutes apart** disagreed on **5 of 15**: three where the AI Overview appeared or vanished entirely, one where a citation flipped, one where the rank moved from 2 to 4. This is the same threat the audit scoring was designed backwards from — a number that moves on its own makes every month-over-month claim fiction — except **the audit's drift sources were ours and could be closed, and this one is Google's and cannot be.** Until a sampling design exists (n sweeps over a window, reported as a rate with its sample size), no citation delta may be shown to a client. Belongs in M5 with predictions and controls, because the answer is a confidence interval, not a bug fix. ✅ **The M4 half shipped.** M4 stamps a mandatory `rationale` on every work item and that field becomes the report's "why" — so a work item derived from a single sweep would put a rationale on a coin flip, which is precisely what this product exists to prevent. A probe-sourced work item now carries its sample size and n<2 cannot be ranked — enforced by a check constraint, and three of Storepro's approved items are discounted to confidence 0.4 on n=2 because of it. The sampling *design* stays here, and when it raises n these items rise on their own with no re-tuning.
- **The other four engines** `M4` `blocked-on-ricky` — ChatGPT, Perplexity, Gemini and Claude adapters are written, typechecked and waiting; each switches on the moment its key appears in the env. One engine is a data point, four is a panel — and ChatGPT is the one a client will ask about by name.
- **CITE port + per-client competitor cohorts** `M4` `method` — Now unblocked: DataForSEO is live and Backlinks is entitled (Storepro: **91 referring domains, 280 backlinks, rank 172**). The manual part still doesn't automate away — each client needs a locked 3–5 competitor cohort declared by an analyst, and until Storepro has one the panel correctly reports competitor presence as *not measured* rather than guessing.

- **The M3 "citation flipped" data point may be a detector artefact** `M5` `method` `risk` — Found while wiring the probe source into the plan. M3's two sweeps straddled a detector change: the first 10 answered runs are `2026-08-a` with an **empty** `engine_sources` array, every later run is `2026-08-b` with 8–14 structured sources. The AI-Overview-appeared-or-vanished changes (3 of the 5) are presence changes and stand regardless. But "one citation flipped" could be the detector rather than Google, and it cannot be settled by re-judging — `rejudge` correctly refuses those runs because re-reading prose alone would drop references the engine returned. **This does not overturn the M3 finding; it weakens one of its five data points.** A clean n≥3 sweep under one detector version settles it, and the M5 sampling design produces that anyway.
- **Effort estimates have never been calibrated — and M5 raised the stakes** `M6` `method` `risk` — The plan's hours are declared constants in `server/plan/rank.ts`, labelled as estimates everywhere they appear. Nothing has checked them against how long the work actually took. `work_items.shipped_at` makes it answerable after two or three cycles — and as of M8 it can actually be written, so the clock on that starts with the first real shipment rather than never. ⚠️ **M5 made this more pressing rather than less.** An item that names 147 specific pages and an 18-hour figure invites somebody to schedule against the number, and the target-recovery correction moved several estimates by 25% or more in a single release — which is itself evidence the constants were never load-bearing before. **Do not quote these hours to a client.**

- **The crawl budget now caps what can be called finished** `M11` `method` — Surfaced by the readback. An item naming 147 pages can only be marked finished when a run assesses all 147, and the crawl stops at 147 of Storepro's 202 discovered pages. Today that is moot — nothing has been fixed yet, so every item fails on its merits. It stops being moot the first time real work is done and the readback reports `incomplete` on an item that is genuinely finished. Two ways out and neither is free: raise the crawl budget for a readback run, or accept a pass rate below 1 on large items and say so on the criterion. Decide before the first month of work lands, not after.

- **A second client is the first real test of the matching thresholds** `M12` `method` `risk` — Five pairs, 40% concentration, ten points of divergence, five-times magnitude band. All four are *declared* constants with a written reason, and all four have been exercised against exactly one property with 50 measurable pages. They produced one prediction from thirteen items, which is the honest answer for Storepro and might be the wrong answer for a site with 500 pages in Search Console. Nothing to change yet; worth knowing that the numbers have one data point behind them.

- **Search Console's top-50 ceiling is now the binding limit on coverage, not the 25-URL cap** `M6` `method` — M5 fixed the *page* truncation and this is what is left. Of Storepro's 202 crawled pages, Search Console returns per-page rows for **50**, and only **31** appear in all four months. So a prediction can only ever be about pages inside that set — 12 of the FAQ item's 147 targets. Every prediction states its coverage in words, so it is a stated limit rather than a hidden one, but it does mean the loop measures the head of the site and says nothing about the tail. The Search Console API can be paged well past the default; whether the extra rows are worth the request budget is a real question and has not been answered.

## ⚪ Backlog

- **Port CORE-EEAT (80 items, 9 content-type profiles)** `M4` `method` — Apache-2.0, with per-item pass/partial/fail bands and calibration examples; expensive to reproduce, free to take. **Explicitly not what M2 built** — M2's 23 checks are a smaller, determinism-first ruleset designed so two runs of an unchanged site produce the same number. CORE-EEAT is a content-quality assessment and a different job; when it lands it should sit alongside the audit score, not inside it, because a judgement-based rubric will not be reproducible in the same way.
- **Port CITE (40 items, 7 domain profiles)** `M4` `method` — Peer-relative by design; each client needs a locked 3–5 domain peer cohort declared at onboarding. That's a human step, budget for it.
- **AI Overview recovery playbook** `M4` `deliverable` — Four-phase incident SOP. ⚠️ **M3 changed what this playbook has to say.** It was drafted assuming the problem is *absence* from the AI Overview; Storepro is the opposite case — cited at source 1–2 and still getting zero clicks. A playbook whose advice is "get cited" is useless to a client who already is. The real question it must answer is what a supplier does when the overview satisfies the buyer without them ever leaving the SERP.
- **Composite content-decay score** `M4` `method` — Traffic 30 / position 25 / CTR 15 / freshness 15 / competitive displacement 15, with action bands and a refresh-vs-rewrite rule.
- **A client-facing format for the report** `M11` `deliverable` — The generator landed in M7: the report is composed from the records, bound to its originating findings, and published behind a gate. What is *not* built is anything that leaves the app — no PDF, no branded template, no email. That was deliberate rather than deferred by accident: the format a client receives is a positioning decision, and the report's own wording is still being read for the first time. Worth doing once one report has been published and read.

## 🅿️ Parking Lot

- **Withdrawing a verdict does not put the prediction back in the work queue** `M10` — Found while building M10's correction path and stated in `docs/corrections.md` rather than discovered in November. The index `predictions (client_id, expected_by) where outcome is null` **is** the work queue — it is the whole reason `too_early` is never written — and a withdrawal *supersedes* rather than nulls, so `predictions.outcome` still holds the original verdict and the row stays out of the queue. Every reader in this system treats a withdrawn verdict as unanswered; the database index does not. **The route back to an answer is another correction stating the corrected reading**, not a second run of the verification writer — the writer is refused by `predictions_verdict_immutable`, correctly, and nothing here weakens that. ⚠️ It matters exactly once: the day somebody withdraws a verdict and expects the prediction to reappear on the "somebody has to come back to this" list. **The shape of the fix, when it is wanted:** a partial index that also excludes predictions whose latest correction withdraws them, which needs the correction chain readable in SQL rather than only in `readThrough` — so it is a view or a generated column, and it is a migration rather than a patch. Not worth it before a verdict has ever been withdrawn.


- **The `app-check` fixture lives in the same database as the real client** — Deliberate: the human gates can only be tested where they actually run, and every one of them writes an immutable row. It is defended by being obvious rather than by being hidden — `prospect` status, `.invalid` domain, and a name that says so wherever a client name appears. **The residual risk is drift:** anything that later iterates clients without filtering on `status = 'active'` will pick it up, and a report composed for it would be a report about nothing. `npm run fixture -- --drop` is the answer and the README carries it. Worth revisiting if a second fixture is ever needed, at which point a `clients.is_fixture` column beats a naming convention.

- **A recorded shipment has no correction path — closed by M10** `M10` ✅ — Same shape as the published report's, and taken deliberately for the same reason: `shipped_at`, `evidence_url` and `owner_id` are frozen the moment they are written, because the date is what every prediction about the work is measured against and a date that can move is a window that can be made to have been met. The cost is that an item shipped by mistake stays shipped and a mistyped evidence link stays mistyped. **The shape of the fix, when it is wanted:** a `work_item_corrections` row that supersedes rather than edits — the original stays on the record, the correction names who made it and why, and the report reads the latest — which is the same answer the report's correction path needs, so build them together rather than twice. ⚠️ **M9 made it three, not two.** A verdict is now frozen on the same grounds, with the same cost — an algorithm update discovered after a verdict cannot be added to its confounders. One design, three call sites: a published report, a recorded shipment, a written verdict. ✅ **Built once, in M10, and pressed on 2026-08-19**: correction `f2d8c709` supersedes shipment `c266bd31` and `work_items.evidence_url` still reads what was written first. What is **still** true and worth keeping here: `recordShipment` is guarded on `shipped_at is null` and the column never moves, so there is no *un*-shipping through the gate — a withdrawn shipment is re-stated by another correction, not by pressing the gate again.

- **The per-item shipping deadline reads the first recorded prediction** — The schema allows one prediction per work item *per metric*, and the shipping panel's per-item *ship by* date takes the first recorded window for that item. With one metric in use that is exact. It stops being exact the day a second metric is predicted on, and the honest surface then is a deadline per prediction rather than per item. Nothing to change until that happens; noted so it is a decision rather than a discovery.

- **`predictions.confounders` has no writer — closed by M9** ✅ The ledger exists, it takes Google-owned sources only, `verified` false is a research lead that refuses a verdict rather than being ignored, and an overlapping update is written into `confounders` and caps the decision at `keep_testing`. What is **still** true and worth keeping here: the ledger is filled **by hand**, nothing watches the Search Status Dashboard, and there is no scheduler in this system — so an empty ledger reads on a verdict as *no confounders* when it means *nobody looked*. The CLI and the panel both say when it is empty; nothing chases it.
- **`gsc_position` and the GA4 metrics are declared and unused** — `metric_kind` carries ten metrics and the mechanism table only ever points at `gsc_impressions` and `gsc_clicks`. Position is available per page and would be a better early indicator than impressions for several rules; it was left out because "lower is better" inverts the direction logic and nothing needed it yet.
- **The directive constraint has an UPDATE-shaped hole** — `0009` requires a complete directive on every item *inserted* into a plan, enforced by a `before insert` trigger. It cannot be an unconditional NOT NULL, because the approved M4 plan `62ff1c2c` has none and rewriting an approved plan's items to satisfy a constraint added afterwards would be editing history to make the present tidy. The consequence: an UPDATE could still blank a directive to `{}` and the CHECK would allow it, because the CHECK has to allow the pre-M5 rows to exist at all. Nothing writes such an update today. Closable when no unapproved pre-M5 items remain — same shape as the `0007` supersede weakening, and parked next to it.
- **An action does not check its prerequisite's targets overlap** — The plan orders `aeo.faq_schema` behind `geo.structured_data`, and the FAQ item's steps tell the specialist to check each page has visible Q&A first. Nothing verifies the two items' *page sets* actually intersect. On Storepro they largely do; on a client where they did not, the ordering would be conservative rather than wrong, which is the safe direction to be imprecise in. Worth doing when a second client makes it observable.
- **`obs.stale_content` targets could be recovered by storing sitemap lastmod per page** — It is the one rule whose full affected list cannot be re-derived, because `lastmod` is sitemap data and page snapshots hold page data. One column on `page_snapshots` fixes it at the next audit run. Not worth a migration on its own; fold it into whatever next touches the crawl.
- **Work item owners — half closed by M8** ✅ `owner_id` is now written, and it is *required*: a shipment carries the identity of whoever recorded it, from the verified session, or it is refused. What is still not built is **assignment** — nobody can be given an item in advance, which is the M12 question alongside per-client scheduling. M7's report attributes work to the finding that produced it and never to a person, and that stays true.
- **Capacity derived from the retainer** — `clients.monthly_retainer_nzd` is on file, but hours-per-dollar is a pricing decision and not the ranker's to invent. Capacity stays an explicit input until Ricky sets a rate.
- **Cross-source fix detection** — An AI-probe observation and an audit finding may share a fix. Deciding that automatically means guessing, and a wrong guess deletes real work from the month, so the merge only collapses declared overlaps. If a pattern emerges across clients it could be worth revisiting with a declared mapping rather than fuzzy matching.
- **Restoring the strict supersede constraint** — `0007` weakened "every superseded plan names its successor" to "a successor pointer may only appear on a superseded plan", because the strict pair was unsatisfiable in either order and Postgres cannot defer a CHECK. The forward guarantee now lives in `writePlan`, which is weaker. A single SQL function doing the whole supersede would put it back in the database.
- **Performance / Core Web Vitals as a fourth pillar** — Deliberately left out of the audit score in M2. Measuring it properly needs a rendering engine and real-user field data, and both would put a number that moves on its own into a client-facing score — the exact failure the scoring method was built to prevent. If we want it, it belongs in its own pillar with its own comparability rules.
- **Rendered-DOM comparison** — Right now we infer that a page needs JavaScript from what's missing in the raw HTML. Running a headless browser would measure the raw-versus-rendered gap directly and would be a genuinely better diagnosis. It stays out of the *score* even then: a headless browser is the least reproducible thing we could put in one.
- **Content production pipeline** — Briefs, drafts and publishing are a separate concern. Work items can reference them by URL until we know we need more.
- **CMS write adapters** — Applying changes directly to client sites (WordPress, Shopify, Webflow, git PR). Approval-gated, and not needed before M7.
- **Multi-agency tenancy** — Built for Growth Partners as a single agency. If Coreshift needs an isolated instance, that's a column plus RLS. Cheap to add later, premature now.
- **API cost accounting** — At 100 clients, per-client API spend becomes real and billable. Worth a table before we get there.
- **Local SEO / Google Business Profile** — Not in scope until a client needs it.
- **Calibration study** — After ~12 months we'll have change/prediction/outcome data across every client. That corpus is the moat: it lets us answer "do refreshes on position 8–15 pages actually beat control?" from our own evidence rather than quoting unsourced blog percentages.
- **Re-issuing a published report — closed by M10, and deliberately not a re-issue** `M10` ✅ — One published report per cycle, enforced by a partial unique index, and a published report cannot be edited or deleted. That is the right default: two documents both claiming to be the account of the same month is worse than either being wrong. But it means a genuine correction to a report a client has already seen has **no path** — the database refuses, which is the safe failure and not a usable one. ✅ **M10 built it, and Ricky's call was that it does not re-issue anything.** Nothing in this system emails or exports a report, so the published body stays exactly as the client received it and remains the document of record; the correction is recorded beside it in the app and carried into the next composition's corrections section, behind the `corrections_stated` publication check. Adding a send would have been a decision about what a client receives, not an architecture change. `079c69d1` is untouched and still frozen at method `2026-08-a` while the composer has moved to `2026-08-c`.
- **In-app prompt sign-off was a CLI flag — closed by M10** `M10` ✅ — Carried from M6 and *not* closed in M7. `npm run probes --review <email>` is the only way to sign a prompt set off, and it now requires a real identity, so it is correct — it is just not a surface. M7 added the in-app publish gate, which was the other half of the same complaint, so this is the half that is left. It matters for M12's “a specialist other than Ricky runs a cycle unaided”: a specialist who cannot sign off a prompt set in the app cannot run a cycle unaided. Should be built alongside M8 or M9 rather than discovered in the handover. ✅ **Closed on 2026-08-19**, one milestone later than this note asked. Both routes now share one writer, so the CLI and the panel cannot drift into disagreeing about what a sign-off is. The card stays here only so the reasoning survives.
- **The report is composed fresh on every request, refusals included** — The panel and the CLI re-derive twelve refusals by re-running the whole control matcher. That is the right call — a stored refusal could claim one the method no longer makes — and it is noticeably the slowest endpoint in the app. Fine at one client and one plan; it will not be fine at ten, and the fix is caching keyed on the plan and the method version rather than storing the refusals.
